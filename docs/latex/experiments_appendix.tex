% Auto-generated by generate-latex from the canonical SQLite control plane
% DO NOT EDIT -- regenerate with: cargo run --release --bin generate-latex

\section{Experiments Portfolio}\label{sec:experiments-appendix}

Total experiments: 200.\\[0.5em]

\subsection{E-001: Cayley-Dickson Motif Census}\label{sec:e001}

\textbf{Binary:} \texttt{motif-census} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Exact enumeration of connected-component structure of diagonal zero-product graph for cross-assessor pairs at each CD doubling (dim=16,32,64,128,256).

\textbf{Run:} \texttt{cargo run --release --bin motif-census -- --dims 16,32,64,128,256 --details}

\textbf{Claims:} C-100, C-101, C-102, C-103, C-104, C-105, C-106, C-107, C-108, C-109, C-110

\subsection{E-002: Multi-Dataset GPU Ultrametric Sweep}\label{sec:e002}

\textbf{Binary:} \texttt{multi-dataset-ultrametric} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} yes

\textbf{Method:} For 9 catalogs: normalize, compute Euclidean distances, ultrametric fraction test with column-shuffled null. 10M triples x 1000 permutations. BH-FDR correction.

\textbf{Run:} \texttt{cargo run --release --bin multi-dataset-ultrametric -- --explore --n-triples 10000000 --n-permutations 1000}

\textbf{Claims:} C-071, C-436, C-437, C-438, C-439, C-440

\subsection{E-003: Real Cosmological Fit (Pantheon+ / DESI BAO)}\label{sec:e003}

\textbf{Binary:} \texttt{real-cosmo-fit} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Joint chi-square over 1578 Pantheon+ SNe + 7 DESI DR1 BAO bins. Analytic M\_B marginalization. Nelder-Mead. Lambda-CDM vs w0-CDM via delta-BIC.

\textbf{Run:} \texttt{cargo run --release --bin real-cosmo-fit}

\textbf{Claims:} C-200, C-201, C-202, C-203, C-204, C-205, C-206, C-207, C-208, C-209, C-210

\subsection{E-004: Kerr Shadow Boundaries}\label{sec:e004}

\textbf{Binary:} \texttt{kerr-shadow} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Bardeen shadow boundary (alpha, beta) for Kerr BH at given spin and inclination.

\textbf{Run:} \texttt{cargo run --release --bin kerr-shadow -- --spin 0.998 --n-points 1000 --inclination 17}

\textbf{Claims:} C-301, C-302, C-303, C-304, C-305, C-306, C-307, C-308, C-309, C-310

\subsection{E-005: Zero-Divisor Graph Invariants}\label{sec:e005}

\textbf{Binary:} \texttt{zd-search} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Build sedenion ZD interaction graph, compute graph-theoretic invariants. Extend to dim=32.

\textbf{Run:} \texttt{cargo run --release --bin zd-search -- --dim 16 --max-pairs 5000}

\textbf{Claims:} C-050, C-051, C-052, C-053, C-054, C-055, C-056, C-057, C-058, C-059, C-060

\subsection{E-006: Gravastar TOV Parameter Sweep}\label{sec:e006}

\textbf{Binary:} \texttt{gravastar-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Solve TOV for three-layer gravastar across polytropic indices and target masses.

\textbf{Run:} \texttt{cargo run --release --bin gravastar-sweep -- --n-gamma 32 --n-mass 32 --output data/csv/gravastar\_sweep.csv}

\textbf{Claims:} C-400, C-401, C-402, C-403, C-404, C-405, C-406, C-407, C-408, C-409, C-410

\subsection{E-007: Tensor Network / PEPS Entropy}\label{sec:e007}

\textbf{Binary:} \texttt{tensor-network} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} Classical tensor network simulator: Bell/GHZ states, random circuits, SVD entropy, PEPS boundary MPS.

\textbf{Run:} \texttt{cargo run --release --bin tensor-network -- scaling --n-min 2 --n-max 12 --output data/csv/entropy\_scaling.csv}

\textbf{Claims:} C-350, C-351, C-352, C-353, C-354, C-355, C-356, C-357, C-358, C-359, C-360

\subsection{E-008: GWTC-3 Mass Clumping (Dip Test)}\label{sec:e008}

\textbf{Binary:} \texttt{mass-clumping} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} Hartigan dip test for multimodality of BBH mass distribution. Permutation-based p-value.

\textbf{Run:} \texttt{cargo run --release --bin mass-clumping -- --n-permutations 10000 --seed 42}

\textbf{Claims:} C-007

\subsection{E-009: Negative-Dimension Eigenvalue Convergence}\label{sec:e009}

\textbf{Binary:} \texttt{neg-dim-eigen} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Eigenvalues of H = T(k) + V(x) with fractional kinetic operator, imaginary-time evolution, epsilon->0 sweep.

\textbf{Run:} \texttt{cargo run --release --bin neg-dim-eigen -- sweep --alpha -1.5 --eps-start 0.5 --eps-end 0.01 --eps-steps 20 --output data/csv/neg\_dim\_convergence.csv}

\textbf{Claims:} C-420, C-421, C-422, C-423, C-424, C-425

\subsection{E-010: Materials Science Baselines (JARVIS + AFLOW)}\label{sec:e010}

\textbf{Binary:} \texttt{materials-baseline} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} JARVIS-DFT and AFLOW datasets, Magpie-style featurization, OLS regression for formation energy and band gap.

\textbf{Run:} \texttt{cargo run --release --bin materials-baseline -- --data-dir data/external --seed 42}

\textbf{Claims:} none

\subsection{E-011: Cross-Stack Locality Comparison (Experiment A)}\label{sec:e011}

\textbf{Binary:} \texttt{cross-stack-locality} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} Compare adjacency-locality metrics across three independent constraint systems: Stack 1 (E10 Billiard): Wall-transition sequence from HyperbolicBilliard, E10 Dynkin graph. Metric: r\_e8 = fraction of consecutive pairs that are E8-adjacent. Null: ColumnIndependentNull (uniform random generator selection). Stack 2 (ET DMZ Walk): Navigation sequences through de Marrais Emanation Table. Metric: r\_dmz = fraction of consecutive ET cells that share a DMZ edge. Null: uniform random cell selection in the ET grid. Stack 3 (CD ZD Catamaran/Twist): Twist-product navigation across zero-divisor graph. Metric: r\_zd = fraction of consecutive basis-pair transitions that are ZD-adjacent. Null: uniform random basis-pair selection. All three use the common abstract model: generator-driven piecewise geodesic dynamics (generator set G, constraint graph Gamma subset G x G, sequence s\_1..s\_n, locality ratio r = |\{i : (s\_i, s\_\{i+1\}) in Gamma\}| / (n-1)). Prediction (ALP, C-476): all three r values significantly exceed their nulls. Falsifier: one or more shows r consistent with uniform/random.

\textbf{Run:} \texttt{cargo run --release --bin cross-stack-locality -- --n-bounces 10000 --n-permutations 1000 --seed 42}

\textbf{Claims:} C-476

\subsection{E-012: ET Discrete Billiard (Experiment B)}\label{sec:e012}

\textbf{Binary:} \texttt{et-billiard} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} Treat the Emanation Table as a discrete billiard wall set. Walls: ET constraints define forbidden/allowed transitions in (S, row, col) space. Reflections: when a trajectory hits an ET "wall" (DMZ boundary), apply the ET rule update (fill/hide involution or strut-constant increment). Symbolic dynamics: record the sequence of wall types hit (DMZ, label-line, empty). Phase structure: compute Lyapunov exponent (or mixing time) as a function of strut constant S. Compare against spectroscopy band classification (Dense/Sparse/Mixed from spectroscopy\_bands()). Prediction: spectroscopy band transitions correspond to qualitative phase changes in the toy billiard (e.g., Dense bands -> low Lyapunov, Sparse -> high Lyapunov). Falsifier: no correlation between spectroscopy classification and billiard dynamics.

\textbf{Run:} \texttt{cargo run --release --bin et-billiard -- --n-levels 4,5,6 --n-steps 10000 --seed 42}

\textbf{Claims:} C-476, C-477

\subsection{E-013: Sky-Limit-Set Correspondence (Experiment C)}\label{sec:e013}

\textbf{Binary:} \texttt{sky-limit-set} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Compare ET skybox pattern invariants to Coxeter group limit set invariants. Step 1: For each CD level N=4,5,6, compute the skybox (G x G grid) and extract: - box-kite count (= N-1) - emptiness clustering exponent (connected-component size distribution) - DMZ density (fraction of cells that are DMZ) - fill/hide period-doubling structure (from Four Corners rule) Step 2: For candidate Coxeter groups (A\_\{N-1\}, B\_\{N-1\}, D\_\{N-1\}), compute limit set invariants from the Coxeter matrix: - rank - fractal dimension of limit set - invariant measure density Step 3: Compare skybox invariants (Step 1) to limit set invariants (Step 2) via correlation and matching tests. Prediction (C-477): systematic correspondence between skybox pattern invariants and Coxeter limit set invariants at matching rank. Falsifier: no Coxeter group at any rank produces invariants matching the ET skybox.

\textbf{Run:} \texttt{cargo run --release --bin sky-limit-set -- --n-levels 4,5,6 --coxeter-types A,B,D}

\textbf{Claims:} C-477

\subsection{E-014: Dimensional Ladder APT Census (4D-4096D)}\label{sec:e014}

\textbf{Binary:} \texttt{dimensional-census} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Exhaustive and Monte Carlo Anti-Diagonal Parity Theorem verification across the full Cayley-Dickson dimensional ladder from dim=4 to dim=4096. Exhaustive mode: Enumerate ALL graph triangles in each zero-divisor component. For each triangle (a,b,c), compute eta via anti-diagonal parity formula: eta(a,b) = psi(lo\_a, hi\_b) XOR psi(hi\_a, lo\_b). Classify as pure (eta constant across 3 edges) or mixed (eta non-constant). Verify 1:3 pure:mixed ratio (Quarter Rule: pure*4 = total exactly). Verify Klein-four fiber symmetry: F(0,0) = F(0,1) = F(1,0) = F(1,1). Monte Carlo mode: Rejection-sample random node triples within components, accept only graph triangles (all 3 edges present), classify via APT. Deterministic with seed=42. Tier classification: dims 4-32 (TIER 0-1, <15s), dims 64-256 (TIER 2, \textasciitilde{}5s release), dims 512-1024 (TIER 3, minutes-hours CPU), dims 2048-4096 (TIER 4, GPU Monte Carlo).

\textbf{Run:} \texttt{cargo run --release --bin dimensional-census -- --slow --details}

\textbf{Claims:} C-570, C-571, C-572

\subsection{E-015: GF(2) Separating Degree Formula Verification (dim 32-256)}\label{sec:e015}

\textbf{Binary:} \texttt{dimensional-census} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} For each Cayley-Dickson dimension d in \{32, 64, 128, 256\}, compute the minimum-degree GF(2) polynomial that separates all motif classes in PG(log2(d)-2, 2). Uses greedy partition refinement for d=256 where brute-force is infeasible (16 classes, C(127,6) candidate tuples). Verifies the formula min\_degree = log2(d) - 2 at each step.

\textbf{Run:} \texttt{cargo test -p gororoba\_algebra --release --lib -- test\_separating\_degree\_formula\_universality --nocapture}

\textbf{Claims:} C-599, C-600

\subsection{E-016: A-infinity Homotopy Gravastar Obstruction Sweep}\label{sec:e016}

\textbf{Binary:} \texttt{gravastar-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Construct a SedenionAInfinity algebra (m\_2=CD product, m\_3=associator) and compute the obstruction norm (Frobenius / dim\textasciicircum{}\{3/2\}). Map obstruction to Bowers-Liang anisotropy parameter lambda via linear coupling. Sweep coupling from 0 to 0.5 and solve the anisotropic gravastar TOV equation at each point to find the stability window.

\textbf{Run:} \texttt{cargo test -p cosmology\_core --release --lib -- homotopy\_bridge --nocapture}

\textbf{Claims:} C-611, C-612, C-613, C-614, C-615

\subsection{E-017: 7-Clique Multi-Resonator Entropy Trap Simulation}\label{sec:e017}

\textbf{Binary:} \texttt{entropy-trap} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Build a 7-channel TCMT multi-resonator system from sedenion box-kite components. Each channel is an independent KerrCavity with resonance frequency offset by the component index. Verify zero crosstalk for uncoupled channels, compute per-channel Shannon entropy and pairwise mutual information via 2D histogram binning, and sweep absorption spectrum across all 7 resonance frequencies.

\textbf{Run:} \texttt{cargo test -p optics\_core --release --lib -- entropy\_trap --nocapture}

\textbf{Claims:} C-616, C-617, C-618, C-619, C-620

\subsection{E-018: He-4 BEC Thermodynamics and Two-Fluid Dynamics}\label{sec:e018}

\textbf{Binary:} \texttt{he4-superfluid} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Compute ideal BEC critical temperature via zeta(3/2) formula, verify condensate fraction law 1-(T/T\_c)\textasciicircum{}(3/2), validate Landau superfluid density fraction with exponent 5.6, and simulate 0D two-fluid relaxation dynamics using RK4 with relaxation timescales tau\_rho = 1 us, tau\_t = 100 us. Verify mass conservation, equilibrium relaxation, and thermal relaxation through the lambda transition.

\textbf{Run:} \texttt{cargo test -p quantum\_core --release --lib -- superfluid --nocapture \&\& cargo test -p quantum\_core --release --lib -- two\_fluid --nocapture}

\textbf{Claims:} C-621, C-622, C-623, C-624, C-625, C-626, C-627

\subsection{E-019: Gross-Pitaevskii Ground State and Vortex Calculation}\label{sec:e019}

\textbf{Binary:} \texttt{he4-superfluid} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Solve the Gross-Pitaevskii equation using Strang split-operator method with FFT-based kinetic evolution. Compute ground states via imaginary-time propagation in a harmonic trap (omega=1), verifying energy recovery E=omega/2 for the linear case (g=0). Test that repulsive interactions (g=50) raise the energy above the linear value. Verify norm preservation during real-time evolution and density centering on the trap.

\textbf{Run:} \texttt{cargo test -p quantum\_core --release --lib -- gross\_pitaevskii --nocapture}

\textbf{Claims:} C-628, C-629

\subsection{E-021: Tessarines vs. Mixed-Quaternions Categorical Distinction Census}\label{sec:e021}

\textbf{Binary:} \texttt{tessarines-mixed-quaternions-census} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} 

\textbf{Run:} \texttt{}

\textbf{Claims:} none

\subsection{E-022: Albert Algebra Commutativity and Exceptional Structure Census}\label{sec:e022}

\textbf{Binary:} \texttt{albert-algebra-structure-census} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} 

\textbf{Run:} \texttt{}

\textbf{Claims:} none

\subsection{E-023: Composition Algebra Taxonomy: Two-Axis Classification Framework}\label{sec:e023}

\textbf{Binary:} \texttt{composition-algebra-taxonomy} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} 

\textbf{Run:} \texttt{}

\textbf{Claims:} none

\subsection{E-024: Registry Event Tracking Infrastructure}\label{sec:e024}

\textbf{Binary:} \texttt{registry-event-tracker} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} 

\textbf{Run:} \texttt{}

\textbf{Claims:} none

\subsection{E-026: Third-Party Source Verification Infrastructure}\label{sec:e026}

\textbf{Binary:} \texttt{third-party-source-verifier} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} 

\textbf{Run:} \texttt{}

\textbf{Claims:} none

\subsection{E-027: Topology-Imbalance Correlation via Persistent Homology (Thesis 1)}\label{sec:e027}

\textbf{Binary:} \texttt{e027-topology-analyzer} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} VR persistent homology on velocity point cloud, CD psi-weighted triangle imbalance, Pearson correlation + Fisher-Yates permutation test

\textbf{Run:} \texttt{cargo run --bin e027-topology-analyzer -- --grid-size 16 --epsilon-steps 20 --max-points 100 --n-permutations 200}

\textbf{Claims:} C-657, C-658, C-659

\subsection{E-028: Lepton Mass Ratio Filtration via Patricia Trie (Thesis 2 Validation)}\label{sec:e028}

\textbf{Binary:} \texttt{lattice-filtration-analyzer} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} Patricia trie survival depth spectrum for 256 sedenion pairs, k-means depth clustering, lepton mass ratio prediction vs PDG values, Fibonacci and sedenion collision storms with power-law latency classification

\textbf{Run:} \texttt{cargo run --release --bin lattice-filtration-analyzer -- --n-steps 5000 --seed 42}

\textbf{Claims:} none

\subsection{E-029: A-infinity Correction Tensor via Neural Homotopy Search (Thesis 3 Validation)}\label{sec:e029}

\textbf{Binary:} \texttt{neural-homotopy-trainer} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} Sedenion associator tensor m\_3 construction, Burn MLP training (Autodiff+NdArray+Adam, 256->128->64->16), pentagon-constrained batch coordinate descent, perturbation robustness testing

\textbf{Run:} \texttt{cargo run --release --bin neural-homotopy-trainer -- --n-steps 500 --n-restarts 5 --batch-size 8 --seed 42}

\textbf{Claims:} C-671, C-672, C-673

\subsection{E-030: TX-1: Imbalance-Modulated Collision Dynamics (T1 x T4)}\label{sec:e030}

\textbf{Binary:} \texttt{thesis-cross-tx1} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} 3D toroidal random walk with CD product noise, imbalance density modulates local noise amplitude via alpha parameter, return-time shell binning, power-law gamma fitting

\textbf{Run:} \texttt{cargo run --release --bin thesis-cross-tx1 -- --grid\_size 16 --n\_steps 50000}

\textbf{Claims:} none

\subsection{E-031: TX-2: Viscosity-to-Filtration Loop (T2 x T4)}\label{sec:e031}

\textbf{Binary:} \texttt{thesis-cross-tx2} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Imbalance -> viscosity -> LBM evolution -> velocity field -> lattice filtration -> latency law classification, lambda sweep for coupling strength

\textbf{Run:} \texttt{cargo run --release --bin thesis-cross-tx2 -- --grid\_size 16 --lbm\_steps 500}

\textbf{Claims:} none

\subsection{E-032: Thesis 2: 3D Associator-Coupled Shear Thickening}\label{sec:e032}

\textbf{Binary:} \texttt{thesis2-3d-thickening} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} SedenionField local associator norm -> coupling field, LBM evolve\_non\_newtonian with Kolmogorov forcing, sweep over alpha and power-law index, compare Newtonian vs non-Newtonian max velocity

\textbf{Run:} \texttt{cargo run --release --bin thesis2-3d-thickening -- --grid\_size 16 --lbm\_steps 500}

\textbf{Claims:} none

\subsection{E-033: Grand Synthesis: All Four Theses Pass Gate Simultaneously}\label{sec:e033}

\textbf{Binary:} \texttt{thesis-synthesis-engine} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} Orchestrated execution of T1 (spatial correlation), T2 (power-law viscosity), T3 (plateau detection + pentagon optimization), T4 (shell return-time power law). Each thesis evaluated independently with falsification gate.

\textbf{Run:} \texttt{cargo run --release --bin thesis-synthesis-engine -- --thesis all --output-dir data/evidence/synthesis\_final}

\textbf{Claims:} C-674

\subsection{E-034: Kubo Linear-Response Imbalance-Viscosity Coupling Derivation}\label{sec:e034}

\textbf{Binary:} \texttt{kubo-coupling-analysis} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} yes

\textbf{Method:} GPU-accelerated exact diagonalization of dim=8 Heisenberg chain with CD interpolation. Fine lambda sweep (100 points) at T=0.5: compute spin Drude weight D\_S, thermal Drude weight K\_th, integrated spectral weight I0\_S, and ballistic fraction B=D\_S/I0\_S. Temperature dependence at 6 probe lambdas. Analytical fits (power law, exponential) and recommended coupling parameters.

\textbf{Run:} \texttt{cargo run --release --features gpu --bin kubo-coupling-analysis}

\textbf{Claims:} C-678, C-679, C-680, C-681

\subsection{E-035: Reynolds Independence: Decaying Turbulence Scaling (Experiment C)}\label{sec:e035}

\textbf{Binary:} \texttt{warp-gpu-experiment} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} yes

\textbf{Method:} Direct Numerical Simulation of decaying turbulence on cubic grids (8\textasciicircum{}3, 16\textasciicircum{}3, 32\textasciicircum{}3, 64\textasciicircum{}3) using GPU LBM (D3Q19). Measures enstrophy decay exponent alpha and Betti-1 topological persistence. Compares decay normalized by initial enstrophy and eddy turnover time to establish resolution-independence threshold.

\textbf{Run:} \texttt{bash run\_reynolds\_sweep.sh}

\textbf{Claims:} C-682

\subsection{E-036: STAR Spin Correlation Decoherence Analysis}\label{sec:e036}

\textbf{Binary:} \texttt{spin-decoherence-pipeline} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Mapping of STAR 2026 spin correlation data to CD imbalance density. Verifies monotonic decay of entanglement and sudden death threshold in the algebraic vacuum limit.

\textbf{Run:} \texttt{cargo run --release --bin spin-decoherence-pipeline}

\textbf{Claims:} none

\subsection{E-037: Single-Minus Amplitude Imbalance Survey}\label{sec:e037}

\textbf{Binary:} \texttt{amplitude-imbalance-survey} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Exhaustive search of the single-minus gluon amplitude support region R1. Maps support region to the global minima of the algebraic imbalance landscape (zero-imbalance chambers).

\textbf{Run:} \texttt{cargo run --release --bin amplitude-imbalance-survey}

\textbf{Claims:} C-776, C-777

\subsection{E-038: LHC 2025 QGP Spin Synthesis}\label{sec:e038}

\textbf{Binary:} \texttt{lhc-2025-qgp-synthesis} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Integration of Run 3 ALICE (Pb-Pb 5.36 TeV) and CMS (pPb 8.16 TeV) results. Predictive modeling using QGPImbalanceBridge to map vorticity and temperature to CD imbalance. Validates algebraic twist correction against CMS azimuthal sign challenge.

\textbf{Run:} \texttt{cargo run --release --bin lhc-2025-qgp-synthesis}

\textbf{Claims:} C-778, C-779

\subsection{E-039: Wavelet Bridging Accuracy Sweep}\label{sec:e039}

\textbf{Binary:} \texttt{wavelet-bridging-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Haar DWT hard-threshold compression vs negative-d reservoir bridging on forced-diffusion surrogate. Sweeps rho in \{0.2, 0.5, 1.0, 1.5, 2.0\} and eps in \{1e-3, 3e-3, 1e-2, 3e-2\}. Reports median RelErr improvement and concurrency.

\textbf{Run:} \texttt{cargo run --release --bin wavelet-bridging-sweep}

\textbf{Claims:} C-703

\subsection{E-040: Meltdown Gating Concurrency}\label{sec:e040}

\textbf{Binary:} \texttt{meltdown-gating-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Adaptive epsilon meltdown gating on forced-diffusion surrogate. Sweeps rho in \{0.5, 1.0, 1.5, 2.0\}. Compares fixed-eps p95 concurrency vs adaptive gating. Reports p95 reduction and RelErr.

\textbf{Run:} \texttt{cargo run --release --bin meltdown-gating-sweep}

\textbf{Claims:} C-704

\subsection{E-041: Wavelet ACF Exponential Calibration}\label{sec:e041}

\textbf{Binary:} \texttt{wavelet-acf-calibration} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Measures autocorrelation function of unresolved wavelet energy on forced-diffusion surrogate. Sweeps eps in \{1e-3, 3e-3, 1e-2, 3e-2\}. Fits log(R(tau)) \textasciitilde{} -kappa*tau*dt via OLS. Reports R2 and kappa.

\textbf{Run:} \texttt{cargo run --release --bin wavelet-acf-calibration}

\textbf{Claims:} C-705

\subsection{E-042: CHSH Wavelet Compression}\label{sec:e042}

\textbf{Binary:} \texttt{chsh-wavelet-compression} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} Software simulation of CHSH Bell-inequality experiment. Generates Binomial-sampled coincidence time series for 4 angle settings. Applies Haar wavelet compression retaining coarsest coefficient. Computes S\_compressed and verifies S > 2.0.

\textbf{Run:} \texttt{cargo run --release --bin chsh-wavelet-compression}

\textbf{Claims:} C-706

\subsection{E-043: Casimir Plate Force Comparison}\label{sec:e043}

\textbf{Binary:} \texttt{casimir-plate-compare} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Computes Casimir force for Si/Ge/SiO2 symmetric plate pairs using corrected Lifshitz formula with Gauss-Legendre quadrature over transverse momentum. Verifies Si > Ge > SiO2 force ordering and eta < 1 at T=0.

\textbf{Run:} \texttt{cargo run --release --bin casimir-plate-compare}

\textbf{Claims:} C-738, C-739

\subsection{E-044: Casimir Drude vs Plasma Model Discrepancy}\label{sec:e044}

\textbf{Binary:} \texttt{casimir-drude-plasma} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Computes Drude-plasma discrepancy for Au/Ag/Cu metallic plates at separations 100nm-5um. Tests n=0 TE contribution controversy: Drude gives r\_TE=0, plasma gives finite r\_TE from omega\_p. Measures percentage discrepancy growth with distance.

\textbf{Run:} \texttt{cargo run --release --bin casimir-drude-plasma}

\textbf{Claims:} C-740, C-741

\subsection{E-045: Wow! Signal Transcription Integrity}\label{sec:e045}

\textbf{Binary:} \texttt{wow-signal-analysis} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Validates wow1977\_transcription.csv by SHA-256 checksum and character-to-intensity mapping. Verifies all 6 characters of 6EQUJ5 decode to [6, 14, 26, 30, 19, 5] via base-36. Checks background intensity levels.

\textbf{Run:} \texttt{cargo run --release --bin wow-signal-analysis -- transcribe}

\textbf{Claims:} C-769

\subsection{E-046: Wow! Signal Drift Rate Kinematics}\label{sec:e046}

\textbf{Binary:} \texttt{wow-signal-analysis} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Computes analytical Earth-motion drift rate bounds at the Wow! signal locale. Uses orbital velocity (29783 m/s) and Big Ear rotational velocity (355 m/s at lat 40.2N) to derive worst-case |drift| < 0.37 Hz/s at 1420.405 MHz.

\textbf{Run:} \texttt{cargo run --release --bin wow-signal-analysis -- drift-rate}

\textbf{Claims:} C-770

\subsection{E-047: BL 6EQUJ5 ABACAD Cadence Falsification}\label{sec:e047}

\textbf{Binary:} \texttt{wow-signal-analysis} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Parses BL 6EQUJ5 GBT manifest, applies ABACAD cadence filter (ON-OFF-ON-OFF-ON-OFF), and verifies zero candidates survive discrimination. Requires filterbank data download for full execution.

\textbf{Run:} \texttt{cargo run --release --bin wow-signal-analysis -- summary}

\textbf{Claims:} C-771

\subsection{E-048: BL 6EQUJ5 Persistent Homology Comparison}\label{sec:e048}

\textbf{Binary:} \texttt{wow-topology} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Computes persistent homology of ON and OFF cadence point clouds from BL 6EQUJ5 candidate features. Compares via Wasserstein-2 and bottleneck distances. Permutation test with 100 shuffles for topological indistinguishability.

\textbf{Run:} \texttt{cargo run --release --bin wow-topology -- morphology}

\textbf{Claims:} C-772

\subsection{E-049: BL 6EQUJ5 Ultrametric Ecology}\label{sec:e049}

\textbf{Binary:} \texttt{wow-topology} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Ultrametric fraction test on BL 6EQUJ5 candidate feature vectors. 10000 triples, 1000 permutations for null distribution. Tests whether RFI candidate structure shows non-trivial ultrametric organization vs column-shuffled null.

\textbf{Run:} \texttt{cargo run --release --bin wow-topology -- ultrametric}

\textbf{Claims:} C-773

\subsection{E-050: Rust-Native BL 6EQUJ5 Doppler Drift Search and ABACAD Pipeline}\label{sec:e050}

\textbf{Binary:} \texttt{bl-doppler-search} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} Pure Rust de-Doppler shift-and-add narrowband signal search on BL 6EQUJ5 GBT filterbank HDF5 files (medium-res 0002). Per-coarse-channel SNR detection with MAD normalization, followed by ABACAD cadence discrimination (signal in ALL ON, NONE of OFF). Replaces turboSETI Python dependency.

\textbf{Run:} \texttt{cargo run --release --bin bl-doppler-search --features hdf5-export -- scan --res 0002 \&\& cargo run --release --bin bl-doppler-search -- cadence \&\& cargo run --release --bin bl-doppler-search -- export}

\textbf{Claims:} C-771, C-772, C-773

\subsection{E-051: ZD Resonance Tau Sweep with Control}\label{sec:e051}

\textbf{Binary:} \texttt{zd-resonance-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} yes

\textbf{Method:} GPU LBM tau sweep at 10 values [0.55..1.00] with ZD modulation amplitude 0.2 (sweep) and 0.0 (control). Each run: 500 steps on 64\textasciicircum{}3 grid, rho\_mean extraction, Hann-windowed FFT, top-5 peak detection with ghost frequency check. Seeding sweep varies pc.lambda at fixed tau. Linear regression of peak\_freq vs tau tests Re-dependence.

\textbf{Run:} \texttt{cargo run --release --bin zd-resonance-sweep -- sweep \&\& cargo run --release --bin zd-resonance-sweep -- control \&\& cargo run --release --bin zd-resonance-sweep -- analyze}

\textbf{Claims:} C-792

\subsection{E-052: Pathion Sink Stability Comparison and Coupling Sweep}\label{sec:e052}

\textbf{Binary:} \texttt{pathion-sink-test} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Sedenion field on 32\textasciicircum{}3 grid with toroidal vortex IC. Compare stability (instability onset step) with and without PathionSink (coupling=0.1, damping=0.01). Sweep coupling [0.01, 0.05, 0.1, 0.5, 1.0] to test monotonic stabilization.

\textbf{Run:} \texttt{cargo run --release --bin pathion-sink-test -- compare \&\& cargo run --release --bin pathion-sink-test -- sweep}

\textbf{Claims:} C-794

\subsection{E-053: Chirality-Algebra Lock 2x2 Matrix Test}\label{sec:e053}

\textbf{Binary:} \texttt{chirality-lock-test} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Run 4 configurations on 32\textasciicircum{}3 sedenion grid: (A) Standard CD + Standard vorticity, (B) Standard CD + Flipped, (C) Conjugated CD + Standard, (D) Conjugated CD + Flipped. 300 steps each. Measure helicity sign, enstrophy, energy, associator norm, stability.

\textbf{Run:} \texttt{cargo run --release --bin chirality-lock-test -- matrix}

\textbf{Claims:} C-795

\subsection{E-054: Moonshine Filterbank ON/OFF Discrimination}\label{sec:e054}

\textbf{Binary:} \texttt{moonshine-filterbank} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Project 1000 synthetic ON-source chunks (Gaussian noise + harmonic signal at SNR=0.1) and 1000 OFF-source chunks (pure noise) onto Leech Lattice via Babai CVP. Classify hole types, compute deep hole fractions. Two-proportion z-test for statistical significance.

\textbf{Run:} \texttt{cargo run --release --bin moonshine-filterbank -- synth}

\textbf{Claims:} C-796

\subsection{E-055: Integrated Wow! + Sedenion Synthesis Pipeline}\label{sec:e055}

\textbf{Binary:} \texttt{wow-sedenion-synthesis} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} End-to-end pipeline: synthetic signal generation (4096 samples with ghost frequency injection), FFT spectral analysis, Leech Lattice projection with deep hole classification, ultrametric fraction test on peak frequencies. Consolidated report with all metrics.

\textbf{Run:} \texttt{cargo run --release --bin wow-sedenion-synthesis -- synth}

\textbf{Claims:} C-792, C-796

\subsection{E-056: Lattice-Hawking Spectrum Sweep}\label{sec:e056}

\textbf{Binary:} \texttt{lattice-hawking-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Sweep grid resolutions N=16,32,64,128 with radial inflow analog black hole. For each N: find acoustic horizon, compute surface gravity and Hawking temperature, compare ideal/lattice-cutoff/viscosity-cutoff spectra, fit effective temperatures.

\textbf{Run:} \texttt{cargo run --release --bin lattice-hawking-sweep}

\textbf{Claims:} C-780

\subsection{E-057: CD Associator Entropy Phase Census}\label{sec:e057}

\textbf{Binary:} \texttt{algebra-entropy-census} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Sweep Cayley-Dickson dimensions 2,4,8,16,32,64,128,256. For each: sample 10000 random unit triples, compute associator norms, histogram into 100 bins, compute Shannon entropy H. Report delta\_H at each transition, zero divisor density.

\textbf{Run:} \texttt{cargo run --release --bin algebra-entropy-census}

\textbf{Claims:} C-781

\subsection{E-058: CHSH-Betti Correlation Sweep}\label{sec:e058}

\textbf{Binary:} \texttt{chsh-betti-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} yes

\textbf{Method:} Run GPU FP64 LBM at 4 configurations (varying tau and forcing amplitude). For each: 500 steps at 32\textasciicircum{}3, snapshot every 10 steps. Extract y-profile (flow variation direction) for CHSH wavelet S. Adaptive p75 threshold for VR Betti-1. Spearman correlation between S and B1 across snapshots.

\textbf{Run:} \texttt{cargo run --release --bin chsh-betti-sweep}

\textbf{Claims:} C-782

\subsection{E-059: Pulsed vs Steady Entropy Injection}\label{sec:e059}

\textbf{Binary:} \texttt{meltdown-pulsed-gate} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Compare 3 protocols at equal total forcing energy: (A) steady rho=5.0, (B) pulsed 50\% duty cycle (rho=10.0), (C) pulsed 25\% duty cycle (rho=20.0). Measure p95 wavelet concurrency AND Shannon entropy at each level after 1000 steps with nu=0.001, dt=1e-3, Haar hard-threshold gating at eps=0.01,0.1,1.0.

\textbf{Run:} \texttt{cargo run --release --bin meltdown-pulsed-gate}

\textbf{Claims:} C-783

\subsection{E-060: Cross-Catalog rho-ghost-fft Spectral Analysis}\label{sec:e060}

\textbf{Binary:} \texttt{rho-ghost-fft} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Apply rho-ghost-fft power spectrum analysis to 10+ catalogs across 5 messengers (FRB, pulsar, SNIa, SETI, null controls). Detect phi\textasciicircum{}\{-1/2\} ghost frequency at 0.2138 normalized. Compare peak frequency, SNR, and FWHM across catalog sizes and wavebands.

\textbf{Run:} \texttt{cargo run --release --bin rho-ghost-fft -- <catalog>}

\textbf{Claims:} C-784, C-785, C-786, C-789

\subsection{E-061: Wow! GBT S-Band Deep Spectral Stack}\label{sec:e061}

\textbf{Binary:} \texttt{rho-ghost-fft} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Deep spectral analysis of 12 Breakthrough Listen GBT S-band (2.25 GHz) filterbank files from 6EQUJ5 follow-up observations. Stacked power spectrum after Doppler correction. Test for phi\textasciicircum{}\{-1/2\} ghost at S-band frequencies.

\textbf{Run:} \texttt{cargo run --release --bin rho-ghost-fft -- --catalog wow-gbt}

\textbf{Claims:} C-787

\subsection{E-062: Multiscale CWT Ricker Wavelet Ghost Analysis}\label{sec:e062}

\textbf{Binary:} \texttt{rho-ghost-fft} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Apply Continuous Wavelet Transform with Ricker wavelet to null-control datasets (Wow! S-band stacked, Swarm magnetic field). Test whether phi\textasciicircum{}\{-1/2\} ghost is a delocalized scale-invariant feature hidden from standard FFT. Compute ghost-band power fraction at width\textasciitilde{}24 (f\textasciitilde{}0.214).

\textbf{Run:} \texttt{python src/scripts/analysis/cwt\_ghost\_analysis.py}

\textbf{Claims:} C-787, C-790

\subsection{E-063: CUDA BF16 ZD Resonance 128\textasciicircum{}3 Tau Sweep}\label{sec:e063}

\textbf{Binary:} \texttt{zd-resonance-cuda} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} yes

\textbf{Method:} D3Q19 LBM at 128\textasciicircum{}3 with BF16 precision and per-cell Sedenion imbalance viscosity (Exponential model, lambda=3.0). Sweep tau from 0.55 to 1.00 (10 values). For each tau: nu\_base=(tau-0.5)/3.0, compute per-cell viscosity field, 1000 steps, extract KE every 10 steps, FFT + ghost peak detection. Control run at matching resolution with constant tau (no ZD modulation). Tests C-792 (frequency locking: |slope| < 0.01 in peak\_freq vs tau) and C-793 (production ghost detection: SNR > 3 at phi\textasciicircum{}\{-1/2\}).

\textbf{Run:} \texttt{cargo run --release --bin zd-resonance-cuda -- sweep --res 128 \&\& cargo run --release --bin zd-resonance-cuda -- control --res 128}

\textbf{Claims:} C-792, C-793

\subsection{E-064: CUDA BF16 ZD Resonance 256\textasciicircum{}3 Tau Sweep}\label{sec:e064}

\textbf{Binary:} \texttt{zd-resonance-cuda} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} yes

\textbf{Method:} D3Q19 LBM at 256\textasciicircum{}3 with BF16 precision. Same tau sweep protocol as E-063 but at 4x higher resolution (16.7M cells). Tests whether ghost peak narrows and converges toward 0.2138 with increasing resolution, paralleling real-data Cat1->Cat2 convergence (C-789).

\textbf{Run:} \texttt{cargo run --release --bin zd-resonance-cuda -- sweep --res 256 --steps 500 \&\& cargo run --release --bin zd-resonance-cuda -- control --res 256 --steps 500}

\textbf{Claims:} C-789, C-792, C-793

\subsection{E-065: CUDA BF16 Reynolds Number Threshold Sweep 128\textasciicircum{}3}\label{sec:e065}

\textbf{Binary:} \texttt{zd-resonance-cuda} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} yes

\textbf{Method:} Fix Sedenion coupling, sweep Reynolds number from laminar to turbulent by varying both tau (0.501-0.70) and forcing amplitude (0.001-0.5). Kolmogorov sinusoidal forcing F\_x = A*sin(2*pi*k*z/N). Motivated by real-data finding: Re\_eff=0.16 (laminar) did not produce sharp Ghost resonance. Tests whether Ghost onset requires specific turbulence intensity (L-787).

\textbf{Run:} \texttt{cargo run --release --bin zd-resonance-cuda -- reynolds-sweep --res 128}

\textbf{Claims:} C-792

\subsection{E-066: 4D ZD Resonance 32\textasciicircum{}3 x 32 Batch Sweep}\label{sec:e066}

\textbf{Binary:} \texttt{zd-resonance-4d} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} yes

\textbf{Method:} D3Q19 batch LBM at 32\textasciicircum{}3 x 32 (1.05M cells) with BF16 precision. 4th dimension (w) parametrizes Sedenion field phase offsets. Tests whether 4D spatial embedding (16 = 4\textasciicircum{}2) reveals coupling symmetries invisible in 3D. Control run with constant tau for null hypothesis.

\textbf{Run:} \texttt{cargo run --release --bin zd-resonance-4d -- sweep-32 \&\& cargo run --release --bin zd-resonance-4d -- control --res 32}

\textbf{Claims:} C-792, C-793

\subsection{E-067: 4D ZD Resonance 64\textasciicircum{}3 x 16 Batch Sweep}\label{sec:e067}

\textbf{Binary:} \texttt{zd-resonance-4d} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} yes

\textbf{Method:} D3Q19 batch LBM at 64\textasciicircum{}3 x 16 (4.2M cells, \textasciitilde{}1.4 GB GPU) with BF16 precision. Higher per-slice resolution than E-066, fewer w-slices. Tests resolution dependence of 4D ghost signatures.

\textbf{Run:} \texttt{cargo run --release --bin zd-resonance-4d -- sweep-64}

\textbf{Claims:} C-792, C-793

\subsection{E-068: Ghost Frequency Multidimensional Statistical Hardening}\label{sec:e068}

\textbf{Binary:} \texttt{ghost-spectral-audit} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} 7-method falsification sweep: (1) MAD robust noise floor with peak exclusion, (2) BH-FDR across all frequency bins, (3) Bonferroni correction for multiple testing, (4) permutation test (10000 shuffles), (5) sorted-distribution bootstrap null (5000 resamples, sorted data only), (6) Lomb-Scargle periodogram with Baluev analytic FAP, (7) multitaper Thomson PSD with F-test. Applied to 17 synthetic datasets (4 null controls, 5 sorted distributions, 4 signal injections, 4 mock catalogs). Combined meta-analysis via Fisher and Stouffer weighted Z.

\textbf{Run:} \texttt{cargo run --release --bin rho-ghost-fft -- batch --datasets data/csv/ghost\_audit/ --column value --permutations 1000 --bootstrap 5000}

\textbf{Claims:} C-784, C-785, C-786, C-789, C-797, C-798

\subsection{E-069: LBM Multi-Precision Comparison (BF16/FP32/FP64-GPU/FP64-CPU)}\label{sec:e069}

\textbf{Binary:} \texttt{lbm-precision-sampler} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} yes

\textbf{Method:} D3Q19 LBM with Kolmogorov sinusoidal forcing at identical ICs across four precision modes (GPU BF16, GPU FP32, GPU FP64, CPU FP64). Kinetic energy trace sampled periodically. FP64 GPU is reference. Tested at 16\textasciicircum{}3 and 32\textasciicircum{}3 grids.

\textbf{Run:} \texttt{cargo run --bin lbm-precision-sampler -- sample --res 32 --steps 200}

\textbf{Claims:} C-799, C-800

\subsection{E-070: Evidence Artifact Packaging and Checksum Verification}\label{sec:e070}

\textbf{Binary:} \texttt{evidence-package} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Walk data/evidence/ directory, compute SHA-256 checksums for all TOML/CSV/JSON artifacts, produce MANIFEST.toml with checksums and file sizes for reproducibility appendix.

\textbf{Run:} \texttt{cargo run --release --bin evidence-package -- bundle --skip-missing}

\textbf{Claims:} none

\subsection{E-071: Real-Data Ghost Frequency Re-Verification (Phase 5)}\label{sec:e071}

\textbf{Binary:} \texttt{ghost-spectral-audit} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} 5-method falsification audit (Rigorous+Bonferroni MAD, Permutation N=10000, Lomb-Scargle Baluev FAP, IAAFT N=1000, Multitaper F-test) on 4 real astronomical catalog datasets: CHIME FRB Cat2 bonsai\_dm (N=5045), ATNF pulsar DM (N=4233), ATNF pulsar period (N=4286), Pantheon+ MU\_SH0ES (N=1701). Tests whether phi\textasciicircum{}\{-1/2\} ghost frequency is detectable under rigorous statistical framework.

\textbf{Run:} \texttt{ghost-spectral-audit single --dataset <csv> --column value --permutations 10000 --bootstrap 10000 --seed 42}

\textbf{Claims:} C-805

\subsection{E-072: TX-2 Viscosity-to-Filtration Loop at 128\textasciicircum{}3 CPU FP64 (C-669 Refutation)}\label{sec:e072}

\textbf{Binary:} \texttt{thesis-cross-tx2} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} TX-2 cross-experiment at 128\textasciicircum{}3 (2M cells) CPU FP64, 500 LBM steps, lambda sweep [0.5, 1.0, 2.0, 5.0, 10.0]. Tests whether turbulent regime at higher resolution resolves Undetermined classification from original 16\textasciicircum{}3 (E-031).

\textbf{Run:} \texttt{cargo run --release --bin thesis-cross-tx2 -- --grid-size 128 --lbm-steps 500 --output-dir data/thesis\_lab/tx2\_128}

\textbf{Claims:} C-669

\subsection{E-073: Numerical Photon-Graviton Mixing Amplitude: Convergence and Ward Identity Verification}\label{sec:e073}

\textbf{Binary:} \texttt{} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Full numerical one-loop photon-graviton amplitude via worldline formalism (Bastianelli-Schubert 2005-2026). Three diagrams: irreducible (GL double integral over proper-time T and worldline modulus u), tadpole (3-way cross-validation: proper-time, Hurwitz zeta, Bernoulli series), external (vacuum polarization insertion). Pure magnetic field, spinor QED. Verification: gauge Ward identity, gravitational Ward identity, weak-field dichroism ratio 4:7.

\textbf{Run:} \texttt{cargo test -p gr\_core -- photon\_graviton}

\textbf{Claims:} C-819, C-820, C-821, C-822, C-823

\subsection{E-074: Son \& Chekhova (2026) SFWM Full Figure Reproduction}\label{sec:e074}

\textbf{Binary:} \texttt{sfwm-figure-repro} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Complete reproduction of all 4 figures from Son \& Chekhova (2026) arXiv:2601.23137v1. Phase-matching analysis using Sellmeier dispersion (Zelmon 1997) and paper-calibrated wavevector mismatches. Figures 2a-d: SFWM phenomenology (P\textasciicircum{}2 scaling, cos\textasciicircum{}2 polarization, coincidences, g(2)=1+a/P\textasciicircum{}2). Figures 3a-b: SPDC comparison (linear P, g(2)=1+a/P). Figures 4a-b: Maker fringes and rate dominance sweep.

\textbf{Run:} \texttt{cargo run --release --bin sfwm-figure-repro -- --output-dir data/sfwm\_repro}

\textbf{Claims:} C-832, C-833, C-834, C-835, C-836, C-837, C-838, C-839

\subsection{E-075: Arleo-Falmagne QGP Path-Length Scaling Reproduction}\label{sec:e075}

\textbf{Binary:} \texttt{arleo-falmagne-scaling} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Reproduce Arleo \& Falmagne (arXiv:2411.13258) QGP parton energy-loss scaling analysis. Optical Glauber model (hard-sphere nuclear density, Gauss-Legendre 2D quadrature) for Pb-Pb/Au-Au/Xe-Xe geometry. R\_AA fitting via Brent minimization to extract epsilon\_bar per centrality. Multi-system density scaling fit: epsilon\_bar = K * (dNch/dy / A\_perp) * L\textasciicircum{}beta with grid search over beta. v2/eccentricity vs d(ln R\_AA)/d(ln pT) linear relation for independent beta extraction.

\textbf{Run:} \texttt{cargo run --release --bin arleo-falmagne-scaling -- full --data-dir data/external}

\textbf{Claims:} C-840, C-841, C-842, C-843, C-844, C-845, C-846, C-847

\subsection{E-076: Ruan-Fan (2009) Fano TCMT Reproduction}\label{sec:e076}

\textbf{Binary:} \texttt{fano-scattering} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Reproduce Ruan \& Fan (2009, arXiv:0909.3323v2) TCMT Fano lineshape analysis for cylindrical scattering. Bessel function solver (complex argument, integer order) for J\_n, Y\_n, H\textasciicircum{}(1,2)\_n. Transfer-matrix Mie solver for concentric cylinders (TM polarization). Single/multi-channel TCMT with Drude permittivity. Lossless and lossy geometries. 4 figures: (1) lossless Fano phi=0,pi/2,pi; (2) lossy Fano same phases; (4) single-channel MDM (Mie vs TCMT); (5) multi-channel MDM with |l|<=2.

\textbf{Run:} \texttt{cargo run --release --bin fano-scattering -- --figure all --output-dir data/fano\_scattering}

\textbf{Claims:} C-848, C-849, C-850, C-851

\subsection{E-077: Landy 2008 TMM Absorption Spectrum Reproduction}\label{sec:e077}

\textbf{Binary:} \texttt{landy-absorber-tmm} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Reproduce Landy et al. (PRL 100, 207402, 2008) near-unity absorption at 11.48 GHz. Lorentz effective-medium model with independently tunable permittivity and permeability. Byrnes amplitude transfer matrix for multilayer stack. Sweep 9-14 GHz, 500 points. Single-layer and two-layer absorption spectra.

\textbf{Run:} \texttt{cargo run --release --bin landy-absorber-tmm}

\textbf{Claims:} C-853, C-854, C-855, C-856

\subsection{E-078: Immirzi Bridge Imbalance-Entropy Mapping}\label{sec:e078}

\textbf{Binary:} \texttt{} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Evaluate 6 candidate mappings from imbalance density phi to Immirzi parameter gamma. Key finding: imbalance-entropy bridge H(phi)/(pi*sqrt(3)) maps phi=3/8 to gamma \textasciitilde{} 0.122, within 2\% of gamma\_NZJ. Binary entropy at phi=1/2 recovers gamma\_BG exactly.

\textbf{Run:} \texttt{cargo test -p sign\_imbalance --lib immirzi\_bridge}

\textbf{Claims:} C-859, C-860, C-861

\subsection{E-079: Sterile-Neutrino Null-Result Audit (Seven-Stream)}\label{sec:e079}

\textbf{Binary:} \texttt{sterile-neutrino-audit} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Seven-stream null-result audit across the Sprint 43-44 spectral and materials sensor campaign. Streams C-703..C-706 cover spectral/wavelet processing sensors; C-707..C-709 cover materials optical sensors. Emits null\_confirmed for the experiment only when all seven streams carry a verified null result, preventing false confirmation when any stream remains open/theoretical.

\textbf{Run:} \texttt{cargo run --release --bin sterile-neutrino-audit -- --output reports/sterile\_neutrino\_audit.toml}

\textbf{Claims:} C-703, C-704, C-705, C-706, C-707, C-708, C-709

\subsection{E-080: Nacelle warp bubble parameter sweep (n=1..16)}\label{sec:e080}

\textbf{Binary:} \texttt{nacelle-warp-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} 2D energy density integration on y-z cross-section at x=0 for each nacelle count. Paper mode restricts to n=2,3,4 matching White 2025.

\textbf{Run:} \texttt{cargo run --release --bin nacelle-warp-sweep -- --paper-mode --velocity 0.1 --radius 100.0 --n-points 80}

\textbf{Claims:} C-869, C-870, C-874

\subsection{E-081: Worldline Casimir numerics: parallel plate validation}\label{sec:e081}

\textbf{Binary:} \texttt{worldline-casimir} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Monte Carlo worldline path integral with Gauss-Legendre quadrature for proper-time integration. 10k loops per T-sample, 500 points per loop.

\textbf{Run:} \texttt{cargo run --release --bin worldline-casimir -- --geometry plates --separation 1.0 --n-loops 10000 --n-profile 20}

\textbf{Claims:} C-871

\subsection{E-082: Algebraic warp sweep: CD tower vs standard GR energy comparison}\label{sec:e082}

\textbf{Binary:} \texttt{algebraic-warp-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} 2D energy integration with algebraic York time correction and sedenion stress-energy source. Coupling alpha=0.01, imbalance at imbalance attractor 3/8.

\textbf{Run:} \texttt{cargo run --release --bin algebraic-warp-sweep -- --mode tower --coupling-alpha 0.01 --velocity 0.1 --radius 100.0}

\textbf{Claims:} C-877, C-878, C-879

\subsection{E-083: Sedenion foliation imbalance-York time correlation analysis}\label{sec:e083}

\textbf{Binary:} \texttt{algebraic-warp-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Construct SedenionFoliation on ADM hypersurfaces, compute imbalance at each grid point, pair with York time from extrinsic curvature. Pearson correlation analysis.

\textbf{Run:} \texttt{cargo run --release --bin algebraic-warp-sweep -- --mode tower --coupling-alpha 0.1}

\textbf{Claims:} C-880

\subsection{E-084: ZD graph spectral dimension census}\label{sec:e084}

\textbf{Binary:} \texttt{zd-spectral-dimension} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} For each CD dim: build cross-assessor ZD graph, compute Laplacian, eigendecompose, compute P(t) on geometric t-grid, SG-smooth ln P vs ln t, extract d\_s plateau via central differences.

\textbf{Run:} \texttt{cargo run --release --bin zd-spectral-dimension -- --min-dim 16 --max-dim 512 --t-points 300}

\textbf{Claims:} C-922, C-923, C-926

\subsection{E-085: Associator entropy mechanism decomposition}\label{sec:e085}

\textbf{Binary:} \texttt{} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Sample random unit triples via Muller's method, compute associator norms, partition by ZD proximity threshold, compare conditional entropies.

\textbf{Run:} \texttt{cargo test -p algebra\_analysis -- associator\_entropy}

\textbf{Claims:} C-924, C-927

\subsection{E-086: ZD Quantum Chaos NNSD Census}\label{sec:e086}

\textbf{Binary:} \texttt{zd-quantum-chaos} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Compute Laplacian eigenvalues per ZD graph component, Gaussian KDE unfold (sigma = 0.1*range), build normalized spacing histogram, fit Brody parameter via method-of-moments bisection, compute KS statistics vs Poisson and GOE CDFs.

\textbf{Run:} \texttt{cargo run --release --bin zd-quantum-chaos -- --min-dim 64 --max-dim 256 --by-motif}

\textbf{Claims:} C-929, C-930

\subsection{E-087: Orthoplex Diffusion Dark Energy Fit}\label{sec:e087}

\textbf{Binary:} \texttt{orthoplex-cosmo-fit} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Bounded Nelder-Mead optimization over 5 parameters (omega\_m, h0, alpha, beta, t\_0) with k=63 fixed. Joint chi2 = chi2\_SN (full 1578x1578 STAT+SYS precision matrix via Cholesky, analytic M\_B marginalization) + chi2\_BAO (mixed isotropic + 2x2 anisotropic covariance) + chi2\_CC + chi2\_fsig8 + chi2\_CMB.

\textbf{Run:} \texttt{cargo run --release --bin orthoplex-cosmo-fit -- --k 63}

\textbf{Claims:} C-931, C-932

\subsection{E-088: Besag-Clifford Clustering Test at 256\textasciicircum{}3}\label{sec:e088}

\textbf{Binary:} \texttt{clustering-256} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} yes

\textbf{Method:} Besag-Clifford permutation test for ZD-imbalance clustering at 256\textasciicircum{}3 grid resolution. GPU-accelerated via Vulkan compute (besag\_clifford.wgsl). Adaptive stopping with Wilson CI.

\textbf{Run:} \texttt{cargo run --release --bin clustering-256 -- --grid 256 --max-perms 1000}

\textbf{Claims:} C-659

\subsection{E-089: Dark Halo Hunt: CDG-2 Consistency Across k Values}\label{sec:e089}

\textbf{Binary:} \texttt{dark-halo-hunt} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} yes

\textbf{Method:} Sweep k values [4,8,16,32,63], run LBM + dark halo detector at each k. CUDA path: SoA layout, L2 pinning, fused collision+streaming. Detect overdense low-velocity cells as halo candidates. Threshold: rho > density\_factor * rho\_mean AND |u| < velocity\_epsilon.

\textbf{Run:} \texttt{cargo run --release --bin dark-halo-hunt -- --cuda --grid 256 --steps 2000 --k-values 4,8,16,32,63}

\textbf{Claims:} none

\subsection{E-090: BIC Model Comparison: Arleo-Falmagne vs CUJET3.0 vs Fractional Langevin}\label{sec:e090}

\textbf{Binary:} \texttt{arleo-falmagne-scaling} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Compare R\_AA predictions from three QGP energy-loss models via Bayesian Information Criterion. Arleo-Falmagne (2 params: beta, K) vs CUJET3.0 (5 params: alpha\_s, mu\_M/T, c\_M, f\_E, f\_M) vs fractional Langevin (3 params: alpha, D\_s, T\_c). BIC = chi2 + k*ln(n). CUJET3.0 digitized from JHEP02(2016)169 Fig. 4. Langevin digitized from arXiv:2401.03757 Fig. 3. Evaluated against ALICE Pb-Pb 5.02 TeV 0-5\% R\_AA.

\textbf{Run:} \texttt{cargo run --release --bin arleo-falmagne-scaling -- bic-compare --data-dir data/external --pt-min 5.0}

\textbf{Claims:} C-938, C-939

\subsection{E-091: Flyby Crucible with Bivector-Embedded Chingon Drag}\label{sec:e091}

\textbf{Binary:} \texttt{flyby-crucible} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Integrate 6 spacecraft trajectories through Earth SOI with bivector-embedded 64D Chingon drag force. Unit-norm embedding with 3 blocks: angular momentum (axes 1-21), velocity (axes 22-42), cross-coupling (axes 43-63). Sign sensitivity via sign(h . v\_wind) where h = r x v. Compare predicted delta-V signs with observed flyby anomaly (Anderson 2008).

\textbf{Run:} \texttt{cargo run --release --bin flyby-crucible}

\textbf{Claims:} C-941, C-942

\subsection{E-092: EHT Pathion Shadow Angular Size for M87*}\label{sec:e092}

\textbf{Binary:} \texttt{pathion-shadow-cli} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Compute Schwarzschild photon sphere shadow angular size and sub-ring demagnification for M87* (mass 6.5e9 M\_sun, distance 16.8 Mpc). Shadow radius = 3*sqrt(3)/2 * r\_s. Sub-ring separation via exp(-pi) demagnification factor. Compare with EHT 2019 observation of \textasciitilde{}42 uas.

\textbf{Run:} \texttt{cargo run --release --bin pathion-shadow-cli}

\textbf{Claims:} C-943

\subsection{E-093: Riemann Resonance Sweep: Brody Parameter for Orthoplex Laplacians}\label{sec:e093}

\textbf{Binary:} \texttt{riemann-resonance-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Compute Laplacian eigenvalue spacing statistics for orthoplex K\_\{2,...,2\} graphs at CD dimensions 4,8,16,32,64,128,256. Classify via Brody parameter q (q=0 Poisson/integrable, q=1 GOE/chaotic). All orthoplex Laplacians have only 3 distinct eigenvalues (0, 2(k-1), 2k) so spacing statistics are degenerate.

\textbf{Run:} \texttt{cargo run --release --bin riemann-resonance-sweep}

\textbf{Claims:} C-944

\subsection{E-094: Zero-Divisor Graph Topology Census (Sedenion + Pathion)}\label{sec:e094}

\textbf{Binary:} \texttt{} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Construct basis participation graphs from exact 4-index zero-divisor detection via cd\_kernel::find\_zero\_divisors\_parallel. Vertices = basis elements, edges connect co-occurring indices in ZD pairs (e\_i+e\_j)(e\_k+/-e\_l)=0. Measure connected components, edge counts, and component sizes at dim=16 (sedenion) and dim=32 (pathion).

\textbf{Run:} \texttt{cargo nextest run -p materials\_core -- test\_zd\_graph}

\textbf{Claims:} C-949

\subsection{E-095: Three-body 64D flyby integration with body-specific triads}\label{sec:e095}

\textbf{Binary:} \texttt{flyby-crucible} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} RK4 integration of 6 spacecraft flybys with 3-body (Earth/Moon/Sun) 64D Chingon bivector drag. Body-specific triads: Earth triad for angular momentum block, Lunar triad for velocity block, Solar triad for cross-coupling block. JPL DE440 ephemeris for Moon/Sun positions.

\textbf{Run:} \texttt{cargo run --release --bin flyby-crucible -- --trace-h}

\textbf{Claims:} C-1101, C-1102

\subsection{E-096: ETA\_WAKE gravitational focusing wake parameter sweep}\label{sec:e096}

\textbf{Binary:} \texttt{flyby-crucible} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} Sweep ETA\_WAKE=[0.0,0.05,0.10,0.15,0.20] with 3-body 64D Chingon drag. Measures effect of asymmetric (cos theta\_wind) density modulation on 6 flyby delta-V predictions. Tests whether gravitational focusing wake can flip Rosetta-I sign.

\textbf{Run:} \texttt{for eta in 0.00 0.05 0.10 0.15 0.20; do cargo run --release --bin flyby-crucible -- --eta-wake \$eta; done}

\textbf{Claims:} C-1103, C-1104

\subsection{E-097: CUDA GPU vs CPU validation at 64D for flyby crucible}\label{sec:e097}

\textbf{Binary:} \texttt{flyby-crucible} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} Run flyby crucible with --gpu flag vs without. Compare all 6 flyby delta-V values. Tolerance: f32 precision (\textasciitilde{}1e-4 relative error).

\textbf{Run:} \texttt{cargo run --release --bin flyby-crucible -- --gpu --no-threebody --dt 2.0}

\textbf{Claims:} C-1105

\subsection{E-098: Dimensional scaling: 128D + 256D GPU flyby crucible}\label{sec:e098}

\textbf{Binary:} \texttt{flyby-crucible} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} Run flyby crucible with --dim 128 --gpu and --dim 256 --gpu. Compare sign patterns and magnitudes against 64D CPU baseline.

\textbf{Run:} \texttt{cargo run --release --bin flyby-crucible --features gpu -- --dim 256 --gpu}

\textbf{Claims:} C-1106, C-1107, C-1108, C-1109, C-1110

\subsection{E-099: 512D Bell inequality (CHSH) via associator torque correlations}\label{sec:e099}

\textbf{Binary:} \texttt{} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} chsh\_violation\_test(dim, n\_samples=1000, seed=42) at dims 16,32,64,512.

\textbf{Run:} \texttt{cargo test --release -p algebra\_experimental -- bell\_inequality --nocapture}

\textbf{Claims:} C-1111, C-1112

\subsection{E-100: 1024D DekaVoudon gauge sector analysis via sampled AVT}\label{sec:e100}

\textbf{Binary:} \texttt{} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} analyze\_1024d\_gauge\_sectors(n\_samples=1\_000\_000, seed=42). HigherAvt::sampled(1024, n, seed) with dedup via HashSet.

\textbf{Run:} \texttt{cargo test --release -p algebra\_experimental -- particle\_physics --nocapture}

\textbf{Claims:} C-1113, C-1114, C-1115

\subsection{E-101: Complex-time Wick rotation + WKB tunneling + Pathion shadow boundary}\label{sec:e101}

\textbf{Binary:} \texttt{} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} Unit tests: wick\_evolve at theta=0/pi/4/pi/2, wkb\_tunneling\_amplitude vs analytical, singularity\_avoidance\_test with/without Pathion, pathion\_shadow\_radius, adaptive\_wick\_evolve, pathion\_variance\_max\_eigenvalue.

\textbf{Run:} \texttt{cargo test --release -p gr\_core -- nbody\_integration --nocapture}

\textbf{Claims:} C-1116, C-1117

\subsection{E-102: 128D Routon non-associative entropy filter on LBM grid}\label{sec:e102}

\textbf{Binary:} \texttt{} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} Unit tests: measure\_routon\_chaos (uniform vs sparse), apply\_to\_lbm bias field, entropy\_filter\_solve convergence and damping, grid\_average\_entropy, avt\_entropy\_baseline\_connection.

\textbf{Run:} \texttt{cargo test --release -p quantum\_core -- intention\_operator --nocapture}

\textbf{Claims:} C-1118, C-1119

\subsection{E-103: Fractal spacetime D\_f=2.7 with Q-tensor metric: flyby + Pioneer test}\label{sec:e103}

\textbf{Binary:} \texttt{} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} Unit tests: scaling\_factor at r\_0, D\_f<4 monotonicity, anomalous acceleration sign, Q-tensor order parameter profile, radial/angular factors, disclination peak, metric positive-definiteness, Pioneer magnitude, flyby sign.

\textbf{Run:} \texttt{cargo test --release -p gr\_core -- fractal\_metric --nocapture}

\textbf{Claims:} C-1120, C-1121

\subsection{E-104: CMB Quadrupole-Octupole Alignment reconciliation via 1024D DekaVoudon cosmic web seeding}\label{sec:e104}

\textbf{Binary:} \texttt{} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} Unit tests: project\_axis\_is\_unit, galactic\_coords\_finite, isotropic\_seeding\_coverage, extract\_multipoles\_isotropic, anisotropic\_has\_larger\_quadrupole, angular\_separation, planck\_alignment\_direction.

\textbf{Run:} \texttt{cargo test --release -p cosmology\_core -- deka\_voudon\_cmb --nocapture}

\textbf{Claims:} C-1122, C-1123

\subsection{E-105: Unified Cayley-Dickson simulation engine end-to-end integration test}\label{sec:e105}

\textbf{Binary:} \texttt{} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} Unit tests: test\_engine\_new, test\_hawking\_temperature\_positive, test\_hawking\_spectrum\_planckian\_shape, test\_hawking\_sgr\_a\_temperature\_magnitude, test\_initialize\_subsystems, test\_cmb\_planck\_separation\_finite, test\_voudon\_hubble, test\_fractal\_flyby\_nonzero\_sign, test\_pioneer\_anomaly\_positive, test\_evidence\_matrix, test\_unified\_run\_all\_finite.

\textbf{Run:} \texttt{cargo test --release -p gororoba\_engine --lib -- singularitarian --nocapture}

\textbf{Claims:} C-1124, C-1125

\subsection{E-106: Majorana braid through CD tower at dims 16-128}\label{sec:e106}

\textbf{Binary:} \texttt{majorana-braid-crucible} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} Clifford braid via gamma\_matrices\_cl8(), CD braid via SignTableCache + rotate\_sparse(), friction = cumulative associator norm.

\textbf{Run:} \texttt{cargo run --release --bin majorana-braid-crucible -- braid --dim 16}

\textbf{Claims:} C-1133, C-1134, C-1136

\subsection{E-107: Box-kite fusion statistics at dim=16}\label{sec:e107}

\textbf{Binary:} \texttt{majorana-braid-crucible} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} cd\_basis\_mul\_sign\_iter(dim, lo, hi) for each assessor pair. Partition by sign, count distinct values.

\textbf{Run:} \texttt{cargo run --release --bin majorana-braid-crucible -- fusion --dim 16}

\textbf{Claims:} C-1135

\subsection{E-108: Braid-aligned CHSH test at dim=16}\label{sec:e108}

\textbf{Binary:} \texttt{majorana-braid-crucible} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} chsh\_braid\_aligned: rotate in ZD subplanes, measure correlators via associator\_measurement\_fast.

\textbf{Run:} \texttt{cargo run --release --bin majorana-braid-crucible -- aligned-chsh --dim 16 --n-samples 10000 --seed 42}

\textbf{Claims:} C-1139

\subsection{E-109: Complex-time Majorana braid Wick rotation sweep}\label{sec:e109}

\textbf{Binary:} \texttt{majorana-braid-crucible} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} complex\_time\_braid: SO(2) rotation with damping factor cos(theta)*sin(theta).

\textbf{Run:} \texttt{cargo run --release --bin majorana-braid-crucible -- complex-time --dim 16 --theta-steps 10}

\textbf{Claims:} C-1138

\subsection{E-110: ADM decomposition verification against exact solutions}\label{sec:e110}

\textbf{Binary:} \texttt{adm-decompose} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Numerical ADM decomposition with finite-difference extrinsic curvature. Compare against PG exact: alpha=1, beta\textasciicircum{}r=sqrt(2M/r), gamma=delta. Hamiltonian constraint residual for vacuum.

\textbf{Run:} \texttt{cargo run --release --bin adm-decompose -- --spacetime schwarzschild --mass 1.0 --radius 10.0 --n-points 20}

\textbf{Claims:} C-868, C-875

\subsection{E-111: Solar wind MHD simulation with Parker spiral B-field}\label{sec:e111}

\textbf{Binary:} \texttt{solar-wind-mhd-sim} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} lbm\_mhd: D3Q19 LBM coupled with induction equation B-field evolution. Parker spiral initialization, Zou-He velocity inlet, Guo forcing from Lorentz force J x B.

\textbf{Run:} \texttt{cargo run --release --bin solar-wind-mhd-sim -- --nx 128 --ny 32 --nz 32 --steps 5000 --tau 0.6 --b0 5.0 --v-sw 0.05 --snap-interval 500 --out data/experiments/e111\_solar\_wind}

\textbf{Claims:} none

\subsection{E-112: Solar wind MHD + dark matter halo gravitational coupling}\label{sec:e112}

\textbf{Binary:} \texttt{solar-wind-dm-mhd} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} lbm\_dm\_mhd: D3Q19 LBM + MHD (E-111 base) coupled with static NFW DM halo gravitational force field. DM force precomputed from NFW enclosed mass M(<r), combined with Lorentz force via Guo scheme. Quantifies max|F\_DM|/max|F\_Lorentz| ratio as null test.

\textbf{Run:} \texttt{cargo run --release --bin solar-wind-dm-mhd -- --nx 128 --ny 32 --nz 32 --steps 5000 --tau 0.6 --b0 5.0 --v-sw 0.05 --dm-density 0.3 --dm-m200 1e12 --dm-c200 10 --snap-interval 500 --out data/experiments/e112\_solar\_wind\_dm}

\textbf{Claims:} C-1143, C-1144, C-1145

\subsection{E-113: Multi-spacecraft solar wind MHD + DM null test replication}\label{sec:e113}

\textbf{Binary:} \texttt{solar-wind-dm-mhd} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Multi-spacecraft replication of E-112 DM null test using independent data sources (ACE MAG L2, WIND SWE+MFI, STEREO-A PLASTIC+IMPACT/MAG). Each dataset flows through spacecraft-specific parser -> *\_to\_omni() adapter -> OmniRecord -> generate\_ic\_from\_omni() -> LBM/MHD grid. RTN->GSE coordinate transform applied to STEREO-A data. Cross-validation via Pearson correlation, RMSE, and bias between overlapping hourly measurements. Confirms instrument independence of |F\_DM|/|F\_Lorentz| \textasciitilde{} O(10\textasciicircum{}-12) null result. DM-baryon drag force (sigma\_chi\_b) sweep identifies detection threshold.

\textbf{Run:} \texttt{cargo run --release --bin solar-wind-dm-mhd -- --ic-file <wind\_ic.csv> --nx 128 --ny 32 --nz 32 --steps 5000 --tau 0.7 --eta 0.01 --dm-density 0.3 --dm-m200 1e12 --dm-c200 10 --snap-interval 500}

\textbf{Claims:} C-1147, C-1148, C-1149, C-1150, C-1151, C-1152, C-1153, C-1154, C-1155

\subsection{E-114: Voyager radial profile reconstruction (1-157 AU)}\label{sec:e114}

\textbf{Binary:} \texttt{solar-wind-ic} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Multi-spacecraft radial profile reconstruction using Voyager 1/2, Pioneer 10/11, Ulysses, New Horizons, Juno, Cassini, and L1 (OMNI2) data. Each spacecraft's parser feeds through *\_to\_omni() adapter with heliocentric coordinates (r\_au, lat\_deg, lon\_deg). build\_radial\_profile() groups records by distance, computes median plasma parameters per distance bin. interpolate\_radial() uses log-linear interpolation for density (r\textasciicircum{}-2 scaling) and log-spaced x-grid for distance mapping. Validates Parker spiral B-field scaling: B\_r \textasciitilde{} r\textasciicircum{}-2, B\_phi \textasciitilde{} r\textasciicircum{}-1.

\textbf{Run:} \texttt{cargo run --release --bin solar-wind-ic -- --omni-file <staged\_omni\_hourly\_file> --voyager1-file <staged\_voyager\_merged\_file> --radial-mode --nx 128 --ny 32 --nz 32}

\textbf{Claims:} C-1157, C-1162, C-1165

\subsection{E-115: Ulysses latitudinal gradient characterization}\label{sec:e115}

\textbf{Binary:} \texttt{solar-wind-ic} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Ulysses SWOOPS + VHM/FGM data from the 1994-1995 fast latitude scan (1.3-5.4 AU, -80 to +80 deg heliographic latitude). ulysses\_latitudinal\_fit() extracts tanh transition parameters (fast\_speed, slow\_speed, transition\_lat, width). latitude\_modulation() applies the fitted profile to the LBM z-axis. Validates McComas et al. (2000) fast/slow bimodality: v\_polar > 1.5 * v\_equatorial.

\textbf{Run:} \texttt{cargo run --release --bin solar-wind-ic -- --omni-file <staged\_omni\_hourly\_file> --ulysses-swoops-file data/external/ulysses/swoops\_hourly.asc --latitudinal --lat-max-deg 80 --nx 128 --ny 32 --nz 64}

\textbf{Claims:} C-1163

\subsection{E-116: Multi-distance DM null sweep: r = \{1, 5, 10, 50, 100\} AU}\label{sec:e116}

\textbf{Binary:} \texttt{solar-wind-dm-mhd} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Radial IC generated by E-114 passed to solar-wind-dm-mhd with per-cell kappa(R) spatial drag coefficient. For each distance bin [r-dr, r+dr], extract the force ratio |F\_DM|/|F\_Lorentz| and report max. kappa\_field varies with NFW enclosed mass M(<r) and local DM number density n\_chi(r). Validates C-1156 (DM null invariance across full heliosphere).

\textbf{Run:} \texttt{cargo run --release --bin solar-wind-dm-mhd -- --ic-file data/output/radial\_ic.csv --nx 128 --ny 32 --nz 32 --steps 5000 --tau 0.7 --dm-r-min 1 --dm-r-max 100 --snap-interval 500}

\textbf{Claims:} C-1156, C-1167, C-1168

\subsection{E-117: Pioneer-Voyager radial cross-validation}\label{sec:e117}

\textbf{Binary:} \texttt{solar-wind-ic} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Cross-validate proton density, speed, and B-field between Pioneer 10/11 and Voyager 1/2 at overlapping heliocentric distances (5-80 AU). Group by distance bins (5 AU width), compute median and IQR for each spacecraft pair. Report Pearson correlation and RMSE. Solar cycle correction via OMNI2 sunspot number proxy.

\textbf{Run:} \texttt{cargo run --release --bin solar-wind-ic -- --voyager1-file <v1> --voyager2-file <v2> --pioneer10-file <p10> --pioneer11-file <p11> --radial-mode --nx 128}

\textbf{Claims:} C-1165, C-1169

\subsection{E-118: Outer heliosphere LBM validity assessment (Kn regime mapping)}\label{sec:e118}

\textbf{Binary:} \texttt{solar-wind-ic} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Compute Knudsen number Kn = lambda\_mfp / L\_cell at each radial distance from 1 to 157 AU using multi-spacecraft density and temperature profiles. Identify the Kn=1 transition radius (LBM -> kinetic boundary). Map Kn regime (Fluid/Transitional/Kinetic) across the heliosphere using classify\_knudsen().

\textbf{Run:} \texttt{cargo run --release --bin solar-wind-ic -- --radial-mode --voyager1-file <v1> --nx 256 --ny 32 --nz 32}

\textbf{Claims:} C-1159, C-1166

\subsection{E-119: Full heliosphere radial IC -> MHD+DM end-to-end pipeline}\label{sec:e119}

\textbf{Binary:} \texttt{solar-wind-dm-mhd} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} End-to-end pipeline: multi-spacecraft data -> radial IC (E-114) -> MHD+DM simulation with distance-parametrized NFW and spatial kappa(R). Validates the complete tomography engine from 1 to 100 AU. Reports force ratios, mass conservation, and stability diagnostics at each distance.

\textbf{Run:} \texttt{cargo run --release --bin solar-wind-dm-mhd -- --ic-file data/output/radial\_ic.csv --nx 256 --ny 32 --nz 32 --steps 10000 --tau 0.7 --eta 0.01 --dm-r-min 1 --dm-r-max 100}

\textbf{Claims:} C-1156, C-1157, C-1160, C-1168

\subsection{E-120: Termination shock MHD simulation with Voyager IC}\label{sec:e120}

\textbf{Binary:} \texttt{solar-wind-dm-mhd} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} MHD simulation of the solar wind at the termination shock region (80-100 AU) using Voyager 1/2 pre-shock data as IC. Radial IC with fine resolution around the shock region. Look for density jump (compression ratio 2-4x), deceleration, and magnetic field amplification consistent with Voyager 1 (2004) and Voyager 2 (2007) observations.

\textbf{Run:} \texttt{cargo run --release --bin solar-wind-dm-mhd -- --ic-file data/output/ts\_ic.csv --nx 256 --ny 64 --nz 64 --steps 20000 --tau 0.7 --eta 0.01 --dm-r-min 80 --dm-r-max 100}

\textbf{Claims:} C-1164

\subsection{E-121: Force-field phi validation: FGA vs 1D numerical PTE}\label{sec:e121}

\textbf{Binary:} \texttt{cr-modulation-sweep} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Run cr-modulation-sweep with nx=64 ny=1 nz=1 (1D radial), steps=5000 to reach quasi-steady state. Compare PteSolver flux at 1 AU to ForceFieldProxy FGA prediction. Also validate phi(r) is monotonically decreasing. Uses Bess-Pamela LIS power law as outer boundary condition.

\textbf{Run:} \texttt{cargo run --release --bin cr-modulation-sweep -- --nx 64 --ny 4 --nz 4 --steps 5000 --n-p 40 --r-min-gv 0.1 --r-max-gv 1000 --r-min-au 1.0 --r-max-au 157.0}

\textbf{Claims:} C-1171, C-1172

\subsection{E-122: Proton spectrum modulation: 1 AU vs 50 AU vs 100 AU}\label{sec:e122}

\textbf{Binary:} \texttt{cr-modulation-sweep} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Run cr-modulation-sweep with r\_min=1 AU, r\_max=157 AU. Extract differential flux J(R) from PTE snapshots at x-cells corresponding to 1, 50, 100 AU. Compare suppression ratios J(50)/J(1) and J(100)/J(1) vs FGA prediction. Validates spatial modulation gradient across heliosphere.

\textbf{Run:} \texttt{cargo run --release --bin cr-modulation-sweep -- --nx 128 --ny 8 --nz 8 --steps 10000 --n-p 40 --r-min-au 1.0 --r-max-au 157.0 --snapshot-interval 2000}

\textbf{Claims:} C-1171, C-1172

\subsection{E-123: Adiabatic deceleration rate vs solar wind divergence}\label{sec:e123}

\textbf{Binary:} \texttt{cr-modulation-sweep} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Initialize PteSolver with Gaussian f centered at 10 GV. Run with purely radial expanding solar wind (div(u) = const = 2*v\_sw/r). Measure peak rigidity bin shift per 100 steps. Compare to analytic prediction: delta(ln R) = (1/3)*div(u)*dt*N\_steps. Tests C-1173.

\textbf{Run:} \texttt{cargo run --release --bin cr-modulation-sweep -- --nx 64 --ny 4 --nz 4 --steps 1000 --n-p 40 --r-min-gv 1.0 --r-max-gv 100.0 --r-min-au 1.0 --r-max-au 157.0 --snapshot-interval 100}

\textbf{Claims:} C-1173

\subsection{E-124: Voyager CRS cross-calibration: V1 vs V2 spatial gradient}\label{sec:e124}

\textbf{Binary:} \texttt{cr-modulation-sweep} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Load Voyager CRS ASCII files for both spacecraft. Compare proton count rates at overlapping heliocentric distances (80-100 AU). Compute intensity gradient dJ/dr and compare to PteSolver modulation gradient. This validates the PTE prediction against in-situ cosmic ray data at the termination shock.

\textbf{Run:} \texttt{cargo run --release --bin cr-modulation-sweep -- --nx 128 --ny 4 --nz 4 --steps 5000 --n-p 40 --r-min-au 80.0 --r-max-au 157.0}

\textbf{Claims:} C-1174

\subsection{E-125: DM null sweep: m\_chi in \{10, 30, 100, 300\} GeV, bb-bar channel}\label{sec:e125}

\textbf{Binary:} \texttt{cr-modulation-sweep} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} For each DM mass, run cr-modulation-sweep with DM source injection enabled at bb-bar channel peak rigidity. Compare the injected flux excess at the source bin vs. the solar modulation amplitude. A null result (excess < 10\% of modulation amplitude) constrains the annihilation cross section for that mass. Four independent runs.

\textbf{Run:} \texttt{cargo run --release --bin cr-modulation-sweep -- --nx 64 --ny 4 --nz 4 --steps 5000 --n-p 40 --dm-mass-gev 100 --dm-channel bb --dm-sigma-v 3e-26}

\textbf{Claims:} C-1175

\subsection{E-126: DM injection at termination shock: Q\_DM profile sensitivity}\label{sec:e126}

\textbf{Binary:} \texttt{cr-modulation-sweep} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Inject DM source at the termination shock boundary (r \textasciitilde{} 90 AU, x \textasciitilde{} 0.57*nx for r\_max=157 AU). Compare uniform vs NFW\textasciicircum{}2-weighted spatial profiles. Measure how the injected flux propagates inward from 90 AU to 1 AU under solar modulation. Sensitivity to Q\_DM amplitude and spatial extent.

\textbf{Run:} \texttt{cargo run --release --bin cr-modulation-sweep -- --nx 128 --ny 4 --nz 4 --steps 10000 --n-p 40 --dm-mass-gev 100 --dm-channel bb --r-min-au 1.0 --r-max-au 157.0}

\textbf{Claims:} C-1175

\subsection{E-127: Full end-to-end: real IC + CRS data -> modulation potential chi\textasciicircum{}2}\label{sec:e127}

\textbf{Binary:} \texttt{cr-modulation-sweep} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Full pipeline: load solar wind IC from E-119 (multi-spacecraft radial profile), run LBM+MHD+PTE coupled simulation for 10000 steps, extract phi(r) at 1/50/100/157 AU, compare to Voyager CRS-derived modulation potential. Compute chi\textasciicircum{}2 goodness of fit. Tests the full Phase 17 one-way coupling pipeline end-to-end.

\textbf{Run:} \texttt{cargo run --release --bin cr-modulation-sweep -- --nx 256 --ny 8 --nz 8 --steps 10000 --n-p 40 --r-min-au 1.0 --r-max-au 157.0 --snapshot-interval 2000}

\textbf{Claims:} C-1171, C-1172, C-1173, C-1174, C-1175

\subsection{E-128: Bartol vs AMDA cross-validation for Voyager 2 B-field (1990-1995)}\label{sec:e128}

\textbf{Binary:} \texttt{} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Compare hourly BR/BT/BN from Bartol 16-col RTN format against AMDA-derived Voyager 2 B-field for the 1990-1995 overlap window. Normalize coordinates (both are RTN). Compute Pearson correlation, RMSE, and systematic offset per component.

\textbf{Run:} \texttt{python3 scripts/cross\_validate\_bartol\_amda.py --start 1990 --end 1995}

\textbf{Claims:} C-1177, C-1181

\subsection{E-129: Pioneer AMDA MAG-only radial B-field profile (5-80 AU)}\label{sec:e129}

\textbf{Binary:} \texttt{} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Fetch Pioneer 10 MAG-only data from AMDA for 1972-1995. Compute annual median |B| per year. Compare against Parker spiral 1/r\textasciicircum{}2 scaling using Pioneer trajectory positions. Verify consistency without plasma contamination.

\textbf{Run:} \texttt{python3 bin/fetch\_pioneer.py --spacecraft p10 --source amda --start 1972 --end 1995}

\textbf{Claims:} C-1178

\subsection{E-130: PSP AMDA inner heliosphere perihelion pass validation}\label{sec:e130}

\textbf{Binary:} \texttt{} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Fetch PSP AMDA data for 2022. Identify perihelion passes (r\_au < 0.15). Verify B-field magnitude follows r\textasciicircum{}-2 scaling between perihelion and 0.5 AU. Validate density scaling n \textasciitilde{} r\textasciicircum{}-2. Compare against published PSP encounter papers.

\textbf{Run:} \texttt{python3 bin/fetch\_psp.py --source amda --start 2022 --end 2022}

\textbf{Claims:} C-1179

\subsection{E-131: Helios corefit radial gradient (0.3-1.0 AU) vs Voyager at matched distances}\label{sec:e131}

\textbf{Binary:} \texttt{} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Fetch Helios 1 AMDA data for 1976-1980. Bin by distance (0.3-1.0 AU in 0.1 AU bins). Compute median B, n, V per bin. Compare against Voyager 2 data at matched distances (1977-1978, \textasciitilde{}1 AU). Verify B \textasciitilde{} 1/r\textasciicircum{}2 and n \textasciitilde{} 1/r\textasciicircum{}2 scaling consistency.

\textbf{Run:} \texttt{python3 bin/fetch\_helios.py --spacecraft helios1 --source amda --start 1976 --end 1980}

\textbf{Claims:} C-1180

\subsection{E-132: Ulysses AMDA vs SPDF cross-validation for polar wind data}\label{sec:e132}

\textbf{Binary:} \texttt{} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Fetch Ulysses AMDA data for 1994-1995 (first fast latitude scan). When SPDF becomes available, compare hourly B-field, density, speed between AMDA-derived and SPDF-original files. Verify agreement within measurement uncertainty.

\textbf{Run:} \texttt{python3 bin/fetch\_ulysses.py --source amda --start 1994 --end 1995}

\textbf{Claims:} C-1184

\subsection{E-133: Juno cruise AMDA B-field validation against Connerney et al. published values}\label{sec:e133}

\textbf{Binary:} \texttt{} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Fetch Juno cruise AMDA data for 2013-2015. Compare mean B-field magnitude at 2-4 AU against published Connerney et al. (2017) FGM calibration values. Verify Parker spiral r\textasciicircum{}-2 scaling between 2 AU and Jupiter approach.

\textbf{Run:} \texttt{python3 bin/fetch\_juno.py --source amda --start 2013 --end 2015}

\textbf{Claims:} C-1185

\subsection{E-134: Wind AMDA vs OMNI2 L1 cross-check for 2024}\label{sec:e134}

\textbf{Binary:} \texttt{} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Fetch Wind AMDA merged data for 2024. Compare hourly B-field and plasma against OMNI2 hourly dataset for the same period (OMNI2 uses Wind as primary L1 source). Agreement should be within resampling noise.

\textbf{Run:} \texttt{python3 bin/fetch\_wind.py --source amda --start 2024 --end 2024}

\textbf{Claims:} C-1186

\subsection{E-135: Pioneer/Flyby governed benchmark staging and verification}\label{sec:e135}

\textbf{Binary:} \texttt{} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Stage Anderson 2008 flyby anomaly events and Anderson 2002 Pioneer anomaly benchmark values as governed external CSV artifacts with explicit bibliography and source contracts. This experiment verifies artifact schema, value drift, and provenance registration only.

\textbf{Run:} \texttt{cargo run --release --bin registry-check}

\textbf{Claims:} C-1204

\subsection{E-136: Governed anomaly benchmark vs environment artifact generation}\label{sec:e136}

\textbf{Binary:} \texttt{pioneer-residual-audit} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Generate governed anomaly comparison artifacts from staged observational benchmarks. flyby-residual-audit validates the external Anderson flyby table against internal config values and emits flyby\_observed\_vs\_modeled.csv. pioneer-residual-audit samples governed Pioneer annual merged environment context at benchmark intervals and emits pioneer\_benchmark\_vs\_environment.csv with explicit provider-contract coverage flags.

\textbf{Run:} \texttt{cargo run --release -p gororoba\_cli\_physics --bin flyby-residual-audit \&\& cargo run --release -p gororoba\_cli\_physics --bin pioneer-residual-audit}

\textbf{Claims:} C-1205, C-1206

\subsection{E-137: Fractal/Q-tensor fit against governed anomaly benchmarks}\label{sec:e137}

\textbf{Binary:} \texttt{fractal-metric-fit} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Sweep plain and Q-tensor fractal-metric parameter grids against the governed flyby and Pioneer benchmark artifacts. Emit fit\_grid.csv and best\_fit.json with an explicit support-or-refute verdict rather than narrative interpretation.

\textbf{Run:} \texttt{cargo run --release -p gororoba\_cli\_physics --bin fractal-metric-fit}

\textbf{Claims:} C-1207

\subsection{E-138: Sprint 79 physics correctness verification gate}\label{sec:e138}

\textbf{Binary:} \texttt{cr-modulation-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Full workspace verification of 37 physics bug fixes across 7 batches: (1) data catalog value corrections (DESI DR2, DES Y6), (2) data\_core structural fixes (B-field ceiling 1/r, RTN rotation, ACE timestamps), (3) solar wind IC fixes (MAG-only, x-column mapping, B interpolation, Parker fallback), (4) MHD IC loading (x=0 inflow, bounds check), (5) CR transport physics (radial div\_u, LIS boundary, DmYieldSpectrum endpoints, LBM-to-AU/s, phi units, CFL check), (6) NFW/DM force path (z-clamp, rho\_crit(z), c(M,z) wiring, galactocentric centering, density scaling, force\_scale), (7) flyby/misc (gravity sign, wake accumulation, chi2 clamping). Verification: clippy clean, 5392 tests pass, governance gate green.

\textbf{Run:} \texttt{cargo clippy --workspace -- -D warnings \&\& cargo test --workspace \&\& make governance-gate}

\textbf{Claims:} C-1208, C-1209, C-1210, C-1211, C-1212, C-1213, C-1214, C-1215, C-1216, C-1217, C-1218, C-1219, C-1220, C-1221, C-1222, C-1223, C-1224, C-1225, C-1226, C-1227, C-1228, C-1229, C-1230

\subsection{E-139: High-dimensional Cayley-Dickson algebra analysis (dim 16..4096)}\label{sec:e139}

\textbf{Binary:} \texttt{dm-cd-4096-analysis} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} cd\_tower\_sweep: Random element sampling for associator norms, zero-divisor density, and norm multiplicativity deviation across CD dims 16..4096. AVT timing via TensorAVT auto-backend.

\textbf{Run:} \texttt{cargo run --release --bin dm-cd-4096-analysis -- --dims 16,32,64,128,256,512,1024,2048,4096 --samples 1000 --seed 42}

\textbf{Claims:} none

\subsection{E-140: Euclid Q1 Zenodo catalog discovery and ingestion}\label{sec:e140}

\textbf{Binary:} \texttt{} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Query Zenodo REST API for all Euclid Q1 supplementary science records. Discover, classify (catalog vs image, size thresholds), and ingest parquet/CSV catalogs for morphology, strong lensing, mergers, photo-z, clustering, compact groups, and globular clusters. MD5 verification against Zenodo checksums. Total footprint capped at 200 GB.

\textbf{Run:} \texttt{cargo run -p gororoba\_cli\_data --bin euclid-fetch -- zenodo-discover \&\& cargo run -p gororoba\_cli\_data --bin euclid-fetch -- zenodo-download --catalog all --skip-existing}

\textbf{Claims:} none

\subsection{E-141: YSU-engine GPU technique distillation into lbm\_3d\_cuda}\label{sec:e141}

\textbf{Binary:} \texttt{} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} yes

\textbf{Method:} Extract transferable GPU optimization patterns from YSU-engine (C11/Vulkan renderer, MIT license) and apply to lbm\_3d\_cuda D3Q19 kernels. Changes: (1) FMA chain optimization in compute\_equilibrium\_f/d using Horner evaluation (2 FMA ops vs 6 separate ops), (2) \_\_ldg() cached reads for read-only fields (tau, force, f\_in), (3) \#pragma unroll on all 19-direction loops, (4) occupancy culling via force-magnitude early-exit in Guo forcing kernels (skip cells with |F|\textasciicircum{}2 < 1e-40), (5) gororoba\_gpu\_bridge wiring with detect\_and\_select() runtime CUDA probe.

\textbf{Run:} \texttt{cargo clippy -p lbm\_3d\_cuda -- -D warnings \&\& cargo test -p lbm\_3d\_cuda}

\textbf{Claims:} none

\subsection{E-142: Magnonic 9-band dispersion reproduction (Kaman et al. 2026)}\label{sec:e142}

\textbf{Binary:} \texttt{magnonic-band-structure} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Compute 9-band magnonic crystal band structure along hexagonal BZ high-symmetry path Gamma-M-K-Gamma using Kaman default parameters (eps\_s=1.50, eps\_p=1.80, eps\_k=2.08, t\_sk=0.25, t\_pk=0.15 GHz). Verify Dirac cone at K near 1.5 GHz and kagome flat band near 2.08 GHz. CSV output with k-distance and 9 band energies.

\textbf{Run:} \texttt{cargo run --release --bin magnonic-band-structure -- bands --n-k 200 --output /tmp/magnonic\_bands.csv}

\textbf{Claims:} C-1231, C-1232, C-1241

\subsection{E-143: Magnonic Chern number sweep (all 9 bands, 51x51 grid)}\label{sec:e143}

\textbf{Binary:} \texttt{magnonic-band-structure} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Compute FHS Berry curvature and Chern numbers for all 9 magnonic bands on a 51x51 BZ grid. Verify C\_total=0 for each band (TRS). Report valley Chern numbers at K and K' valleys. Verify VCN(K) = -VCN(K') antisymmetry.

\textbf{Run:} \texttt{cargo run --release --bin magnonic-band-structure -- chern --n-grid 51}

\textbf{Claims:} C-1233, C-1234, C-1246, C-1250

\subsection{E-144: Magnonic valley Chern vs inversion breaking strength}\label{sec:e144}

\textbf{Binary:} \texttt{magnonic-band-structure} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Sweep inversion breaking parameter delta\_eps\_s from 0 to 1.0 GHz in 20 steps. At each step compute valley Chern numbers for the Dirac bands. Verify gap opens monotonically and VCN approaches +/-0.5 for small breaking.

\textbf{Run:} \texttt{cargo run --release --bin magnonic-band-structure -- sweep --param delta-eps --min 0.0 --max 1.0 --steps 20 --n-grid 31}

\textbf{Claims:} C-1235, C-1247, C-1250

\subsection{E-145: Magnonic flat band detection and DOS enhancement}\label{sec:e145}

\textbf{Binary:} \texttt{magnonic-band-structure} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Run flat band detection on 9-band model with bandwidth threshold 0.1 GHz. Identify flat band indices and compute DOS enhancement ratio (dispersive bandwidth / flat bandwidth). Verify kagome flat band near 2.08 GHz with enhancement > 100x.

\textbf{Run:} \texttt{cargo run --release --bin magnonic-band-structure -- flat-band --threshold 0.1 --n-k 200}

\textbf{Claims:} C-1232, C-1236

\subsection{E-146: Domain wall boundary modes and point defect localized modes}\label{sec:e146}

\textbf{Binary:} \texttt{magnonic-band-structure} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Two-part experiment: (1) Build 20-cell domain wall supercell with Kaman default inversion breaking, compute ribbon band structure along edge BZ, verify in-gap boundary modes. (2) Build point defect supercell (radius=3), find localized mode eigenvalues inside bulk gap, estimate Q factor.

\textbf{Run:} \texttt{cargo run --release --bin magnonic-band-structure -- domain-wall --n-cells 20 --n-k 100 \&\& cargo run --release --bin magnonic-band-structure -- defect --radius 3}

\textbf{Claims:} C-1237, C-1238, C-1239

\subsection{E-147: ZD crystal band spectra D=16,32,64}\label{sec:e147}

\textbf{Binary:} \texttt{zd-crystal-bands} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Compute ZD motif graph adjacency spectra at D=16,32,64 via block-diagonal component eigendecomposition. Verify integer spectrum, flat band fraction, and degeneracy pattern at each dimension.

\textbf{Run:} \texttt{cargo run --release --bin zd-crystal-bands -- --dims 16,32,64 --details}

\textbf{Claims:} C-1255, C-1256

\subsection{E-148: Flat band fraction scaling D=16..256}\label{sec:e148}

\textbf{Binary:} \texttt{zd-crystal-bands} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Compute flat band fraction across D=16,32,64,128,256 to test dimension-independence of the 0.5 localization strength. Fit scaling law (constant vs linear vs log).

\textbf{Run:} \texttt{cargo run --release --bin zd-crystal-bands -- --dims 16,32,64,128,256}

\textbf{Claims:} C-1257

\subsection{E-149: Reggiani factorization verification}\label{sec:e149}

\textbf{Binary:} \texttt{zd-crystal-bands} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Verify that the D=16 partner graph degeneracy pattern \{7,14,42,14,7\} factors as 7 * \{1,2,6,2,1\} by computing GCD of degeneracies and confirming reduced pattern.

\textbf{Run:} \texttt{cargo run --release --bin zd-crystal-bands -- --dims 16 --reggiani}

\textbf{Claims:} C-1258

\subsection{E-150: Euclid Sersic to LBM density, n=4 test galaxy}\label{sec:e150}

\textbf{Binary:} \texttt{euclid-dm-coupling} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Generate LBM initial condition from Sersic n=4 de Vaucouleurs profile with R\_e=5 kpc on 32\textasciicircum{}3 grid. Verify positive-definite deprojected density and azimuthal symmetry for circular (e=0) profile. Run 100 LBM steps to confirm stability.

\textbf{Run:} \texttt{cargo run --release --bin euclid-dm-coupling -- --grid 32 --steps 100 --sersic-n 4 --r-e-kpc 5}

\textbf{Claims:} C-1260, C-1261

\subsection{E-151: NFW + Sersic combined density, D\_f measurement}\label{sec:e151}

\textbf{Binary:} \texttt{euclid-dm-coupling} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Combined NFW dark matter halo (M200=1e12 Msun) and Sersic baryonic density on 64\textasciicircum{}3 grid. Run 500 LBM steps with DM-baryon coupling. Measure fractal dimension D\_f of resulting density field.

\textbf{Run:} \texttt{cargo run --release --bin euclid-dm-coupling -- --grid 64 --steps 500 --m200 1e12}

\textbf{Claims:} none

\subsection{E-152: GPU D\_f sweep: 100 Euclid Q1 galaxies, 64\textasciicircum{}3, 200 steps}\label{sec:e152}

\textbf{Binary:} \texttt{euclid-df-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} yes

\textbf{Method:} Read 100 galaxies from Euclid Q1 useful\_physical\_measurements.parquet, prepare Sersic+NFW density for each, initialize CUDA LBM solver at 64\textasciicircum{}3, evolve 200 steps, measure D\_f via box-counting on host-synced density. Output per-galaxy CSV with full provenance and bootstrap CI summary.

\textbf{Run:} \texttt{cargo run --release --bin euclid-df-sweep --features euclid-catalog -- --catalog data/external/euclid/zenodo/15106473/useful\_physical\_measurements.parquet --grid 64 --steps 200 --max-galaxies 100 --cuda}

\textbf{Claims:} C-1262, C-1264, C-1265, C-1266, C-1268

\subsection{E-153: GPU D\_f sweep: 1000 Euclid Q1 galaxies (extended run)}\label{sec:e153}

\textbf{Binary:} \texttt{euclid-df-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} yes

\textbf{Method:} Extended version of E-152 with 1000 galaxies for tighter bootstrap CI and morphological type statistics.

\textbf{Run:} \texttt{cargo run --release --bin euclid-df-sweep --features euclid-catalog -- --catalog data/external/euclid/zenodo/15106473/useful\_physical\_measurements.parquet --grid 64 --steps 200 --max-galaxies 1000 --cuda}

\textbf{Claims:} C-1262, C-1266

\subsection{E-154: GPU vs CPU cross-validation: 10 galaxies}\label{sec:e154}

\textbf{Binary:} \texttt{euclid-df-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} yes

\textbf{Method:} Run 10 galaxies through both CPU (LbmSolver3D) and GPU (LbmSolver3DCuda FP32) paths. Compare per-galaxy D\_f values. Assert |D\_f\_gpu - D\_f\_cpu| < 0.05 for all galaxies.

\textbf{Run:} \texttt{cargo run --release --bin euclid-df-sweep --features euclid-catalog -- --catalog data/external/euclid/zenodo/15106473/useful\_physical\_measurements.parquet --grid 64 --steps 200 --max-galaxies 10 --cpu -o /tmp/cpu.csv \&\& cargo run --release --bin euclid-df-sweep --features euclid-catalog -- --catalog data/external/euclid/zenodo/15106473/useful\_physical\_measurements.parquet --grid 64 --steps 200 --max-galaxies 10 --cuda -o /tmp/gpu.csv}

\textbf{Claims:} C-1263, C-1267

\subsection{E-155: M/L systematic: morphological vs fixed M/L=3.0}\label{sec:e155}

\textbf{Binary:} \texttt{euclid-df-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} yes

\textbf{Method:} Run 100 galaxies with catalog M/L (--morphological-ml false) and compare against morphological M/L baseline (E-152). Quantify systematic D\_f shift.

\textbf{Run:} \texttt{cargo run --release --bin euclid-df-sweep --features euclid-catalog -- --catalog data/external/euclid/zenodo/15106473/useful\_physical\_measurements.parquet --grid 64 --steps 200 --max-galaxies 100 --morphological-ml false --cuda}

\textbf{Claims:} C-1264

\subsection{E-156: Grid resolution study: 32 vs 64 vs 128 at N=20 galaxies}\label{sec:e156}

\textbf{Binary:} \texttt{euclid-df-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Run 20 galaxies at three grid resolutions (32\textasciicircum{}3, 64\textasciicircum{}3, 128\textasciicircum{}3) to quantify D\_f convergence with grid refinement.

\textbf{Run:} \texttt{for G in 32 64 128; do cargo run --release --bin euclid-df-sweep --features euclid-catalog -- --catalog data/external/euclid/zenodo/15106473/useful\_physical\_measurements.parquet --grid \$G --steps 200 --max-galaxies 20 --cpu; done}

\textbf{Claims:} C-1262

\subsection{E-157: Otsu vs median threshold: 10 galaxies, compare D\_f distributions}\label{sec:e157}

\textbf{Binary:} \texttt{euclid-df-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Run 10 galaxies with Otsu threshold (current) and compare df\_initial distribution variance vs Sprint 82 median baseline.

\textbf{Run:} \texttt{cargo run --release --bin euclid-df-sweep --features euclid-catalog -- --catalog data/external/euclid/zenodo/15106473/useful\_physical\_measurements.parquet --grid 64 --steps 200 --max-galaxies 10 --cpu}

\textbf{Claims:} C-1269

\subsection{E-158: Smagorinsky vs static tau: 10 concentrated galaxies, D\_f trajectories}\label{sec:e158}

\textbf{Binary:} \texttt{euclid-df-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Compare D\_f before/after LBM with Smagorinsky C\_s=0.1 vs static tau=1.5 for 10 galaxies. Expect Smagorinsky preserves concentrated galaxy structure.

\textbf{Run:} \texttt{cargo run --release --bin euclid-df-sweep --features euclid-catalog -- --catalog data/external/euclid/zenodo/15106473/useful\_physical\_measurements.parquet --grid 64 --steps 200 --max-galaxies 10 --cpu --smagorinsky-cs 0.1}

\textbf{Claims:} C-1272

\subsection{E-159: SoA vs AoS GPU performance: 100 galaxies at 64\textasciicircum{}3}\label{sec:e159}

\textbf{Binary:} \texttt{euclid-df-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} yes

\textbf{Method:} Benchmark 100 galaxies at 64\textasciicircum{}3 with SoA kernel (S83) vs AoS (S82 baseline). Measure wall-clock time and per-galaxy latency.

\textbf{Run:} \texttt{cargo run --release --bin euclid-df-sweep --features euclid-catalog,gpu -- --catalog data/external/euclid/zenodo/15106473/useful\_physical\_measurements.parquet --grid 64 --steps 200 --max-galaxies 100 --cuda}

\textbf{Claims:} C-1271

\subsection{E-160: 128\textasciicircum{}3 sweep: 100 galaxies with Otsu + Smagorinsky}\label{sec:e160}

\textbf{Binary:} \texttt{euclid-df-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} yes

\textbf{Method:} Scale to 128\textasciicircum{}3 grid with Smagorinsky LES and Otsu threshold. Verify D\_f convergence with grid refinement.

\textbf{Run:} \texttt{cargo run --release --bin euclid-df-sweep --features euclid-catalog,gpu -- --catalog data/external/euclid/zenodo/15106473/useful\_physical\_measurements.parquet --grid 128 --steps 200 --max-galaxies 100 --cuda --smagorinsky-cs 0.1}

\textbf{Claims:} C-1271, C-1272

\subsection{E-161: ZD coupling sweep: alpha\_zd = \{0.01, 0.05, 0.1, 0.5\}}\label{sec:e161}

\textbf{Binary:} \texttt{euclid-df-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Sweep ZD algebraic forcing strength on 10 galaxies. Compare D\_f with and without ZD coupling to quantify topological confinement effect.

\textbf{Run:} \texttt{for A in 0.01 0.1 0.5; do cargo run --release --bin euclid-df-sweep --features euclid-catalog -- --catalog data/external/euclid/zenodo/15106473/useful\_physical\_measurements.parquet --grid 64 --steps 200 --max-galaxies 10 --cpu --smagorinsky-cs 0.1 --alpha-zd \$A; done}

\textbf{Claims:} C-1273

\subsection{E-162: MRT + Plummer softening: density floor calibration sweep}\label{sec:e162}

\textbf{Binary:} \texttt{euclid-df-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Systematic sweep of density\_floor (0.01-0.2) with MRT collision, Plummer softening eps=0.5*dx, tau=0.7 on 10 Euclid galaxies. Identifies stability boundary and optimal production parameters for 64\textasciicircum{}3.

\textbf{Run:} \texttt{for FLOOR in 0.03 0.04 0.042 0.045 0.05 0.1; do cargo run --release --bin euclid-df-sweep --features euclid-catalog -- --catalog ... --grid 64 --steps 200 --max-galaxies 10 --cpu --mrt --tau 0.7 --softening-eps 0.5 --density-floor \$FLOOR; done}

\textbf{Claims:} C-1274, C-1275, C-1276

\subsection{E-163: GPU MRT 100-galaxy D\_f sweep at 64\textasciicircum{}3 with zero-copy box-counting}\label{sec:e163}

\textbf{Binary:} \texttt{euclid-df-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} yes

\textbf{Method:} 100-galaxy sweep with GPU MRT collision (d'Humieres D3Q19 SoA), Plummer softening eps=0.5*dx, density floor 0.045, tau=0.7. D\_f measured entirely on GPU via Otsu threshold + box-counting (no density readback).

\textbf{Run:} \texttt{cargo run --release --bin euclid-df-sweep --features euclid-catalog,gpu -- --catalog data/external/euclid/zenodo/15106473/useful\_physical\_measurements.parquet --grid 64 --steps 200 --max-galaxies 100 --mrt}

\textbf{Claims:} C-1282, C-1283

\subsection{E-164: GPU MRT 10-galaxy D\_f sweep at 128\textasciicircum{}3: resolution convergence}\label{sec:e164}

\textbf{Binary:} \texttt{euclid-df-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} yes

\textbf{Method:} 10-galaxy sweep at 128\textasciicircum{}3 with GPU MRT collision, zero-copy GPU box-counting. Tests resolution convergence toward CD D=16 topological prediction D\_f=2.7268.

\textbf{Run:} \texttt{cargo run --release --bin euclid-df-sweep --features euclid-catalog,gpu -- --catalog data/external/euclid/zenodo/15106473/useful\_physical\_measurements.parquet --grid 128 --steps 200 --max-galaxies 10 --mrt}

\textbf{Claims:} C-1284, C-1286, C-1287

\subsection{E-165: GPU zero-copy box-counting validation: PCIe elimination}\label{sec:e165}

\textbf{Binary:} \texttt{euclid-df-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} yes

\textbf{Method:} Validate GPU-resident Otsu threshold + GPU box-counting against readback path. GPU min/max reduction (shared memory tree) -> GPU 256-bin histogram -> 1 KB readback -> CPU Otsu -> GPU box-counting. Measures PCIe savings and D\_f agreement.

\textbf{Run:} \texttt{cargo run --release --bin euclid-df-sweep --features euclid-catalog,gpu -- --catalog data/external/euclid/zenodo/15106473/useful\_physical\_measurements.parquet --grid 128 --steps 200 --max-galaxies 10 --mrt}

\textbf{Claims:} C-1285, C-1286

\subsection{E-166: GPU MRT 100-galaxy D\_f sweep at 128\textasciicircum{}3: definitive CD topology validation}\label{sec:e166}

\textbf{Binary:} \texttt{euclid-df-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} yes

\textbf{Method:} 100-galaxy sweep at 128\textasciicircum{}3 with GPU MRT collision (d'Humieres D3Q19 SoA), Plummer softening eps=0.5*dx, density floor 0.045, tau=0.7. Uses readback-path box-counting (pre-zero-copy build). Definitive test of CD D=16 flat band topology prediction D\_f=2.7268.

\textbf{Run:} \texttt{cargo run --release --bin euclid-df-sweep --features euclid-catalog,gpu -- --catalog data/external/euclid/zenodo/15106473/useful\_physical\_measurements.parquet --grid 128 --steps 200 --max-galaxies 100 --mrt -o /tmp/euclid\_df\_gpu\_mrt\_128\_100.csv}

\textbf{Claims:} C-1287, C-1288, C-1289

\subsection{E-167: MRT vs BGK stability envelope: density contrast sweep at tau=0.7}\label{sec:e167}

\textbf{Binary:} \texttt{mrt-stability-audit} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Systematic BGK vs MRT stability comparison at increasing density contrast (2x-100x) using Gaussian density perturbation on n\textasciicircum{}3 grid. Reports max Mach number, mass conservation error, and crash threshold per collision mode. Uses LbmBackend dispatcher for unified CPU/GPU access.

\textbf{Run:} \texttt{cargo run --release --bin mrt-stability-audit -- --n 16 --tau 0.7 --steps 200 --contrast-min 2.0 --contrast-max 100.0 --n-levels 20 --csv}

\textbf{Claims:} C-1290

\subsection{E-168: D=64 flat band fraction verification via block-diagonal eigensolver}\label{sec:e168}

\textbf{Binary:} \texttt{zd-crystal-bands} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Block-diagonal eigendecomposition of D=64 ZD partner graph (16384 vertices, 31 motif components). Verifies that exactly 50\% of eigenvalues are zero, extending the flat\_band\_fraction = 1/2 invariance from D=16 and D=32.

\textbf{Run:} \texttt{cargo test --release -p algebra\_analysis -- --ignored flat\_band\_fraction\_d64}

\textbf{Claims:} C-1291

\subsection{E-169: Thread coarsening benchmark: 1T=2C vs 1T=1C at 128\textasciicircum{}3}\label{sec:e169}

\textbf{Binary:} \texttt{euclid-df-sweep} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} yes

\textbf{Method:} Compare GPU MRT performance with thread coarsening (1 thread = 2 cells, \_\_launch\_bounds\_\_(128, 9)) vs baseline (1 thread = 1 cell). Measure ms/galaxy at 128\textasciicircum{}3 with 100 galaxies. Both paths use identical collision physics.

\textbf{Run:} \texttt{cargo run --release --bin euclid-df-sweep --features euclid-catalog,gpu -- --catalog data/external/euclid/zenodo/15106473/useful\_physical\_measurements.parquet --grid 128 --steps 200 --max-galaxies 100 --mrt --coarsened}

\textbf{Claims:} C-1292, C-1296

\subsection{E-172: Roofline benchmark: per-kernel nsight-compute at 128\textasciicircum{}3}\label{sec:e172}

\textbf{Binary:} \texttt{euclid-df-sweep} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} yes

\textbf{Method:} Profile MRT collision, streaming, Smagorinsky, and box-counting kernels at 128\textasciicircum{}3 using nsight-compute. Measure achieved bandwidth, SM utilization, register count, and occupancy per kernel. Validate GPU-side max-Ma reduction kernel timing (< 10us target).

\textbf{Run:} \texttt{ncu --set full cargo run --release --bin euclid-df-sweep --features euclid-catalog,gpu -- --catalog data/external/euclid/zenodo/15106473/useful\_physical\_measurements.parquet --grid 128 --steps 200 --max-galaxies 1 --mrt}

\textbf{Claims:} C-1295, C-1296

\subsection{E-174: Null hypothesis control: 6 conditions x N trials to exclude D\_f=2.73 pipeline artifact}\label{sec:e174}

\textbf{Binary:} \texttt{euclid-df-sweep} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} yes

\textbf{Method:} Six control conditions test whether D\_f=2.73 is a pipeline artifact: C0 (uniform+zero force), C1 (uniform+ZD), C2 (white noise+zero), C3 (white noise+ZD), C4 (Sersic+zero), C5 (Sersic+full force, positive control). All use identical MRT 128\textasciicircum{}3 pipeline. Null hypothesis rejected iff C0-C4 produce D\_f statistically distinguishable from 2.73 and C5 reproduces E-166.

\textbf{Run:} \texttt{cargo run --release --bin euclid-df-sweep --features euclid-catalog,gpu -- --null-hypothesis --null-n-trials 10 --grid 128 --steps 200 --mrt --catalog data/external/euclid/zenodo/15106473/useful\_physical\_measurements.parquet}

\textbf{Claims:} C-1297

\subsection{E-175: CPU AoSoA + AVX2 benchmark: AoS vs AoSoA at 64\textasciicircum{}3 and 128\textasciicircum{}3 MRT f64 on Zen 3}\label{sec:e175}

\textbf{Binary:} \texttt{mrt-stability-audit} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Benchmark CPU LBM solver with AoSoA layout + wide::f64x4 SIMD collision vs previous AoS scalar. Test at 64\textasciicircum{}3 (V-Cache resident, compute-bound) and 128\textasciicircum{}3 (DDR4-limited). Use RUSTFLAGS=-C target-cpu=znver3 for optimal Zen 3 code generation.

\textbf{Run:} \texttt{RUSTFLAGS="-C target-cpu=znver3" cargo run --release --no-default-features --bin mrt-stability-audit -- --n 64 --tau 0.7 --steps 200 --n-levels 1}

\textbf{Claims:} C-1298

\subsection{E-176: CPU/GPU cross-validation: AoSoA/SoA layout parity D\_f agreement}\label{sec:e176}

\textbf{Binary:} \texttt{mrt-stability-audit} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Run identical physical problem (Gaussian density contrast, MRT collision, tau=0.7) on both CPU (AoSoA f64) and GPU (SoA f32) backends via LbmBackend dispatcher. Compare D\_f, max Mach, and mass conservation to validate structural parity between the two memory layouts.

\textbf{Run:} \texttt{cargo run --release --bin mrt-stability-audit -- --n 64 --tau 0.7 --steps 100 --n-levels 5 --csv}

\textbf{Claims:} C-1299

\subsection{E-177: YSU-Inspired CUDA Optimization Suite: MRT ILP + CUDA Graph + Tiled Pull-Scheme + Multi-Stream Pipeline}\label{sec:e177}

\textbf{Binary:} \texttt{euclid-df-sweep} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} no \quad \textbf{GPU:} yes

\textbf{Method:} 4-item GPU optimization suite targeting compound 15-50\% speedup at 128\textasciicircum{}3 on Ada Lovelace SM 8.9. Item 1: MRT forward/inverse transform ILP restructure (balanced fmaf trees). Item 2: Multi-stream galaxy pipeline (2-stream event-based overlap). Item 3: CUDA Graph 2-step capture fixing double-buffer bug + step\_n() bulk stepping. Item 4: Shared-memory tiled pull-scheme kernels (8x8x4 tile, 45.6 KB shared, cooperative striped load). Verification: clippy clean, Taylor-Green 6/6 pass, GPU debug init 3/3 pass.

\textbf{Run:} \texttt{cargo run --release --bin euclid-df-sweep -- --n-galaxies 100 --resolution 128 --gpu --tiling}

\textbf{Claims:} C-1300, C-1301, C-1302, C-1303, C-1304, C-1305, C-1306, C-1307

\subsection{E-178: Harmonic Halo Rotation Curve: 7-mode box-kite modulation sweep}\label{sec:e178}

\textbf{Binary:} \texttt{harmonic-halo-rotation-curve} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Compute NFW rotation curves with harmonic halo modulation at alpha\_zd = \{0.0, 0.01, 0.05, 0.1\} for MW-mass (1e12 Msun) and UDG-mass (1e9 Msun) halos. Compare v\_circ profiles, measure max |delta\_v|, verify alpha\_zd=0 recovery of standard NFW. 7-mode box-kite wavenumbers from strut signatures.

\textbf{Run:} \texttt{cargo run --release --bin harmonic-halo-rotation-curve -- --m200 1e12 --alpha-zd 0.05 --csv harmonic\_halo\_mw.csv}

\textbf{Claims:} C-1313, C-1314

\subsection{E-179: Falsification of slope\_ratio = 42\textasciicircum{}2/10000 claim (C-1329)}\label{sec:e179}

\textbf{Binary:} \texttt{} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Three unit tests in lbm\_core: (1) vary associator norm over \{0.1..10.0\} with fixed grid, confirm slope\_ratio is identical to 14 digits (norm cancellation); (2) vary strain-rate grid, confirm 0.1764 value is destroyed (grid dependence); (3) vary alpha and beta, confirm slope\_ratio unchanged (parameter invariance). Analytical proof: norm\textasciicircum{}beta is multiplicative in nu, cancels in ratio.

\textbf{Run:} \texttt{cargo test -p lbm\_core test\_c1329}

\textbf{Claims:} C-1329

\subsection{E-180: Harmonic Halo Stacking: SPARC + Gaia MW rotation curve falsification test}\label{sec:e180}

\textbf{Binary:} \texttt{harmonic-halo-stacking} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Download SPARC (175 galaxies) and Gaia DR3 MW rotation curve. For each galaxy: (1) fit NFW to get r\_s, (2) subtract smooth NFW to get residuals delta\_v(r), (3) rescale to r/r\_s coordinates. Stack all residuals. Compute Fourier power at 7 predicted wavenumbers k\_n' = 2*pi*n/7. Compare to null hypothesis (1000 random phase shuffles). Separately analyze MW curve via Gaia DR3 (Jiao et al. 2023). Report alpha\_zd upper limit or detection significance.

\textbf{Run:} \texttt{cargo run --release --bin harmonic-halo-stacking -- --sparc-dir data/external/sparc --nfw-fits data/external/sparc\_nfw/li2020\_nfw\_fits.csv --csv-out harmonic\_stacking\_results.csv}

\textbf{Claims:} C-1313, C-1332

\subsection{E-181: x87 FP-80 vs DD vs nalgebra eigenvalue solver benchmark}\label{sec:e181}

\textbf{Binary:} \texttt{} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} Criterion benchmarks on 8x8 and 16x16 known-spectrum symmetric matrices (Householder-rotated diagonal with eigenvalues 1..n). Three solvers: (1) nalgebra symmetric\_eigenvalues (LAPACK-style Jacobi, pure f64), (2) x87 FP-80 inline asm Jacobi (hardware 64-bit mantissa via fpatan+fsincos chain), (3) DD software Jacobi (\textasciitilde{}31 decimal digits via Knuth TwoSum + Dekker TwoProduct). Precision audit: all solvers recover known spectrum to machine epsilon. Additionally evaluated `extended` crate (MIT, crates.io) and rejected: same 64-bit mantissa, conversion overhead, no atan2/sincos, LLVM spill hazard.

\textbf{Run:} \texttt{cargo bench -p algebra\_analysis -- x87}

\textbf{Claims:} C-1333, C-1337

\subsection{E-182: Harmonic Halo stacking: multi-CD-dimension sweep (D=16,32,64,128,256)}\label{sec:e182}

\textbf{Binary:} \texttt{harmonic-halo-stacking} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Extend E-180 stacking to higher Cayley-Dickson dimensions. For each D in \{16,32,64,128,256\}: (1) compute ZD count, assessor count, and box-kite equivalent via algebra\_analysis, (2) derive mode count and assessor fraction, (3) run stacking with dimension-specific wavenumbers k\_n = 2*pi*n/(N\_modes * r\_s), (4) compare detection SNR across dimensions. Tests whether the harmonic signature is better described by pathion (D=32), vouton (D=64), or higher structures.

\textbf{Run:} \texttt{cargo run --release --bin harmonic-halo-stacking -- --cd-dim 32 --sparc data/external/sparc/table2\_rotcurves.dat --nfw-fits data/external/sparc\_nfw/table1.dat}

\textbf{Claims:} C-1338

\subsection{E-183: MaNGA N=6992 harmonic halo stacking null result with pseudo-slit rotation curves (D=16,64,256,1024)}\label{sec:e183}

\textbf{Binary:} \texttt{harmonic-halo-stacking-manga} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Two-stage pipeline: (1) DAPall Guillotine -- morphological + quality cuts on MaNGA DR17 DAPall catalog (Sersic n<2.5, 30<i<70 deg, Ha EW>2A, log M*>8.5) to select \textasciitilde{}3000 disk galaxies. (2) MAPS pseudo-slit extraction -- download per-galaxy MAPS FITS files via HTTP from data.sdss.org (6 parallel threads), extract 1D rotation curves from 2D H-alpha EMLINE\_GVEL maps along kinematic PA, deproject by sin(i). Normalize by SMHM-derived NFW (Moster+2013), stack, Fourier-analyze at CD dimensions D=16..1024. NOTE: future reruns should use rsync://data.sdss.org instead of HTTP to avoid Apache QoS token-bucket exhaustion (cwnd=10 per thread at N>2000). The per-galaxy delta\_v(x) = (v\_obs - v\_NFW) / v\_NFW projection at x = r/r\_s is novel: MaNGA DAP provides only 2D EMLINE\_GVEL maps; no pre-existing 1D NFW-normalized rotation curve catalog exists for this dataset.

\textbf{Run:} \texttt{harmonic-halo-stacking-manga --rotcurves data/external/manga/rotcurves/manga\_rotcurves\_all.csv --dapall data/external/manga/dapall\_selection.csv}

\textbf{Claims:} C-1346, C-1347, C-1348, C-1365, C-1366, C-1367, C-1368

\subsection{E-184: Braid fidelity audit: confirm fidelity < 1.0 at all dim >= 16 (C-1350, C-1351)}\label{sec:e184}

\textbf{Binary:} \texttt{majorana-braid-crucible} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Run braid paths at dim=16 and dim=32 via the cd\_braid() function. Confirm topological\_friction > 0 for all paths (C-1134 bound >= 4). Confirm BraidResult.fidelity = 0.0 for all dim >= 16 (threshold < 1e-12 never met). Confirm friction increases monotonically from dim=16 to dim=32 (friction\_dimensional\_scaling). New test: test\_c1350\_braid\_fidelity\_always\_below\_unity in majorana\_braiding.rs.

\textbf{Run:} \texttt{cargo test -p algebra\_experimental --lib -- majorana}

\textbf{Claims:} C-1350, C-1351, C-1352

\subsection{E-185: Harmonic halo falsification replication: alpha\_zd=0 NFW recovery + DM cross-section independence (C-1353, C-1354)}\label{sec:e185}

\textbf{Binary:} \texttt{harmonic-halo-rotation-curve} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Run cargo test -p cosmology\_core --lib -- harmonic (25 unit tests). Verify alpha\_zd=0 recovers NFW exactly (C-1314 replication). Verify modulation < 1\% at alpha\_zd=0.01 (C-1313 bound). Verify detection\_threshold(175) > alpha\_zd\_limit from C-1338. New test: test\_c1353\_dm\_cross\_section\_not\_algebraic in dm\_force.rs asserting sigma\_chi\_b default=0.0 and no algebraic derivation from CD.

\textbf{Run:} \texttt{cargo test -p cosmology\_core --lib -- harmonic \&\& cargo test -p lbm\_3d --lib -- dm\_force}

\textbf{Claims:} C-1353, C-1354

\subsection{E-186: Obstruction norm normalization sensitivity: 5 convention variants at dim=16 (C-1357)}\label{sec:e186}

\textbf{Binary:} \texttt{gravastar-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Compute obstruction spectrum for dim=16 sedenion A-infinity algebra. Test 5 normalization conventions: (1) frobenius/dim\textasciicircum{}1.5 (current, 8.725), (2) frobenius/dim (\textasciitilde{}34.9), (3) frobenius/sqrt(dim) (\textasciitilde{}139.6), (4) frobenius/dim\textasciicircum{}2 (\textasciitilde{}2.18), (5) spectral\_radius/dim\textasciicircum{}1.5 (\textasciitilde{}7.76). If values differ >10\% across conventions, normalization dependence is confirmed. New test: test\_c1357\_obstruction\_norm\_normalization\_sensitivity in homotopy\_algebra.rs.

\textbf{Run:} \texttt{cargo test -p algebra\_analysis --lib -- test\_c1357}

\textbf{Claims:} C-1357

\subsection{E-187: Gravastar-to-dark-energy chain termination audit (C-1357)}\label{sec:e187}

\textbf{Binary:} \texttt{gravastar-sweep} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Code audit: verify solve\_gravastar\_homotopy() output is GravastarSolution \{mass, r2, compactness, is\_causal\} with no cosmological fields. Verify orthoplex\_diffusion.rs w\_orthoplex() has no reference to obstruction\_norm or homotopy\_bridge. Verify DarkEnergyEOS.v imports no gravastar module. New test: test\_c1357\_gravastar\_chain\_terminates\_at\_tov in homotopy\_bridge.rs.

\textbf{Run:} \texttt{cargo test -p cosmology\_core --lib -- homotopy\_bridge}

\textbf{Claims:} C-1357

\subsection{E-188: x87 FP-80 vs Kahan vs naive f64 accumulation benchmark suite (Criterion 100-sample, n=4..512)}\label{sec:e188}

\textbf{Binary:} \texttt{x87\_bench} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} cargo bench -p algebra\_analysis -- x87 (Criterion, 100 samples, 3s warmup). Five benchmark families: (1) sum\_throughput n=4..512 three variants, (2) dot\_throughput n=4..512, (3) norm\_sq\_throughput n=4..512 + x87\_norm\_sq\_16\_fixed, (4) horner\_throughput n=4..32, (5) precision near-cancellation and ill-conditioned dot. Six Jacobi benchmarks: nalgebra/x87/DD at 8x8 and 16x16. All on x86\_64 nightly-2026-03-05, --release, algebra\_analysis crate.

\textbf{Run:} \texttt{cargo bench -p algebra\_analysis -- x87}

\textbf{Claims:} C-1349, C-1359, C-1360, C-1361, C-1362

\subsection{E-189: AVX/FMA follow-on slice for x87 accumulation benchmarks at n=512}\label{sec:e189}

\textbf{Binary:} \texttt{x87\_bench} \quad \textbf{Status:} active \quad \textbf{Deterministic:} no \quad \textbf{GPU:} no

\textbf{Method:} Criterion follow-on run after the AVX packed-double + FMA path landed in `cd\_kernel`. Filter the existing benchmark suite to the `n=512` slice so the naive, Kahan, x87, and `avx2\_fma` variants can be compared on the same array length without rerunning the entire matrix. Command used: `cargo bench -p algebra\_analysis --bench x87\_bench -- 512`. The summary CSV records the midpoint of Criterion's reported confidence interval for each median timing.

\textbf{Run:} \texttt{cargo bench -p algebra\_analysis --bench x87\_bench -- 512}

\textbf{Claims:} C-1360, C-1361, C-1362

\subsection{E-190: Evidence-first thesis 42 support bundle}\label{sec:e190}

\textbf{Binary:} \texttt{thesis-42-support} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Build one repo-consistent support bundle from three existing lanes: (1) the C-010 non-local metamaterial design-stage benchmark, (2) the algebraic Majorana complex-time friction sweep with inverse-bound residual checks, and (3) harmonic-halo/NFW observable summaries with exact alpha\_zd=0 recovery. Emit one TOML summary plus CSV sidecars.

\textbf{Run:} \texttt{cargo run --release --bin thesis-42-support -- --output-dir data/evidence/thesis\_42\_support}

\textbf{Claims:} none

\subsection{E-191: CD dimension sweep: harmonic halo stacking at D=16,32,64,128,256,512,1024}\label{sec:e191}

\textbf{Binary:} \texttt{harmonic-halo-stacking} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Run harmonic-halo-stacking on 93 SPARC galaxies (chi2<3, c200>=2) with --cd-dim=\{16,32,64,128,256,512,1024\}. At each CD dimension D, the number of Fourier modes is D/2-1 and the assessor fraction is exactly 0.5 (combinatorial identity). Detection SNR and alpha\_zd estimates compared across all 7 dimensions.

\textbf{Run:} \texttt{harmonic-halo-stacking --sparc data/external/sparc/table2\_rotcurves.dat --nfw-fits data/external/sparc\_nfw/table1.dat --cd-dim \{16..1024\} --min-c200 2.0 --max-chi2 3.0 --x-min 0.5 --x-max 10.0}

\textbf{Claims:} C-1344, C-1345

\subsection{E-192: Non-static MaNGA signal analysis: STFT spectrogram + derivative + Rayleigh phase coherence}\label{sec:e192}

\textbf{Binary:} \texttt{harmonic-halo-signal-analysis} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Apply non-static signal analysis (STFT spectrogram, derivative stacking, jackknife Rayleigh phase coherence) to MaNGA E-183 stacked profiles. STFT uses Gaussian window sigma\_x=1.5 r/r\_s at 32 window centers. Derivative stacking kills the DC baryonic baseline. Rayleigh R test on jackknife drop-one-bin phases at CD-ZD wavenumbers. Expected: STFT power concentrated x<2, derivative DFT red-noise, Rayleigh R<0.3 p>0.3 at all modes. Synthetic pipeline validation: 11 conditions x 3 regimes x 20 seeds = 660 runs on N=6992 galaxies confirm zero false positives and injection anti-correlation.

\textbf{Run:} \texttt{harmonic-halo-signal-analysis --stacked-csv data/results/e183/lie\_jordan\_full.csv --min-per-bin 10 --sigma-x 1.5 --n-windows 32 --stft-csv data/results/e183/stft\_full.csv --deriv-csv data/results/e183/deriv\_full.csv --rayleigh-csv data/results/e183/rayleigh\_full.csv}

\textbf{Claims:} C-1372, C-1373, C-1411, C-1412, C-1413, C-1414, C-1415, C-1416, C-1417, C-1418

\subsection{E-193: THINGS HI 21cm Rotation Curve Extraction}\label{sec:e193}

\textbf{Binary:} \texttt{hi-cube-rotcurve-extractor} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Extract rotation curves from THINGS FITS HI cubes to x > 10 r\_s with hi-cube-rotcurve-extractor. Target the 34 THINGS galaxies with usable inclination control so the lane can test whether HI kinematics extend the outer-halo reach beyond the MaNGA IFU truncation limit near x\textasciitilde{}1.36 r\_s.

\textbf{Run:} \texttt{hi-cube-rotcurve-extractor --input-glob "data/external/things/*.fits" --output data/external/things/hi\_rotcurves.csv}

\textbf{Claims:} none

\subsection{E-194: Galaxy-Ensemble Rayleigh Phase Coherence}\label{sec:e194}

\textbf{Binary:} \texttt{harmonic-halo-galaxy-phase} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Run the galaxy-ensemble Rayleigh phase-coherence test on all 6992 MaNGA galaxies at the seven CD-ZD wavenumbers. The goal is to confirm that ensemble phases stay incoherent (target R < 0.024 and p > 0.5 at every mode) and to remove the N=19 jackknife limitation from E-192.

\textbf{Run:} \texttt{harmonic-halo-galaxy-phase --rotcurves data/external/manga/manga\_rotcurves\_all.csv --dapall data/external/manga/dapall\_selection.csv --output data/external/manga/galaxy\_phase\_coherence.csv}

\textbf{Claims:} none

\subsection{E-195: Multi-Panel Null Result Visualization}\label{sec:e195}

\textbf{Binary:} \texttt{harmonic-halo-visualize} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Generate the publication-ready PGFPlots panels for the E-183/E-192 null-result lane: the stacked spatial residual profile with systematics, the derivative DFT, and the STFT spectrogram. Emit three .tex fragments that can be dropped directly into the MaNGA manuscript build.

\textbf{Run:} \texttt{harmonic-halo-visualize --stack data/results/e183/manga\_stack\_D16.csv --stft data/results/e183/stft\_full.csv --deriv data/results/e183/deriv\_full.csv --out-dir docs/latex/figs}

\textbf{Claims:} none

\subsection{E-196: MaNGA Stacking with PSF Beam-Smearing Correction}\label{sec:e196}

\textbf{Binary:} \texttt{harmonic-halo-stacking-manga} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Re-run the MaNGA stacking lane from E-183 with PSF-flagged inner bins excluded (r < PSF\_FWHM \textasciitilde{}= 2.5 arcsec). The expected outcome is a smaller inner-bin spike near x\textasciitilde{}0.95, a lower RMS residual, and a clean side-by-side SNR comparison with and without the beam-smearing mask.

\textbf{Run:} \texttt{harmonic-halo-stacking-manga --rotcurves data/external/manga/manga\_rotcurves\_all.csv --dapall data/external/manga/dapall\_selection.csv --exclude-inner-psf --output data/external/manga/manga\_stack\_psf\_corrected.csv}

\textbf{Claims:} none

\subsection{E-197: Signal Injection-Recovery Validation}\label{sec:e197}

\textbf{Binary:} \texttt{harmonic-halo-injection} \quad \textbf{Status:} planned \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Inject synthetic ZD harmonic signals with alpha\_zd in \{0, 0.001, 0.002, 0.004, 0.008, 0.016\} into the MaNGA rotation-curve lane, then measure SNR and recovery ratio. The planned acceptance target is SNR > 3 with recovery\_ratio in [0.7, 1.3] for alpha\_zd >= 0.004.

\textbf{Run:} \texttt{harmonic-halo-injection --rotcurves data/external/manga/manga\_rotcurves\_all.csv --dapall data/external/manga/dapall\_selection.csv --alphas 0,0.001,0.002,0.004,0.008,0.016 --output data/external/manga/injection\_recovery.csv}

\textbf{Claims:} none

\subsection{E-198: LoTSS DR2/DR3 MaNGA overlap audit and radio-quiet split}\label{sec:e198}

\textbf{Binary:} \texttt{lotss-fetch} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Join the selected MaNGA DR17 sample to DRPall sky coordinates, measure declination-band coverage ceilings, then crossmatch each target against LoTSS DR2 and DR3 source catalogs at 3 arcsec. Compare overall and declination-banded detection fractions to test whether the low-overlap conjecture is a DR2 coverage effect and preserve the LoTSS-undetected Radio-Quiet subset for downstream MaNGA kinematic analyses.

\textbf{Run:} \texttt{lotss-fetch manga-preflight --manga-selection data/external/manga/dapall\_selection.csv --manga-drpall data/external/manga/drpall-v3\_1\_1.fits --report reports/lotss\_manga\_preflight\_2026-03-13.toml \&\& lotss-fetch crossmatch-manga --release dr2 --input-format fits --input data/external/radio\_surveys/lotss\_dr2.fits --manga-selection data/external/manga/dapall\_selection.csv --manga-drpall data/external/manga/drpall-v3\_1\_1.fits --output data/external/manga/manga\_lotss\_xmatch\_dr2.csv --report reports/manga\_lotss\_xmatch\_dr2.toml \&\& lotss-fetch crossmatch-manga --release dr3 --input-format fits --input data/external/radio\_surveys/lotss\_dr3.fits --manga-selection data/external/manga/dapall\_selection.csv --manga-drpall data/external/manga/drpall-v3\_1\_1.fits --output data/external/manga/manga\_lotss\_xmatch\_dr3.csv --report reports/manga\_lotss\_xmatch\_dr3.toml}

\textbf{Claims:} none

\subsection{E-201: H1 face-on galaxy reanalysis with red-noise baseline subtraction (E-201)}\label{sec:e201}

\textbf{Binary:} \texttt{face-on-red-noise-rayleigh} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} H1 face-on reanalysis: restrict to i<45 deg (N=3140), stack, compute Rayleigh R at 7 CD-ZD wavenumbers, fit k\textasciicircum{}gamma red-noise envelope (gamma=0.808 prior + free-gamma ablation), compute residuals, bootstrap 200x for alpha\_zd CI. Also runs whitened matched filter and window function sensitivity on face-on stacked profile.

\textbf{Run:} \texttt{cargo run --release -p gororoba\_cli\_physics --bin face-on-red-noise-rayleigh -- --rotcurves data/external/manga/rotcurves/manga\_rotcurves\_all.csv --dapall data/external/manga/subsamples/dapall\_lowi.csv --cd-dim 16 --seed 42}

\textbf{Claims:} C-1430, C-1431, C-1433, C-1434

\subsection{E-202: H2 Q3 mass-quartile injection recovery validation (E-202)}\label{sec:e202}

\textbf{Binary:} \texttt{q3-injection-recovery} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} H2 Q3 injection recovery validation: sort galaxies by log(M200), extract Q3 (50th-75th percentile, N=1748, 6 valid bins). Inject alpha\_zd at \{0.001, 0.004, 0.01\}, measure delta\_SNR. Compare with Q1 control (18 bins) and synthetic-6-bin Q1 (window isolation). Bootstrap 200x.

\textbf{Run:} \texttt{cargo run --release -p gororoba\_cli\_physics --bin q3-injection-recovery -- --rotcurves data/external/manga/rotcurves/manga\_rotcurves\_all.csv --dapall data/external/manga/dapall\_selection.csv --cd-dim 16 --seed 42}

\textbf{Claims:} C-1432

\subsection{E-203: Legacy cross-validation audit for refuted algebraic CSV claims}\label{sec:e203}

\textbf{Binary:} \texttt{legacy-crossval-audit} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Exact Rust cross-validation of legacy CSV claims: parity-mask vs commutativity mismatch (C-020), explicit Lambda\_1024 exceptional exclusions (C-021), and moonshine coefficient/log-column sanity checks (C-543).

\textbf{Run:} \texttt{cargo run -p gororoba\_cli\_algebra --bin legacy-crossval-audit -- --output data/output/claims\_falsification/legacy\_crossval\_audit.toml}

\textbf{Claims:} C-020, C-021, C-543

\subsection{E-204: Deterministic audit for refuted external Cayley-Dickson CSV claims}\label{sec:e204}

\textbf{Binary:} \texttt{cd-external-audit} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Exact Rust audit of refuted external Cayley-Dickson CSV claims: sparse 64D adjacency methodology mismatch (C-450), lattice-difference norm census excluding E8 roots (C-455), direct associativity mismatches in the 256D basis-properties CSV (C-456), and parity-clique failure across 16D/32D/64D (C-463).

\textbf{Run:} \texttt{cargo run -p gororoba\_cli\_algebra --bin cd-external-audit -- --output data/output/claims\_falsification/cd\_external\_audit.toml}

\textbf{Claims:} C-450, C-455, C-456, C-463

\subsection{E-205: Deterministic audit for four refuted algebraic-structural claims}\label{sec:e205}

\textbf{Binary:} \texttt{algebra-refutation-audit} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Exact Rust audit for four algebraic-structural negative claims: dim=32 motif classes require cubic GF(2) separation rather than a linear xor mask (C-445), multiplication coupling survives only for the identity basis in dim=16 and dim=32 Lambda dictionaries (C-466), candidate-B associator fibers fail to classify pure triangles across 16D/32D/64D (C-518), and real trace-free J\_3(O) surveys stay far from Singh's delta\textasciicircum{}2 = 3/8 target (C-585).

\textbf{Run:} \texttt{cargo run -p gororoba\_cli\_algebra --bin algebra-refutation-audit -- --output data/output/claims\_falsification/algebra\_refutation\_audit.toml}

\textbf{Claims:} C-445, C-466, C-518, C-585

\subsection{E-206: Deterministic audit for PMNS, mass-hierarchy, and numerology refutations}\label{sec:e206}

\textbf{Binary:} \texttt{particle-numerology-audit} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Shared Rust audit for six particle-numerology negative claims: octonion-subalgebra principal angles collapse to \{0,90\} rather than PMNS values (C-069); democratic associator mixing remains far from PMNS probabilities and is not better than a random doubly-stochastic null (C-077); three-angle Givens fits are exact for arbitrary targets and therefore non-evidentiary (C-081); small Yukawa-like diagonal perturbations improve the democratic matrix only modestly and remain null-like (C-084); depth-based associator norms fail to reproduce the lepton hierarchy and are not significant against a random-assignment null (C-587); and the Planck/BCS 1764 coincidence disappears outside chosen unit systems while the BCS factor remains pi/e\textasciicircum{}gamma numerics (C-1355).

\textbf{Run:} \texttt{cargo run -p gororoba\_cli\_data --bin particle-numerology-audit -- --output data/output/claims\_falsification/particle\_numerology\_audit.toml}

\textbf{Claims:} C-069, C-077, C-081, C-084, C-587, C-1355

\subsection{E-207: Deterministic audit for Cayley-Dickson spectrum, shape-match, and SO(7) orbit refutations}\label{sec:e207}

\textbf{Binary:} \texttt{cd-pattern-baseline-audit} \quad \textbf{Status:} active \quad \textbf{Deterministic:} yes \quad \textbf{GPU:} no

\textbf{Method:} Shared Rust audit for three refuted or methodology-insufficient Cayley-Dickson pattern claims: partner-graph eigenvalue ladders are compared against PDG masses under both log and linear transforms with random-subset nulls (C-068); exact associator-quantile curves are compared against checked-in NANOGrav medians with Frechet nulls and simple template families (C-070); and generic SO(7) rotations leave the exact zero-divisor lane immediately, so the surviving orbit structure is discrete rather than continuous (C-092).

\textbf{Run:} \texttt{cargo run -p gororoba\_cli\_data --bin cd-pattern-baseline-audit -- --output data/output/claims\_falsification/cd\_pattern\_baseline\_audit.toml}

\textbf{Claims:} C-068, C-070, C-092

