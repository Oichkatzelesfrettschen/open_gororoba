# Claim-family evidence adjudication

The canonical revisions separate retained scientific observations from defects in
their producers and reference identities. Original statements remain in the claim
revision history; original failed inputs, thresholds and replay results remain in
the audit bundle. The ordered work register is
`plans/claim-family-evidence-adjudication.toml`. Detailed contracts, source receipts
and numerical results live in `data/output/audit/claim-family-evidence-adjudication/`.

| Family | Measured result | Remaining obligation |
| --- | --- | --- |
| C-659 / C-834 | C-659 records p=0.5841584 and an inconclusive transition to Provisional. Seven signed SFWM reference tests pass; nominal ratio 0.0477442 passes 0.05, while the retained input envelope crosses the threshold. | Percolation producer identity and source-input robustness remain separate. |
| Crossing detector | Joint simultaneous intervals across 120 paired contrasts favor 47 controls and leave 73 directions unresolved. All six matched-capacity geometric intervals cross zero. C-1744 records the bounded comparison. | Practical equivalence needs a declared margin. The separate 0.005 requirement remains unchanged. |
| External crossing admission | Fresh provider bytes reproduce the retained backward timestamp and original SHA256. | Corrected provider evidence or a separately declared intake amendment must precede inference. |
| Operational utility | Activation input inventory distinguishes whole-block labels, randomized review and actual receipt/alert timestamps. | Real observations and owner-supplied costs remain required; synthetic fixtures establish neither latency nor utility. |
| Dark-matter force chain | Corrected drag units, density-weighted Guo forcing, SI magnetic/Parker conversions, strict IC admission and layout-aware inlet tests pass. A 90-row periodic force sweep distinguishes resolved and unresolved responses. | C-1746/E-297 independently verifies finite open-x transport and face-flux accounting. Physical outflow suitability, magnetic boundaries, gravitational frame, admitted spacecraft inputs and coupled numerical sensitivity still block a physical bound. |
| LBM instruments | Real evolving CPU/CUDA and graph-reuse fixtures pass unchanged gates after time-level alignment and dependency repair. C-1745 records the bounded device result. Survival requires finite positive fields and explicit mass/Mach budgets. | E-298 retains 50 direct-step null trials and six step-1 Mach failures with the original executable. E-300/C-1748 fixes a shared MRT impulse defect that backend agreement missed. Paired physical resolutions and fractal interpretation remain unadmitted. |
| Magnonics | Source Tables I/II have separate constructors and an explicit A/B sign map. Angular and exchange units pass independent slopes. Topology rejects sampled degeneracy/singular links. Paired patches distinguish boundary states and reciprocal participation; both walls carry spatial diagnostics. | E-299/C-1747 reproduces source-fitted spectral support within the unchanged 0.03 GHz gate and independent geometry/folding oracles. Global band isolation, author-code retrieval and independent device prediction remain unresolved. |
| Ward identities | Independent interpolation and source rescaling checks repair an intercept sign and an extra counterterm T-squared factor. Scalar off-shell and selected transverse cases pass; spinor and full-vector predicates retain failures. | Quadrature refinement exceeds virtuality-ladder spread; certified error and missing contact/boundary terms remain unresolved. |
| Optics | Faithful replay retains 0.0159400 error against the 0.01 gate. Archive, extracted TeX and PDF identities are separate; primary TeX explicitly describes fitting. | Source-landmark discrepancy and a finite repository-wide receipt manifest remain open. |
| QGP / NuFit | Population admission rejects collision-energy, species and centrality mismatches before QGP ranking. Independent minimization confirms the retained optimizer but exposes a different scoring formula; fitting and scoring now share the declared model. NuFit release/variant identity governs interval membership; historical composite references remain available explicitly. | QGP source curves, covariance and fitting freedoms require admission before model ranking. |
| Finite algebra / Rocq | Exact rational-rank witnesses cover all 243 motif components at D=16,32,64,128,256. Maximum degree is D/2-4. Rocq assumption queries expose the kernel-equality axioms separately from skew-adjoint proofs. | Universal motif extension and source-general kernel equality remain unproved by the inspected artifacts. |

The independent Ward oracle shares the physics tensors and quadrature with the
producer. Its independence covers contraction order, lower-point assembly and
intercept arithmetic; the oracle does not independently derive the integrand.
Likewise, a finite-grid topology admission establishes sampled gaps and link
conditioning, rather than a global band-isolation proof.

Canonical writes use `gororoba-db claim correct-content`, `claim set-evidence`
and typed `claim transition` specifications. Content correction checks the expected
statement and locator atomically and retains both complete prestate values. A
stale prestate or failed audit insertion aborts the correction transaction.
Experiment registrations bind the actual replay producer instead of reusing an
unrelated historical experiment ID.

The execution-planning verifier admits integration tests by
`cargo-test:<package>:<target>` after checking the Cargo manifest and source path.
External tools use versioned identities declared in
`plans/external-execution-targets.toml`; the declaration binds a hashed identity
receipt and its source receipt. Registration verifies retained evidence and leaves
runtime availability and scientific reproduction as separate predicates.

Replays use the commands and input/source hashes recorded in each evidence receipt.
The geometric protocol seals its source files and Cargo.lock; formatting or changing
those files requires a separately declared replay amendment. Export canonical views
with `provenance export-control-plane`, then run `make registry-integrity` and the
repository validation chain. Publication requires the SQLite delta, generated views,
contracts and retained evidence in the same reviewed change.

The MRT impulse oracle supplies a check independent of backend agreement. Before
correction, all 36 CPU/CUDA comparisons passed while both producers delivered only
0.375 of prescribed momentum impulse at tau=0.8. Moment-dependent Guo weighting
and the CUDA half-force velocity correction pass the unchanged conformance and
impulse gates. The finite successor covers standard FP32 push direct/captured
paths. Precision-specific kernels and alternate dispatch validation remain open.

The catalog reader accounts for 399562 source rows: 383308 admitted and 16254
rejected with field-level reasons. E-298 retains paired input arrays and every
failed trial. Adaptive box counting preserves the same mask under contrast scales
1/256, 1 and 256; the statistic alone therefore cannot establish perturbation
amplitude or algebra specificity. Uniform density has an absent measured slope;
the production scalar value 3 is a convention.

The galaxy population reconciliation preserves original 100/100 summaries apart
from the later 100/70 fleet report. The reported mean shift compares 93 nonoutliers
with all 70 completed objects. Source-implied fixed spatial spacing doubles the
box rather than refining a fixed domain, and the replay executable digest remains
unknown. Exact paired CSVs and physical execution identity are required before
convergence inference.

The primary QGP audit verifies all 38 ALICE fixture rows against fresh HEPData.
CUJET Figure 3(a) describes 2.76 TeV 20-30 percent charged hadrons. Prakash Figure 4
models static-medium charm quarks. Typed source quarantine rejects all five
unsupported historical vectors even after display-only relabeling; tests preserve
every numerical array and legacy score penalty. Matched covariance, calibration
freedom and authenticated curve extraction remain prerequisites to ranking.

The Ward source check separates three operations: proper-time rescaling,
polarization contraction and ultraviolet cancellation. The source Eq.4.10 uses a
counterterm without the pre-rescaling T-squared factor. An independent integral
rejects the old measure and verifies the corrected measure at two masses for
both loop selectors. That measure check retains the coefficient convention and
establishes neither its scalar-source identity nor ultraviolet finiteness.
Generic transverse controls distinguish omitted diagrams; the spinor projected
residual and the original full-vector tests retain failed scientific gates.
