[followup]
id = "casimir-optics-discrimination-ci-review-closure"
pr = 185
diagnostic_head = "602804651073313b253e24a698f78b3e30ab7121"
execution_policy = "GitHub Actions owns builds, tests, lint, formatting, and repository gates. Local work is limited to source inspection, hashes, and git diff --check."
publication_policy = "Collect the complete diagnostic-head denominator before one consolidated CI repair. If review findings arrive after that push, let its generation terminate, consolidate the review repairs once, and publish one final closure generation without canceling or superseding an active run."
completion_gate = "The replacement generation passes, every review thread has an evidence-backed disposition, PR 185 merges, canonical main fast-forwards to the merge, and the isolated worktree is removed after parity proof."

[[task]]
id = "calibrated-information-finite-output"
status = "implemented_pending_ci"
finding = "Finite discrimination inputs can produce non-finite squared information or calibration sensitivity."
required_change = "Reject each non-finite result as NumericalFailure and retain overflow regressions."
verification = "Hosted stats_core tests and Clippy pass."

[[task]]
id = "ci-canonical-worker-environment"
status = "implemented_pending_ci"
finding = "Main CI exports worker aliases but omits CARGO_BUILD_JOBS and RAYON_NUM_THREADS."
required_change = "Export every alias and canonical variable with the unchanged available_parallelism count in each worker setup."
verification = "Hosted validation-resource-contract and actionlint pass."

[[task]]
id = "ci-multiple-large-package-shards"
status = "implemented_pending_ci"
finding = "The Rust shard generator isolates only one explicit-binary-heavy package."
required_change = "Accept repeated target-shard packages and partition physics, data, and algebra binaries exactly once."
verification = "Hosted xtask shard tests prove deterministic complete non-overlapping coverage."

[[task]]
id = "ci-hydrated-scientific-replay"
status = "implemented_pending_ci"
finding = "Hydration-dependent lattice, NuFIT, and box-counting tests remain ignored in ordinary and scheduled validation."
required_change = "Retrieve the declared archive by pinned identity, materialize only declared paths, and execute the ignored scientific tests in a fail-slow hosted lane."
verification = "Hosted hydration verifies archive and member hashes, rejects missing materialization, and executes every declared replay test."

[[task]]
id = "diagnostic-generation-denominator"
status = "complete"
finding = "Repeated pushes canceled diagnostic generations before their complete failures were known."
required_change = "Hold publication until all five workflows for the diagnostic head reach terminal states and their outcomes are classified."
verification = "Runs 34743205007, 34743205012, 34743205033, 34743205038, and 34743205034 reached terminal state before the replacement push. Proofs, benchmarks, paper, and unsafe survey passed. CI run 34743205007 found one stale native-output-manifest.toml byte mismatch and one E0382 moved-value error duplicated by Clippy and light-5; all other Rust shards passed."

[[task]]
id = "diagnostic-native-manifest-byte-parity"
status = "implemented_pending_ci"
finding = "The retained native output manifest differed from hosted regeneration by one final blank line."
required_change = "Retain the generator-produced two-newline file ending without changing scientific values."
verification = "Hosted retained-audit freshness passes and local byte inspection reports final bytes 0a 0a."

[[task]]
id = "materials-state-validation-owned-graph"
status = "implemented_pending_ci"
finding = "A material-record test moved its graph into one mutation and then reused the moved value in a second mutation."
required_change = "Clone the graph for the first independently mutated fixture."
verification = "Hosted materials_core tests and workspace Clippy pass."

[[task]]
id = "review-extreme-column-scale-invariance"
status = "implemented_pending_ci"
finding = "Direct column norms can underflow or overflow for finite nuisance scales and erase an exact nuisance direction."
required_change = "Normalize each column through a maximum-component factor and a bounded scaled norm without multiplying the factors."
verification = "Hosted stats_core tests preserve exact one-coordinate fits at scales 1e-200 and 1e200."

[[task]]
id = "review-quantity-basis-single-authority"
status = "implemented_pending_ci"
finding = "Vector and tensor payload basis metadata can disagree with the quantity-level basis field."
required_change = "Keep the payload basis authoritative; reject empty optional aliases and require any populated alias to agree exactly."
verification = "Hosted materials_core tests accept the single payload authority and reject empty or contradictory aliases."

[[task]]
id = "review-specimen-substrate-acyclicity"
status = "implemented_pending_ci"
finding = "Existence checks alone admit self-referential and multi-specimen substrate cycles."
required_change = "Walk each single-parent substrate chain and reject the first repeated specimen identity."
verification = "Hosted materials_core tests reject one-node and two-node cycles."

[[task]]
id = "review-docs-fail-slow-independence"
status = "implemented_pending_ci"
finding = "The documentation job waits for aggregate validation success and therefore hides independently executable documentation failures."
required_change = "Depend only on validation-core routing output; retain aggregate validation as a separate Pages deployment prerequisite."
verification = "Hosted validation-resource-contract and actionlint pass, and docs-gate executes whenever routing selects documentation."

[[task]]
id = "ci-contract-leading-option-literals"
status = "implemented_pending_ci"
finding = "The collector contract passed patterns beginning with two hyphens to grep without an option terminator."
required_change = "Terminate grep options before every literal workflow contract."
verification = "Hosted validation-resource-contract accepts the scientific replay command literals."

[[task]]
id = "hydrator-archive-selection-count-semantics"
status = "implemented_pending_ci"
finding = "Bounded hydration overloaded the established verified_objects report field with the selected-object count."
required_change = "Retain the complete archive-inventory count and report the selected hash/install denominator in a separate field."
verification = "Hosted hydrator tests cover historical archive supersets and bounded one-of-two selection."

[[task]]
id = "target-shard-duplicate-mutation-isolation"
status = "implemented_pending_ci"
finding = "The duplicate target-package mutation also duplicated the routed package scope and exercised the wrong earlier rejection."
required_change = "Deduplicate only the synthetic routed scope while retaining repeated target-package options."
verification = "Hosted xtask tests reach and assert the dedicated duplicate target-package diagnostic."

[[task]]
id = "replacement-generation-denominator"
status = "complete"
finding = "CI run 34744700854 exercised the consolidated CI architecture and retained independent leaf outcomes through aggregate failure."
required_change = "Classify every failed leaf before the final review-closure push."
verification = "Scientific replay and all three companion workflows passed. Forty CI jobs passed. The three failed leaves reduce to the grep option boundary, the hydrator report-field regression, and the target-shard mutation fixture; each has one tracked repair above."

[[task]]
id = "review-bounded-distance-finite-result"
status = "implemented_pending_ci"
finding = "A finite residual vector can have an overflowing naive Euclidean norm, making bounded profile distance return infinity as a successful result."
required_change = "Compute the residual norm through maximum-component scaling and reject a result outside the finite output domain."
verification = "Hosted stats_core tests reject an overflowing two-component bounded-profile distance."

[[task]]
id = "review-covariance-symmetry-scaled-comparison"
status = "implemented_pending_ci"
finding = "Frobenius norms of large finite covariance entries can both overflow and admit an asymmetric covariance."
required_change = "Scale covariance entries by their maximum magnitude before comparing the relative symmetry error."
verification = "Hosted stats_core tests reject an asymmetric covariance with 1e200 entries."

[[task]]
id = "review-fisher-information-finite-result"
status = "implemented_pending_ci"
finding = "A noisy signal component can overflow the Fisher-information accumulator and collide with the intentional infinity result for a deterministic null direction."
required_change = "Reject non-finite accumulation in positive-noise eigendirections while retaining intentional infinity only for deterministic signal components."
verification = "Hosted stats_core tests reject overflow for covariance one and signal 1e200."

[[task]]
id = "review-retained-source-exact-denominator"
status = "complete"
finding = "The native audit accepted any nonempty subset of the twelve load-bearing retained sources."
required_change = "Compile and compare the exact source-ID and repository-path denominator before hashing source bytes."
verification = "Hosted native-audit tests reject replacement of any expected source identity."

[[task]]
id = "review-retained-source-symlink-boundary"
status = "complete"
finding = "Lexically safe retained-source paths could traverse symbolic links and hash bytes outside the repository retention boundary."
required_change = "Inspect every repository-relative path component with symlink metadata and reject symbolic links before reading bytes."
verification = "Hosted Unix native-audit tests reject a retained path whose final component is a symbolic link."

[[task]]
id = "review-frontier-verifier-ci-execution"
status = "complete"
finding = "The finite-frontier verifier existed but the routed hosted Casimir audit leaf did not execute it."
required_change = "Run verify-finite-frontier in the same routed fail-slow CI leaf as native output freshness and pin the command in the validation-resource contract."
verification = "Hosted validation-policy and routed Casimir audit jobs execute the exact frontier partition verifier."

[[task]]
id = "final-generation-denominator"
status = "complete"
finding = "CI run 34746905311 completed all 47 jobs without cancellation: 43 passed, two independent leaves failed, aggregate validation failed by construction, and docs deployment skipped by admission policy."
required_change = "Repair the policy contract's source boundary and both stats_core failures in one final follow-up."
verification = "The four companion workflows passed. Scientific replay, docs-gate, validation-core, and 39 of 40 Rust matrix jobs passed. The independent failures were validation-policy and light-2."

[[task]]
id = "frontier-contract-source-boundary"
status = "complete"
finding = "The policy verifier searched ci.yml for the finite-frontier command even though the command belongs to the routed Make target."
required_change = "Search workflow contracts in ci.yml and inspect the bounded frontier-check Make recipe separately for its verifier and canonical-plan arguments."
verification = "Hosted validation-policy passes both source-specific contract checks."

[[task]]
id = "fixed-nuisance-profile-distance"
status = "implemented_pending_ci"
finding = "Clarabel rejected an exact zero-width nuisance box before the overflow-safe distance calculation could classify the finite residual."
required_change = "Evaluate an all-fixed nuisance box directly at its unique parameter vector and classify every coordinate as active at both bounds."
verification = "Hosted stats_core tests must return the finite sqrt(2)*1e308 distance for a fixed zero nuisance parameter and reject the overflowing sqrt(2)*1.3e308 distance."

[[task]]
id = "calibration-fixture-mechanical-edit-repair"
status = "complete"
finding = "A broad replacement accidentally changed the established independent-calibration fixture from a unit nuisance column to a 1e308 column."
required_change = "Restore the original unit nuisance fixture while retaining dedicated extreme-scale tests in their own cases."
verification = "Hosted stats_core independent_calibration_increases_information passes unchanged semantics."

[[task]]
id = "account-hosted-runner-admission-rca"
status = "complete"
finding = "At exact head a0fcf3a0, four workflows remained queued with zero jobs and the byte-identical proofs workflow ended startup_failure with zero jobs. An exhaustive scan of 548 active owned repositories found zero live hosted jobs outside these records. The public repository uses free standard runners, the Pro account permits 40 concurrent standard jobs, repository Actions are enabled, and September billing reports zero net Actions charges."
required_change = "Treat the five-run split as pre-job scheduler admission failure rather than source validation, and replay through one repository-controlled event root."
verification = "The next exact-head ci run creates jobs and reaches a terminal denominator; any repeated zero-job startup failure is escalated with run, check-suite, account-scan, and billing evidence."

[[task]]
id = "single-ci-event-admission-root"
status = "complete"
finding = "One broad pull request independently created benchmark, ci, paper, proofs, and unsafe-survey workflow runs. The reachable job frontier can exceed the account's 40-job concurrency even though each runner correctly uses all process-visible CPUs."
required_change = "Make ci.yml the sole pull-request and main-push event root, route component paths once, and invoke benchmark, proof, paper, and unsafe-survey evidence through reusable workflows. Preserve manual component dispatch and the monthly unsafe schedule."
verification = "Hosted actionlint and validation-resource-contract pass; one exact-head pull-request workflow contains each selected component job and the aggregate verdict accounts for selected and skipped components."

[[task]]
id = "retired-local-validation-target-removal"
status = "complete"
finding = "Retired local Make targets remained callable refusal stubs and preserved obsolete orchestration names in the active Make graph."
required_change = "Remove every local, pre-push, and local-scoped compatibility target and retain a CI policy check that rejects their reintroduction."
verification = "Hosted validation-resource-contract proves every retired name is unresolved, hook executors remain absent, and CI-only targets reject a non-GitHub execution boundary."

[[task]]
id = "local-command-worker-limit-removal"
status = "complete"
finding = "The shared Cargo environment injected one worker into local mutation and generator commands even though repository validation was closed locally."
required_change = "Leave Cargo, Rayon, and Rust test worker variables unset outside an admitted GitHub job; preserve exact process-visible CPU propagation inside hosted jobs."
verification = "Hosted validation-resource-contract observes workers=automatic outside GitHub Actions and the exact supplied worker count inside the trusted boundary."

[[task]]
id = "scheduler-index-residue"
status = "complete"
finding = "Three zero-job runs from May and one August self-hosted run with a completed skipped job remain listed as queued or in_progress. GitHub's cancel endpoint reports each run as already completed."
required_change = "Exclude the internally terminal records from concurrency accounting and delete their empty or skipped workflow-run records through the explicit run-deletion endpoint."
verification = "DELETE accepted runs 25907145181, 25907085641, 26596485186, and 31339431698; subsequent GET requests returned HTTP 404 for all four IDs."

[[task]]
id = "single-root-replacement-generation"
status = "complete"
finding = "The a0fcf3a0 generation cannot validate source because GitHub admitted zero jobs."
required_change = "Publish one consolidated CI admission repair after static source review and do not supersede its generation."
verification = "Run 34769957057 at exact head 67af5dfa completed 52 jobs without cancellation: 49 passed, one Rust leaf exposed the incorrect overflow fixture, the aggregate failed by construction, and one deployment job skipped by admission policy."

[[task]]
id = "hydrator-unselected-object-digest"
status = "implemented_pending_ci"
finding = "Bounded hydration checked an unselected archive member's byte length without checking its declared object digest."
required_change = "Hash every archive member while staging only selected objects, and reject a corrupt unselected member before installation."
verification = "Hosted hydrator tests corrupt an equal-length unselected member and require clean failure."

[[task]]
id = "finite-frontier-closed-open-dependency"
status = "implemented_pending_ci"
finding = "The finite-frontier verifier accepted a closed row whose ordering dependency remained open."
required_change = "Reject each closed-to-open dependency edge after compiling the complete row map."
verification = "Hosted finite-frontier tests mutate one closed row to depend on an expected-open row and require the exact diagnostic."

[[task]]
id = "zero-temperature-observable-selective-integration"
status = "implemented_pending_ci"
finding = "Each zero-temperature pressure or energy request evaluated both independent quadrature integrands and discarded one result."
required_change = "Select the pressure or energy integrand before quadrature while retaining one shared transformed-domain implementation."
verification = "Hosted materials_core tests preserve ideal normalization, multilayer order contrast, and the independent energy-derivative identity."

[[task]]
id = "casimir-toolchain-identity-route"
status = "implemented_pending_ci"
finding = "The retained Casimir source-model identity includes rust-toolchain.toml, but the path router omitted that file from the freshness lane."
required_change = "Route a rust-toolchain.toml change through the Casimir audit freshness check."
verification = "Hosted validation-policy accepts the route and path classification selects the audit for toolchain-only changes."

[[task]]
id = "bounded-profile-wide-interval-tolerance"
status = "implemented_pending_ci"
finding = "Subtracting opposite-sign finite nuisance bounds could overflow and make feasibility and active-bound tolerances infinite."
required_change = "Scale tolerances from the maximum endpoint magnitude without forming the interval width."
verification = "Hosted stats_core tests classify zero as inactive inside [-1e308, 1e308]."

[[task]]
id = "covariance-negative-diagonal-dynamic-range"
status = "implemented_pending_ci"
finding = "A global covariance-magnitude tolerance could hide a negative variance when an unrelated diagonal entry was much larger."
required_change = "Reject every negative covariance diagonal entry before applying scale-aware symmetry and eigensystem tolerances."
verification = "Hosted materials_core tests reject diag(1e200, -1) as non-positive-semidefinite."

[[task]]
id = "exact-head-4892d755-denominator"
status = "complete"
finding = "Run 34772236288 admitted 52 jobs and reached a complete denominator: 49 passed, validation-core failed on one stale native-output-manifest.toml source identity, aggregate validation failed by construction, and docs deployment skipped. Every Rust shard, all three scientific replays, documentation, benchmarks, proofs, paper, and unsafe survey passed."
required_change = "Let every job reach a terminal state, classify every independent failure, and preserve the complete denominator before publication."
verification = "Run 34772236288 completed at exact head 4892d755 with 49 success, two failure, one skipped, and zero canceled jobs."

[[task]]
id = "validation-routing-expensive-leaf-overlap"
status = "implemented_pending_ci"
finding = "The routing job serialized 11m48s of governance and 6m31s of Casimir audit work after routing, delaying Rust fan-out and hiding independent outcomes behind one 22-minute job."
required_change = "Keep validation-core as the routing authority and run governance plus integrity and the Casimir audit as separately admitted jobs with exact per-runner worker detection."
verification = "Hosted actionlint and resource contracts pass; Rust shards start after routing; selected governance and Casimir jobs overlap and remain explicit aggregate prerequisites."

[[task]]
id = "scientific-replay-hydration-fanout"
status = "implemented_pending_ci"
finding = "Hydration, lattice replay, NuFIT replay, and box-counting replay ran serially in one 39-minute job."
required_change = "Materialize the bounded hash-verified payload once, upload the selected inputs, run selected replay leaves through a fail-slow matrix, and aggregate their terminal result."
verification = "Hosted CI runs every selected leaf independently, retains one report per leaf, and distinguishes not_selected, blocked_input, executed_fail, and executed_pass."

[[task]]
id = "box-counting-replay-narrow-owner"
status = "implemented_pending_ci"
finding = "The retained 16-cubed, 24-step box-counting replay executed in 0.05 seconds, but its gororoba_cli_physics integration target compiled 383 crates for 29m31s."
required_change = "Move the replay to lbm_3d while preserving the retained input hashes, CPU MRT path, population stability checks, adaptive geometry assertions, and optional evolved-field receipt."
verification = "Hosted lbm_3d replay passes with the expected scalar and masks, and its build avoids the gororoba_cli_physics umbrella target."

[[task]]
id = "scientific-replay-lane-cache-retention"
status = "implemented_pending_ci"
finding = "The scientific replay restored exact-head and fallback caches but never saved a scientific replay cache, forcing repeated cold compilation."
required_change = "Save distinct hydration and replay-lane caches with stable Cargo.lock and toolchain fallback prefixes plus run-unique exact-head primary keys."
verification = "A subsequent hosted head reports cache restoration for each previously built selected lane without concurrent cache-key collisions."

[[task]]
id = "native-manifest-current-source-identity"
status = "implemented_pending_ci"
finding = "Run 34772236288 generated a new stats_core source identity, and the pending selective Lifshitz integration repair changes a second retained source byte identity."
required_change = "Recompute the exact ordered path-NUL-bytes-NUL source identity and both changed source hashes without altering retained numerical output claims."
verification = "Hosted Casimir audit freshness reproduces the source manifest and all retained numerical outputs byte-for-byte."

[[task]]
id = "scientific-replay-selected-input-boundary"
status = "implemented_pending_ci"
finding = "The first fan-out draft selected replay jobs independently but hydrated and uploaded all eight inputs, so an unrelated unavailable object could block a selected leaf."
required_change = "Build hydration and staging path arrays from the selected replay matrix and upload only their path-preserving union."
verification = "Hosted routing mutations select each replay alone and its hydration receipt contains only that replay's declared inputs."

[[task]]
id = "ci-run-unique-leaf-cache-keys"
status = "implemented_pending_ci"
finding = "A cache key ending at the commit SHA collides across pull-request, push, dispatch, and rerun executions of the same source."
required_change = "Append run ID and run attempt to each new immutable primary key while retaining commit- and lock-scoped restore prefixes."
verification = "Hosted policy rejects every new leaf key that omits the run-unique suffix."

[[task]]
id = "casimir-route-policy-literal"
status = "implemented_pending_ci"
finding = "The first route policy draft passed two literal backslashes to fixed-string grep while the workflow regex contains one."
required_change = "Match the exact one-backslash rust-toolchain path expression in the workflow source."
verification = "Hosted validation-resource-contract accepts the Casimir toolchain route literal."

[[task]]
id = "exact-head-41325237-lockfile-precondition"
status = "implemented_pending_ci"
finding = "Run 34774800856 admitted 40 concurrent hosted jobs, but the lbm_3d replay relocation added repo_root and sha2 dev-dependencies without adding them to the lbm_3d Cargo.lock package dependency list. Scientific hydration and both benchmark architectures independently stopped at Cargo's --locked precondition before source validation."
required_change = "Add the two existing workspace packages to the lbm_3d lockfile dependency list, then publish one replacement generation after the canceled run reaches terminal state."
verification = "Run 34775470409 proved that the workspace's simultaneous sha2 0.10.9 and 0.11.0 packages require the disambiguated sha2 0.11.0 lock dependency. The next replacement generation must pass Cargo's locked dependency-graph check in scientific hydration, both benchmark architectures, and every selected Rust validation leaf."

[[task]]
id = "long-job-scientific-replay-compile-surface"
status = "implemented_pending_ci"
finding = "Run 34772236288 spent 29 minutes 33 seconds compiling 383 crates for the gororoba_cli_physics box-counting integration target and 0.05 seconds executing the retained replay. The runner exposed four CPUs and Cargo received all four."
required_change = "Keep retained input and dynamics assertions in the narrow lbm_3d integration target, fan out the three replay leaves, and save one cache per replay lane."
verification = "The replacement box-counting leaf compiles lbm_3d rather than gororoba_cli_physics and reports its setup, build, and test durations independently."

[[task]]
id = "long-job-documentation-critical-path"
status = "implemented_pending_ci"
finding = "Run 34772236288 spent 20 minutes 15 seconds in full-workspace rustdoc and 2 minutes 25 seconds building registry-emit in a second target/profile. The runner exposed four CPUs and CARGO_BUILD_JOBS remained four despite Cargo rejecting a stale inherited GNU make jobserver file descriptor."
required_change = "Start docs-gate immediately after the four-minute routing authority instead of after governance and Casimir validation; preserve the full-workspace documentation and generated-book evidence as one independently visible leaf."
verification = "The replacement docs-gate starts after validation-core routing, overlaps governance, Casimir, and Rust leaves, and reports the unchanged process-visible four-worker budget."

[[task]]
id = "rust-shard-cache-quota-thrash"
status = "implemented_pending_ci"
finding = "The repository cache API exposed 13 caches totaling 13.81 GB after one generation. Seven remaining ci-rust shard caches occupied 8.44 GB with duplicate target graphs, while the successfully saved Rocq cache had already been evicted and the next proof job rebuilt the complete toolchain."
required_change = "Restore every Rust matrix leaf from the one routing cache for the run and stop writing one immutable target cache per shard. Retain caches only for independent owners whose build products have cross-run value."
verification = "Hosted policy rejects ci-rust matrix cache keys, all Rust leaves restore the exact-run routing key, and the post-run cache inventory contains no new per-shard target caches."

[[task]]
id = "proof-opam-worker-propagation"
status = "implemented_pending_ci"
finding = "The proof workflow passed all four detected CPUs to Make but left opam's switch and package builds to an implicit default."
required_change = "Export OPAMJOBS from the unchanged std::thread::available_parallelism result and pass it explicitly to both opam build commands."
verification = "Hosted proof logs report the process-visible worker count and both cold opam commands receive exactly that count."
