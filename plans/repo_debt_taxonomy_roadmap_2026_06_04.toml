# Reconciled, evidence-grounded technical-debt taxonomy and forward roadmap.
#
# WHY: The repository accumulated three overlapping debt artifacts (the
# 2026-04-11 roadmap with 12 classes, the 2026-05-12 post-gate tracker,
# and ~24 scattered DEBT-* identifiers strewn across plans, data, docs,
# and source). The fragmentation is itself a debt (DEBT-TAXONOMY-COHERENCE):
# a reader cannot tell a canonical debt CLASS from a Stage-B work-item TAG,
# and several debt surfaces the maintainer named (methodology, organizational,
# language/package-manager/delivery, and the domain-aligned numerical /
# dimensional / transcription / proof-extraction surfaces) were only
# implicit. This file reconciles all prior identifiers into one coherent
# six-axis taxonomy, grounds every class in a reproducible measurement, and
# defines the forward phasewise/stepwise/taskwise program.
#
# WHAT: 22 canonical debt classes across 6 macro-axes; a reconciliation
# table mapping every legacy DEBT-* tag onto a canonical class; a fresh
# measured baseline reference (data/output/debt_baseline_2026_06_04.toml);
# a machine-readable strict gate baseline; and 8 forward phases with atomic,
# falsifiable tasks.
#
# HOW: Each [[debt_class]] carries a current_magnitude metric with the exact
# command that reproduces it, confirmed_examples by path, a resolution
# pattern, and a falsifiable_acceptance criterion (a command whose pass/fail
# decides whether the class is closed). Phases reference these via task
# acceptance_hooks. Re-measure by re-running repo-audit (see [provenance]).

[meta]
id = "REPO-DEBT-TAXONOMY-ROADMAP-2026-06-04"
updated = "2026-08-01"
status = "active"
authoritative = true
scope = "open_gororoba repository: code structure, build/toolchain/delivery, test/verification, data/provenance, scientific/methodology, documentation/organizational debt"
source_mode = "repo_evidence_plus_repo_audit_anchored_measurement"
supersedes = "plans/repo_debt_roadmap_2026_04_11.toml"
subsumes = ["plans/post_gate_optimization_2026_05_12.toml"]
evidence_baseline = "data/output/debt_baseline_2026_06_04.toml"
strict_gate_baseline = "data/output/audit/2026-07-09/repo_audit_anchored_2026_07_10.toml"
predecessor_baselines = [
    "data/output/debt_baseline_2026_04_30.toml",
    "data/output/debt_baseline_2026_05_09.toml",
]

[meta.relationship_to_predecessors]
roadmap_2026_04_11 = "Historical. Its 12 [[debt_class]] ids are preserved verbatim here (no renames -- renaming breaks the C-/I-/E- style cross-references in source comments and finding-docs). Its PH-0..PH-6 + PH-MOD phase plan is treated as executed-or-in-flight; the open remainder is folded into the forward phases below."
post_gate_optimization_2026_05_12 = "Folded into DEBT-MODULARIZATION (monolith remainder), DEBT-DATA-PROVENANCE (registry counter reconciliation), and DEBT-OBSERVABILITY (gate-timing soak). Its task-id references (#116-#130) remain valid task history."
scattered_tags = "DEBT-PROOF-1/-2, DEBT-UNSAFE-1/-CRATES/-PROOFS/-PROOFS-SCAFFOLDING, DEBT-TEST-1/-2, DEBT-PLACEHOLDER-1/-2, DEBT-EXPERIMENT-1, DEBT-CLAIMS-PROOF are Stage-B WORK-ITEM TAGS (in [task_scope_adjustments] of the 2026-05-09 baseline and in docs/engineering/*_2026_05_09.md finding-docs), not canonical classes. They are reconciled in [[identifier_reconciliation]] below."

# ----------------------------------------------------------------------------
# Execution contract (inherited from the 2026-04-11 roadmap, unchanged).
# ----------------------------------------------------------------------------
[execution_contract]
warnings_as_errors = true
full_workspace_gate_default = false
prefer_crate_scoped_validation = true
network_in_tests = "forbidden"
default_network_features = "forbidden_without_explicit_opt_in"
generated_files = "do_not_hand_edit"
registry_mutation = "sqlite_canonical_then_export"
documentation_updates = "update_source_registries_first_then_regenerate"
hardcoded_demonstrations = "forbidden"
ascii_only = true
comment_hygiene = "no_task_pr_phase_or_deictic_refs_in_source"

# ----------------------------------------------------------------------------
# Six-axis taxonomy. Each axis groups the canonical classes that share a
# review surface, a tooling lane, and an owner mental-model. The grouping
# maps the maintainer's enumerated debt kinds onto measurable classes.
# ----------------------------------------------------------------------------
[taxonomy]
axis_count = 6
class_count = 22

[taxonomy.axes.A_code_structure]
label = "Code and structure debt"
classes = [
    "DEBT-MODULARIZATION",
    "DEBT-STRUCTURAL-ARCHITECTURE",
    "DEBT-DUPLICATION",
    "DEBT-GENERATED-ARTIFACT",
    "DEBT-MEMORY-SAFETY",
]
review_lane = "cargo clippy + cpd-audit + repo-audit (unsafe/SAFETY) + monolith inventory"

[taxonomy.axes.B_build_toolchain_delivery]
label = "Build, toolchain, package-manager, and delivery debt"
classes = [
    "DEBT-BUILD-WORKSPACE",
    "DEBT-SUPPLY-CHAIN",
]
review_lane = "Makefile/xtask migration, cargo-deny, dep-audit, semver-check, toolchain pin"

[taxonomy.axes.C_test_verification]
label = "Test and verification debt"
classes = [
    "DEBT-TEST-VERIFICATION",
    "DEBT-FORMAL-VERIFICATION",
    "DEBT-PROOF-EXTRACTION-FIDELITY",
    "DEBT-OBSERVABILITY",
    "DEBT-MEASUREMENT-VALIDITY",
]
review_lane = "nextest coverage, proofs/ vos+vok, extraction parity, gate-timing telemetry, audit-instrument population validity"

[taxonomy.axes.D_data_provenance]
label = "Data and provenance debt"
classes = [
    "DEBT-DATA-PROVENANCE",
    "DEBT-DATA-TRANSCRIPTION",
]
review_lane = "provenance registry, source hashing, transcribed-table codegen-from-source"

[taxonomy.axes.E_scientific_methodology]
label = "Scientific and methodology debt (domain-aligned)"
classes = [
    "DEBT-NUMERICAL-ALGORITHM",
    "DEBT-NUMERICAL-REPRODUCIBILITY",
    "DEBT-DIMENSIONAL-UNITS",
    "DEBT-SCIENTIFIC-EVIDENCE",
    "DEBT-METHODOLOGY",
]
review_lane = "solver parity tests, determinism/seed control, unit-correctness, claim-evidence linkage, falsifiability discipline"

[taxonomy.axes.F_documentation_organizational]
label = "Documentation and organizational debt"
classes = [
    "DEBT-DOCUMENTATION-REQUIREMENTS",
    "DEBT-ORGANIZATIONAL",
    "DEBT-TAXONOMY-COHERENCE",
]
review_lane = "registry-driven docs regeneration, plan/branch lifecycle, agent coordination, this taxonomy"

# ----------------------------------------------------------------------------
# Measurement provenance. The numbers embedded in current_magnitude below
# come from the repo-audit binary (anchored, comment/string-stripped regex)
# and from direct find/wc/ripgrep census. Every number is reproducible.
# ----------------------------------------------------------------------------
[provenance]
audit_binary = "crates/gororoba_cli_data/src/bin/repo_audit.rs"
audit_command = "REPO_AUDIT_OUT=data/output/audit/2026-06-04 make repo-audit  # writes the dated anchored snapshot and includes SQLite revisions"
audit_strict_command = "make repo-audit-strict  # compares against data/output/audit/2026-07-09/repo_audit_anchored_2026_07_10.toml by default (cache-invariant walker + shadow marker + kernel metrics)"
metric_taxonomy_doc = "docs/engineering/repo_audit_metric_taxonomy.md"
monolith_inventory_command = "find crates xtask -name '*.rs' -not -path '*/registry_mirrors/*' | xargs wc -l | sort -rn"
registry_count_command = "grep -c '^\\[\\[claim\\]\\]' registry/claims.toml  # and insights/experiments/binaries"
measured_at = "2026-06-04"

# ----------------------------------------------------------------------------
# AXIS A -- CODE AND STRUCTURE DEBT
# ----------------------------------------------------------------------------

[[debt_class]]
id = "DEBT-MODULARIZATION"
axis = "A_code_structure"
label = "Hand-written monolith modularization"
risk = "Multi-thousand-line hand-written .rs files slow language servers, defeat focused review, and concentrate merge conflicts. Single impl blocks running thousands of lines mask coupling and obscure module boundaries."
current_magnitude = "2026-07-09 census: worst non-generated files are optical_database.rs 7638 (transcribed data), boxkites/tests.rs 7016 (relocated test sibling), repo_visuals.rs 4394, provenance_store/lib.rs 4372 (REGREW +152 from 4220 post-split -- the only prior split that is regressing), emanation/tests.rs 4327. The dominant hand-written hotspot class is the CLI bin/ tree: registry_emit.rs 3779, human_acquire.rs 3707, governance_verify.rs 3586, execution_planning.rs 3434, plus xtask/src/main.rs 3535. All other PH-MOD splits held within a few lines (heliosphere_eval 1932, source_provenance 2303, codebook 2397, boxkites parent 638, sedenion_subalgebras parent 105); residual over-2500 stragglers: cd_external.rs 2823, nanograv/engine.rs 2714, lbm_3d_cuda/lib.rs 2546."
magnitude_reproduce = "find crates xtask -name '*.rs' -not -path '*/registry_mirrors/*' | xargs wc -l | sort -rn | head -25"
confirmed_examples = [
    "crates/materials_core/src/optical_database.rs (7638; but see DEBT-DATA-TRANSCRIPTION -- this is transcribed data, not logic)",
    "crates/algebra_analysis/src/boxkites/tests.rs (7016; boxkites.rs main split into submodules already, tests remain bulky)",
    "crates/gororoba_cli_data/src/bin/repo_visuals.rs (4394)",
    "crates/provenance_store/src/lib.rs (4372; was 5335, split to 4220, regrew +152 -- re-split candidate)",
    "xtask/src/main.rs (3535; manual string-dispatcher monolith, still growing)",
    "CLI bin/ hotspot class: registry_emit.rs 3779, human_acquire.rs 3707, governance_verify.rs 3586, execution_planning.rs 3434",
]
resolution_pattern = "Split monoliths by logical surface (types vs algorithms vs IO vs phenomenon classes), keep the public API stable via pub use re-exports from the original module path, validate per-crate with cargo check + clippy + nextest under workspace deny-warnings. Test-heavy files (boxkites/tests, emanation/tests) use the #[cfg(test)] mod tests; + sibling parent/tests.rs relocation."
falsifiable_acceptance = "find crates xtask -name '*.rs' -not -path '*/registry_mirrors/*' | xargs wc -l | awk '$2!=\"total\" && $1>=4000 && index($2,\"optical_database.rs\")==0 && index($2,\"crystal_symmetry.rs\")==0 && index($2,\"boxkites/tests.rs\")==0 && index($2,\"emanation/tests.rs\")==0 {bad=1; print} END{exit bad}'"
status = "in_progress"
predecessor_ids = ["DEBT-MODULARIZATION (PH-MOD)"]
cross_links = [
    "plans/post_gate_optimization_2026_05_12.toml task #116-#127",
    "DEBT-DATA-TRANSCRIPTION",
]

[[debt_class]]
id = "DEBT-STRUCTURAL-ARCHITECTURE"
axis = "A_code_structure"
label = "Crate-boundary and layering architecture"
risk = "Blurred crate boundaries obscure ownership; CLI crates depending directly on low-level numerical substrate (faer, wide) defeat domain-facade isolation and make feature isolation and review harder."
current_magnitude = "data_core still mixes pure model/parse/validate with fetch/cache/provider surfaces; some CLI crates depend directly on faer/wide rather than through a domain facade. Qualitative -- tracked by cargo tree depth-1 audits per CLI crate."
magnitude_reproduce = "cargo tree -p gororoba_cli_data --depth 1 ; cargo tree -p data_core --no-default-features"
confirmed_examples = [
    "crates/data_core fetch/parse/validate mixing (docs/engineering/data_core_surface_audit_2026_04_06.txt)",
    "CLI-to-substrate direct deps (docs/engineering/simd_algorithmic_cayley_dickson_audit_2026_04_06.txt)",
]
resolution_pattern = "Split pure core from IO/network planes; move math behind domain facades (algebra_analysis, spectral_core, quantum_core); make opt-in features explicit; thin CLI crates to parse/dispatch/report."
falsifiable_acceptance = "cargo tree --depth 1 for each gororoba_cli_* crate shows no direct faer or wide edge; cargo check -p data_core --no-default-features succeeds with an empty default feature set."
status = "in_progress"
predecessor_ids = ["DEBT-STRUCTURAL-ARCHITECTURE"]
cross_links = ["roadmap_2026_04_11 PH-2, PH-3"]

[[debt_class]]
id = "DEBT-DUPLICATION"
axis = "A_code_structure"
label = "Code duplication (CPD-detected)"
risk = "Repeated hand-written logic causes drift, review fatigue, and inconsistent fixes. Divergent copies of the same optimizer or kernel can yield inconsistent scientific conclusions."
current_magnitude = "Tracked by PMD CPD token-clusters; the make lanes already separate domain logic (cpd-audit), tooling (cpd-audit-tooling), and generated surfaces (cpd-audit-generated). Strict gate (cpd-audit-strict) is held behind generated-surface and lexical-error triage."
magnitude_reproduce = "make cpd-audit CPD_TOP=20"
confirmed_examples = [
    "Historical: bounded_nelder_mead duplicated between cosmology bounce and observational fitting (resolved -- hoisted to shared module).",
    "CPD clusters in algebra_analysis boxkites, spectral_core neg_dim, heliosphere feature cube.",
]
resolution_pattern = "Separate generated/table duplication from hand-written logic duplication; extract shared helpers with parity checks; promote cpd-audit-strict to a gate only after generated-surface quarantine and lexical triage."
falsifiable_acceptance = "make cpd-audit-strict exits zero at the agreed minimum-token threshold with the generated and tooling lanes excluded."
status = "in_progress"
predecessor_ids = ["DEBT-DUPLICATION"]
cross_links = ["roadmap_2026_04_11 PMD lanes", "DEBT-GENERATED-ARTIFACT"]

[[debt_class]]
id = "DEBT-GENERATED-ARTIFACT"
axis = "A_code_structure"
label = "Generated artifact and source-of-truth quarantine"
risk = "Large generated Rust mirrors drown language servers and CPD reports and invite accidental manual edits, desyncing them from their canonical source (registry SQLite)."
current_magnitude = "crates/data_core/src/registry_mirrors/ holds hundreds of generated Rust files mirroring the SQLite control plane; these are excluded from monolith and CPD logic lanes by design."
magnitude_reproduce = "find crates/data_core/src/registry_mirrors -name '*.rs' | wc -l ; make registry-verify-mirrors"
confirmed_examples = [
    "crates/data_core/src/registry_mirrors/ (generated from registry/canonical/control_plane.sqlite3)",
    "registry/*.toml (AUTO-GENERATED read-only compat exports; header on line 1 of each).",
]
resolution_pattern = "Mark every generated surface with source + generator + regeneration command metadata; keep separate CPD reports for generated vs hand-written; never hand-edit a file whose first line is the AUTO-GENERATED header."
falsifiable_acceptance = "make registry-verify-mirrors and the governance-gate-readonly schema_signatures check both pass, proving mirrors match canonical SQLite."
status = "controlled"
predecessor_ids = ["DEBT-GENERATED-ARTIFACT"]
cross_links = ["docs/engineering/registry_canonical_architecture.md"]

[[debt_class]]
id = "DEBT-MEMORY-SAFETY"
axis = "A_code_structure"
label = "Unsafe-block SAFETY-comment coverage"
risk = "unsafe blocks without a SAFETY justification cannot be reviewed for the invariant they assume. Highest-risk pattern is raw-pointer AoSoA indexing in the Cayley-Dickson SIMD kernels."
current_magnitude = "520 unsafe blocks in crates/ with 193 SAFETY comments (37.1% block-level coverage) at the 2026-07-09 anchored audit, up from 65/392 = 16.6% at 2026-05-09. The absolute uncovered surface (327) is the live target. The previously-reported separate lane of 3420 unsafe blocks in proofs/ Rust test/verifier scaffolding is a MEASUREMENT ARTIFACT, not project code: no .rs file has ever been git-tracked under proofs/, and the 130-file/3420-block count reappears in any audit run while proofs/.cache/gate-target holds dependency build-script outputs. The proofs scaffolding lane is retired; see DEBT-MEASUREMENT-VALIDITY."
magnitude_reproduce = "make repo-audit  # reads unsafe_blocks vs safety_comments per root; crates/ row only -- the proofs row is cache-contaminated until the exclusion fix lands"
confirmed_examples = [
    "crates/cd_kernel raw-pointer SoA indexing (UnsafeAoSoAPtr highest-risk per unsafe audit).",
    "Retired artifact: by_root.proofs 3420 unsafe blocks = dependency build outputs under proofs/.cache/gate-target (0 when the cache is pruned; git log --all shows zero .rs ever tracked under proofs/).",
]
resolution_pattern = "Add a // SAFETY: comment immediately above every unsafe block stating the upheld invariant; drive crates/ block-level coverage toward 100% with documented exceptions; keep the proofs/ scaffolding lane separate (test-only, lower priority)."
falsifiable_acceptance = "repo-audit reports safety_comments >= unsafe_blocks for crates/ (100% block coverage) OR a documented exceptions list accounts for every uncovered block."
status = "in_progress"
predecessor_ids = [
    "DEBT-UNSAFE-1",
    "DEBT-UNSAFE-CRATES",
    "DEBT-UNSAFE-PROOFS",
    "DEBT-UNSAFE-PROOFS-SCAFFOLDING",
]
cross_links = ["docs/engineering/unsafe_safety_methodology_2026_05_09.md", "make geiger"]

# ----------------------------------------------------------------------------
# AXIS B -- BUILD, TOOLCHAIN, PACKAGE-MANAGER, AND DELIVERY DEBT
# ----------------------------------------------------------------------------

[[debt_class]]
id = "DEBT-BUILD-WORKSPACE"
axis = "B_build_toolchain_delivery"
label = "Build system, package-manager, root workspace, and delivery"
risk = "Makefile sprawl, implicit feature defaults, toolchain-pin drift, and a manual xtask dispatcher obscure the real release contract and resist typed validation."
current_magnitude = "xtask migration is PARTIAL: 20 subcommands behind a 3535-line manual string-match dispatcher, while only 11 of ~191 Makefile cargo-run sites route through xtask (Makefile: 2497 lines, ~316 targets; coverage 5-10%). Toolchain pinned nightly-2026-04-05; the stale nightly-2026-03-05 prose in CLAUDE.md and AGENTS.md was corrected 2026-07-09. WARNINGS-NET HOLE: 5 workspace members carry no [lints] block (fixed_point_lbm, gororoba_cli_quantum, lit_search, manga_experiments, xtask), and cargo requires the explicit [lints] workspace = true opt-in, so workspace warnings = deny does NOT apply to them. The .githooks/pre-push prose disagrees with itself (header enumerates 5 steps, runtime echo says 4-gate, AGENTS.md canonically documents 6; all six logical gates do run). 12 live .sh scripts contradict the flat no-shell policy; several are declared xtask fallbacks (detect_worker_budget.sh) but the policy admits no exception ledger."
magnitude_reproduce = "grep -c '=> run_' xtask/src/main.rs ; grep -rn nightly rust-toolchain.toml registry/requirements_narrative.toml AGENTS.md CLAUDE.md"
confirmed_examples = [
    "xtask/src/main.rs (3411-line manual dispatcher; also a DEBT-MODULARIZATION item).",
    "Makefile policy-heavy recipes (cpd-audit-strict, integrity, governance) still shell-driven.",
    "Stale-doc drift RESOLVED 2026-07-09: AGENTS.md:89 and CLAUDE.md:96 corrected to nightly-2026-04-05.",
    "Warnings-net hole: crates/fixed_point_lbm, crates/gororoba_cli_quantum, crates/lit_search, crates/manga_experiments, xtask lack [lints] workspace = true.",
    "12 tracked .sh files (bin/, scripts/) vs the no-shell policy; no exception ledger exists.",
    "proofs/Makefile toolchain guard accepts ANY rocq on PATH as fallback: with the rocq-certirocq switch active, the system Rocq (OCaml 5.5.0) ran against a .vo tree built by the pinned rocq-9.1.1 switch (OCaml 5.4.0), yielding mixed-toolchain parse errors instead of a fail-fast message naming the documented opam env remedy.",
]
resolution_pattern = "Keep make as the user-facing front door; migrate policy-heavy parsing into typed xtask subcommands with structured JSON/TOML output; convert the manual string dispatcher to a clap Subcommand enum; update the stale nightly references in the prose guides to the authoritative pin."
falsifiable_acceptance = "cargo run -q -p xtask -- --help >/dev/null && test \"$(grep -rh nightly rust-toolchain.toml AGENTS.md CLAUDE.md registry/requirements_narrative.toml | grep -o 'nightly-[0-9-]*' | sort -u | wc -l)\" -eq 1; every workspace member Cargo.toml (and xtask/Cargo.toml) contains a [lints] table inheriting workspace = true; .githooks/pre-push header, runtime echo, and AGENTS.md agree on one gate count; each tracked .sh file is either absorbed into xtask or listed in a shell-exception ledger with rationale"
status = "in_progress"
predecessor_ids = ["DEBT-BUILD-WORKSPACE"]
cross_links = ["roadmap_2026_04_11 PH-5"]

[[debt_class]]
id = "DEBT-SUPPLY-CHAIN"
axis = "B_build_toolchain_delivery"
label = "Security, license, and dependency-advisory currency"
risk = "Advisories, duplicate dependency versions, unused deps, and accumulating RUSTSEC ignores enter through wide workspace growth; an ignore list that only grows hides unaddressed advisories."
current_magnitude = "deny.toml has 6 active RUSTSEC ignores, down from 7 at the 2026-04-30 baseline (the rustls-webpki cluster 0098/0099/0104 was resolved via 0.103.13; core2 0105 was added). An earlier 7->9 figure was a measurement artifact: grep -c RUSTSEC counts the 3 resolved ids named in the deny.toml resolution comment. Use grep -cE '^[[:space:]]*\"RUSTSEC' for the true active count. 2160 duplicate-dependency lines in cargo tree -d; cargo-machete reports 0 unused deps (Stage A)."
magnitude_reproduce = "grep -cE '^[[:space:]]*\"RUSTSEC' deny.toml ; cargo tree -d | wc -l ; make machete"
confirmed_examples = [
    "deny.toml has 6 active RUSTSEC ignores, each already annotated with the blocking dependency and dependency path, plus docs/adr/rustsec-dispositions.md.",
    "rustls-webpki 0098/0099/0104 resolved 2026-04-30 via cargo update -p rustls-webpki --precise 0.103.13.",
    "2160 duplicate-dependency lines (cargo tree -d).",
]
resolution_pattern = "Keep cargo-deny as the hard gate; for each RUSTSEC ignore record the blocking dependency and a re-check trigger (already present in deny.toml + the ADR); use duplicate-version audits for planning, not as a hard gate. Count active ignores with the anchored regex, never a bare grep -c."
falsifiable_acceptance = "Every entry in deny.toml [advisories].ignore has an adjacent comment naming the blocking dependency and a re-check trigger (satisfied); grep -cE '^[[:space:]]*\"RUSTSEC' deny.toml does not exceed the prior snapshot without a recorded justification."
status = "controlled"
predecessor_ids = ["DEBT-SUPPLY-CHAIN"]
cross_links = ["make cargo-deny-check", "make dep-audit"]

# ----------------------------------------------------------------------------
# AXIS C -- TEST AND VERIFICATION DEBT
# ----------------------------------------------------------------------------

[[debt_class]]
id = "DEBT-TEST-VERIFICATION"
axis = "C_test_verification"
label = "Test coverage, ignored tests, and verification gates"
risk = "High-value behavior lacking narrow reproducible hooks forces slow full-workspace validation or under-validated changes. #[ignore] tests silently skip coverage; unjustified clippy allows hide real lints."
current_magnitude = "176 #[ignore] attributes in crates/ at 2026-07-09. Lane audit (2026-07-09): exactly ONE lane dispatches ignored tests -- make heavy (Makefile:987-990, --run-ignored only, two invocations covering the workspace) -- and it is MANUAL-ONLY; no CI workflow runs any ignored test. Classification: 79 GPU/hardware-gated (self-skipping without an adapter; legitimate), 68 heavy-research-lane, 1 known-limitation (harper_chern.rs:617, fix-or-delete), 3 GPU-overlap not-implemented (boxkites Monte Carlo x2, besag_clifford_cuda.rs:478 -- dead even with hardware), 28 bare-attribute (27 with runtime comments, 1 truly bare). Five tests are PROVABLY DEAD: documented runtimes (boxkites/tests.rs:6916 ~17h, :6904 ~2h, :6954 ~60m, :6239 ~35-40m; cd_tower_violations.rs:801 ~9m) exceed the heavy lane's terminate budgets (20 min heavy-math, 6 min elsewhere), so the only lane that dispatches them kills them; the non-heavy-math invocation also compiles UNOPTIMIZED, worsening every runtime comment. allow_clippy_unjustified holds at 0 in crates/, but that gate's population excludes allow(dead_code): 282 item-level #[allow] attrs show ~40% adjacent-rationale coverage on a 20-site sample (~170 bare, dead_code-dominant). Anchored todo_fixme_xxx_hack = 0 counts only non-comment tokens; the comment-resident actionable census is ~26 TODO/FIXME (registry-mirror data excluded) plus 4 in lbm_3d_cuda .cu kernels outside the .rs-only scan (see DEBT-MEASUREMENT-VALIDITY). 0 unimplemented!/todo! macros; 41 unreachable! (exhaustiveness pins, informational). Test-surface reproducibility is clean: 0 zero-test crates, 0 sleeps/network/absolute-path hazards inside test code."
magnitude_reproduce = "make repo-audit  # ignore_attrs, allow_clippy_attrs, todo_fixme_xxx_hack, *_macro_calls"
confirmed_examples = [
    "131 #[ignore] tests (GPU/network gating; exercised via opt-in --ignored lane).",
    "gate-audit historically omitted CPD/semver/deny/docs-freshness (now composed in make audit-deep).",
]
resolution_pattern = "Add crate-scoped smoke/parity hooks first, compose into opt-in deeper gates; keep allow_clippy_unjustified at 0; audit #[ignore] tests against the --ignored opt-in run to confirm each is exercised somewhere."
falsifiable_acceptance = "make repo-audit-strict-unjustified passes with per-root cap 0; every #[ignore] test is either run in the --ignored lane or carries a one-line reason on the attribute."
status = "in_progress" # strict-unjustified gate passes; ignored-test audit remains
predecessor_ids = [
    "DEBT-TEST-VERIFICATION",
    "DEBT-TEST-1",
    "DEBT-TEST-2",
    "DEBT-PLACEHOLDER-1",
    "DEBT-PLACEHOLDER-2",
]
cross_links = [
    "docs/engineering/repo_audit_metric_taxonomy.md",
    "docs/engineering/clippy_allow_inventory_2026_05_09.md",
]

[[debt_class]]
id = "DEBT-FORMAL-VERIFICATION"
axis = "C_test_verification"
label = "Rocq proof completeness (admits, axioms, parameters)"
risk = "Admitted proofs, top-level axioms, and parameters that should be theorems weaken the formal-verification guarantee. A high axiom count can hide an inconsistent axiomatization (the Moreno Ker L_a case found an outright inconsistent H-module axiom)."
current_magnitude = "297 .v files on disk (296 tracked; QuatRotationLaws.v is an in-flight commit-candidate with its _RocqProject hunk). 2026-07-09 anchored audit: 0 strict Admitted/admit; 31 top-level + 84 indented Axiom = 115; 8 + 49 Parameter = 57; 172 total proof gaps. FINDINGS 2026-07-09, remediated same day: (a) BUILD-MANIFEST ORPHANS -- _RocqProject listed 286 of 297 on-disk files; the 9 substantive orphans are now registered (295 total; the 2 extraction/ orphans stay intentional), after fixing the rot the registration exposed: five orphans still imported the retired CDProofs.* namespace (renamed to OpenGororoba), and CDFidelity.v (relocated from the unmapped proofs root into theories/) failed its first-ever body check -- a broken lra branch AND an inconsistent free axiom (wht_preserves_associator quantified before/after norms independently, proving any two reals equal -- the C1543 failure pattern again). Remediation: two vacuous exists-form axioms converted to proved lemmas (witness 0), the WHT axiom restated soundly over a wht_assoc_image function symbol, the negative Rcase_abs branch closed by contradiction. (b) PLACEHOLDER-DATA TABLE -- C1467_XORSignCocycle.v shipped 9 of 16 sign-table rows marked PLACEHOLDER (and the unmarked rows were wrong too); the table is regenerated from the in-file cd_sign_fuel recurrence and bound to it by the new sed_sign_table_correct vm_compute theorem. (c) Free (non-Module-Type) axioms concentrate in: SStructuralGaps.v (deliberate gap registry), CarlsonIntegrals.v, TightBindingTopology.v, C958_ZDGraphTopology.v, PathionZDGraph.v, CDFidelity.v (1 sound axiom post-remediation); the interface files Sedenion.v/OctonionNorm.v/CayleyDicksonAlgebra.v carry zero axioms."
magnitude_reproduce = "make repo-audit  # rocq_admitted_strict, rocq_axiom_strict/_indented, rocq_parameter_strict/_indented"
confirmed_examples = [
    "data/output/audit/2026-05-09/axiom_dispositions.toml (1773 lines classifying every axiom).",
    "Resolved: inconsistent C1543 H-module axiom dropped (commit 9492771f); non-alternative witnesses added.",
]
resolution_pattern = "Hold strict Admitted/admit at zero; for each top-level Axiom record a disposition (necessary external theorem vs provable-but-deferred); convert interface-obligation indented axioms to derived lemmas where feasible; never ship an axiom that introduces inconsistency (test with a derived False-witness check)."
falsifiable_acceptance = "make -C proofs vos && make -C proofs vok both succeed; repo-audit rocq_admitted_strict == 0 && rocq_admit_strict == 0; every top-level Axiom has a disposition row in axiom_dispositions.toml; the diff between on-disk non-extraction .v files and _RocqProject entries is empty; C1467_XORSignCocycle.v carries zero PLACEHOLDER rows or moves out of verified/."
status = "controlled"
predecessor_ids = ["DEBT-FORMAL-VERIFICATION", "DEBT-PROOF-1", "DEBT-PROOF-2", "DEBT-CLAIMS-PROOF"]
cross_links = ["docs/engineering/formal_proof_field_schema_2026_05_09.md"]

[[debt_class]]
id = "DEBT-PROOF-EXTRACTION-FIDELITY"
axis = "C_test_verification"
label = "Binding between Rocq-proven models and shipping Rust code"
risk = "A proof can be internally complete yet prove a property of an idealized model that the Rust executable does not actually implement. Without a binding, the formal-verification badge overclaims: the proof constrains a definition, not the kernel that runs. This is the single highest research-integrity risk for arXiv-grade claims."
current_magnitude = "The repo proves theorems about Cayley-Dickson structures (Moreno 1997, Brown 1972) and ships Rust kernels (cd_kernel) computing in those algebras. The first explicit proof-to-executable binding now exists: crates/cd_kernel/tests/proof_extraction_fidelity.rs reimplements the Rocq CDDoubleFunctor::cd_mul / cd_conj recurrence as an independent oracle and asserts all three production multiply paths (recursive cd_multiply, SIMD cd_multiply_simd, sign-table multiply_via_table) match it on seeded random inputs at dims 8..256, plus the octonion composition identity at dim 8. Remaining: most of the other 144-of-1441 proof-backed claims still bind by naming convention, not by a parity test."
magnitude_reproduce = "grep -c 'formal_proof' registry/claims.toml ; ls proofs/extraction 2>/dev/null ; rg 'Brown1972|Moreno1997' crates/cd_kernel/src | wc -l"
confirmed_examples = [
    "cd_kernel Rust multiplication vs Rocq sed_mul / oct_mul definitions (parity asserted by naming, not by a generated oracle).",
    "FidelityCD / tower-instantiation proofs referenced in scattered DEBT-PROOF tags.",
]
resolution_pattern = "For each safety-critical algebraic identity, add a property-based Rust test (proptest) whose oracle is the same recurrence the Rocq proof uses, so the executable is checked against the proven recurrence on random inputs; record the theorem-id <-> Rust-function binding in the claim registry's formal_proof field."
falsifiable_acceptance = "cargo nextest run -p cd_kernel --test proof_extraction_fidelity passes (recurrence-binding landed); and each claim with formal_proof_present names both a Rocq theorem id AND a Rust test that exercises the corresponding executable path."
status = "in_progress" # first binding landed: cd_kernel multiply <-> CDDoubleFunctor::cd_mul
predecessor_ids = []
cross_links = [
    "DEBT-FORMAL-VERIFICATION",
    "DEBT-SCIENTIFIC-EVIDENCE",
    "crates/cd_kernel/tests/proof_extraction_fidelity.rs",
]

[[debt_class]]
id = "DEBT-OBSERVABILITY"
axis = "C_test_verification"
label = "Gate-timing telemetry and audit reproducibility instrumentation"
risk = "Without timing telemetry and regression checks, gate slowdowns and audit-number drift go unnoticed until they block a push. A regression check wired with continue-on-error provides no protection."
current_magnitude = "gate-timing-summary / gate-timing-regression-check exist in xtask and emit JSONL under data/output/audit/<date>/gate-timing-*.jsonl. The checker filters history by the gate-timing-<unix-ts>.jsonl artifact timestamp, not checkout mtime. The CI gate-timing-regression-check is now a hard gate: .github/workflows/ci.yml carries the HARD GATE comment and no continue-on-error on the timing step."
magnitude_reproduce = "rg -n 'continue-on-error|timing' .github/workflows/ci.yml ; ls data/output/audit/*/gate-timing-*.jsonl"
confirmed_examples = [
    ".github/workflows/ci.yml gate-timing-regression-check hard gate (soak window ended 2026-05-26).",
    "xtask gate-timing-summary, gate-tools-status.",
]
resolution_pattern = "Keep gate-timing-regression-check as a hard gate and keep the JSONL telemetry as the durable evidence surface."
falsifiable_acceptance = "awk '/name: Gate-timing regression check/{inside=1; next} inside && /^      - name: /{inside=0} inside && /continue-on-error/{found=1} END{exit found}' .github/workflows/ci.yml; a deliberately-slowed gate fails CI."
status = "controlled"
predecessor_ids = []
cross_links = [
    "plans/post_gate_optimization_2026_05_12.toml task 122-followup",
    "docs/engineering/pre_push_gate_optimization.md",
]

[[debt_class]]
id = "DEBT-MEASUREMENT-VALIDITY"
axis = "C_test_verification"
label = "Audit-instrument population validity (meta-measurement)"
risk = "An audit metric that scans the wrong population reports confident numbers about nothing: debt work gets aimed at phantom surfaces, real surfaces stay invisible, and baseline deltas flip sign with environmental state (build caches) rather than code changes. The repo's own metric-taxonomy doc warns about comment-vs-code grep artifacts; the flagship debt baselines carried instrument artifacts of the same species."
current_magnitude = "Five instances confirmed 2026-07-09. (1) CACHE CONTAMINATION -- RESOLVED same day: repo_audit.rs now prunes .cache/ and target/ under every root (EXCLUDED_DIR_NAMES) and declares the exclusion set in [meta]; with the cache present the fixed walker reports by_root.proofs 0/0/0, matching the cache-pruned 2026-06-25 row, vs 130 rust_files / 3420 unsafe_blocks / 64 allow_clippy_unjustified from the pre-fix walker -- git history contains no .rs ever tracked under proofs/. Every prior '3420 proofs scaffolding' figure was this artifact. (2) COMPLEMENT-POPULATION METRIC -- RESOLVED same day: the new todo_fixme_comment_resident shadow metric (raw-minus-stripped count, registry_mirrors excluded) reports 47 comment-resident markers alongside the anchored 0. (3) EXTENSION BLINDNESS -- RESOLVED same day: .cu/.wgsl files are scanned into kernel_files (76) and kernel_marker_count (4). Both new fields carry serde defaults so pre-existing baselines parse; both register as regression classes in the strict delta. (4) UNANCHORED COUNTERS -- OPEN: project.toml proof_files=186 equals no measurable population; binary_count=485 vs 484 in both compat TOML and canonical SQLite; resync via the SQLite lane remains. (5) GATE-SCOPE GAP -- OPEN: allow_clippy_unjustified=0 polices clippy allows only; ~170 bare allow(dead_code) sit outside every gate. (6) STALE GATE BINARY -- FOUND 2026-07-10: governance-gate-readonly consumes cached gate-tools binaries, so the scanner hardening landed in source (fix(governance) commits) while the cached pre-hardening binary kept passing; a cache refresh exposed 138 pre-existing UNREGISTERED markdown files. The gate binary cache needs a source-hash invalidation key so the gate that runs is always the gate that is committed."
magnitude_reproduce = "grep -A4 'by_root.proofs' data/output/audit/2026-07-09/repo_audit_anchored_2026_07_09_cache_contaminated.toml data/output/audit/2026-07-09/repo_audit_anchored_2026_07_10.toml  # 3420 -> 0 unsafe across the walker fix, cache present in both; git log --all --name-only -- 'proofs/**/*.rs' | grep -c rs  # 0"
confirmed_examples = [
    "Fixture pair: repo_audit_anchored_2026_07_09_cache_contaminated.toml by_root.proofs (pre-fix walker, cache present: 130/3420/64) vs repo_audit_anchored_2026_07_10.toml (excluding walker, cache present: 0/0/0); the 2026-06-25 snapshot (cache pruned) matches the fixed row.",
    "crates/lbm_3d_cuda/src/kernels_dark_halo.cu lines 15-18: four real TODO/FIXME invisible to the .rs-only marker scan.",
    "registry/project.toml proof_files=186 (disk 297 / tracked 296 / _RocqProject 286 / theories 132 / SQLite theorems 131).",
]
resolution_pattern = "Every audit metric declares its scanned population (roots, file extensions, exclusion globs) in the emitted TOML; repo-audit excludes .cache/ and target/ under every root; comment-resident markers get their own shadow metric instead of being stripped into vacuity; kernel-source extensions (.cu, .wgsl) join the marker scan; every project.toml counter names its reproduction command; strict-gate comparisons pin the exclusion set so cache state cannot flip a delta."
falsifiable_acceptance = "Two consecutive repo-audit runs, one with proofs/.cache present and one with it pruned, emit identical by_root.proofs numbers (satisfied by the fixture pair); the emitted [meta] block lists exclusion globs (satisfied); a comment-resident TODO/FIXME shadow metric row exists; every project.toml counter reproduces via its named command."
status = "in_progress" # instance 1 (cache contamination) resolved; shadow metric, extension scan, allow-audit widening, counter resync remain
predecessor_ids = ["DEBT-UNSAFE-PROOFS", "DEBT-UNSAFE-PROOFS-SCAFFOLDING"]
cross_links = [
    "DEBT-OBSERVABILITY",
    "DEBT-MEMORY-SAFETY",
    "DEBT-DATA-PROVENANCE",
    "docs/engineering/repo_audit_metric_taxonomy.md",
]

# ----------------------------------------------------------------------------
# AXIS D -- DATA AND PROVENANCE DEBT
# ----------------------------------------------------------------------------

[[debt_class]]
id = "DEBT-DATA-PROVENANCE"
axis = "D_data_provenance"
label = "External-source provenance and network isolation"
risk = "Network access, cache mutation, and source provenance can leak into pure scientific computation lanes; registry counters can drift from their canonical SQLite source."
current_magnitude = "270 provenance records; 114180 external files under data/external (gitignored except README). Registry counter state 2026-07-09: compat binaries.toml and canonical SQLite binaries_cp now AGREE at 484 (the historical 452-vs-485 compat gap has closed), but registry/project.toml still declares binary_count=485 (off-by-one) and proof_files=186, a figure matching NO measurable population (disk 297, tracked 296, _RocqProject 286, theories/ 132; SQLite theorems table 131). Both counters need the canonical resync lane; see DEBT-MEASUREMENT-VALIDITY. Claims/experiments/insights counters are fully coherent across declared/compat/SQLite (1441/227/182). data_core default features should remain network-free."
magnitude_reproduce = "grep binary_count registry/project.toml ; grep -c '^\\[\\[binary\\]\\]' registry/binaries.toml ; cargo tree -p data_core --no-default-features"
confirmed_examples = [
    "data/external/ provenance hashing (270 records).",
    "Registry counter gap 485 vs 452 (documented-by-design compat filtering).",
]
resolution_pattern = "Keep default network features empty; route fetches through explicit provider binaries; hash and index external artifacts; mutate registry counters only via gororoba-db -> provenance export-control-plane -> registry-integrity, never by hand-editing the AUTO-GENERATED TOMLs."
falsifiable_acceptance = "cargo check -p data_core --no-default-features succeeds; make governance-gate-readonly passes (schema_signatures match); no registry counter is hand-edited (git blame shows export-tool authorship)."
status = "controlled"
predecessor_ids = ["DEBT-DATA-PROVENANCE", "DEBT-EXPERIMENT-1"]
cross_links = ["roadmap_2026_04_11 PH-2", "plans/post_gate_optimization_2026_05_12.toml task #129"]

[[debt_class]]
id = "DEBT-DATA-TRANSCRIPTION"
axis = "D_data_provenance"
label = "Human-transcribed scientific tables and codegen-from-source"
risk = "Large literature-transcribed constant tables (optical dielectric functions, crystallographic space groups) are hand-keyed from books and papers. A transcription error is a silent physics bug, and the giant .rs tables defeat review while NOT being machine-generated (so the generated-artifact quarantine does not apply)."
current_magnitude = "optical_database.rs 7638 lines transcribed from Palik (1998) and Klimchitskaya et al. (2009); crystal_symmetry.rs 4599 lines transcribed from the International Tables for Crystallography and Burns & Glazer (1990). Both carry explicit TRANSCRIBED-DATA headers and #![allow(clippy::all)]."
magnitude_reproduce = "rg -l 'TRANSCRIBED-DATA' crates ; wc -l crates/materials_core/src/optical_database.rs crates/materials_core/src/crystal_symmetry.rs"
confirmed_examples = [
    "crates/materials_core/src/optical_database.rs (Palik 1998, Klimchitskaya 2009).",
    "crates/materials_core/src/crystal_symmetry.rs (International Tables, Burns & Glazer 1990).",
]
resolution_pattern = "Move the transcribed data into a checked-in TOML/JSON source with explicit per-row citation (page/table), generate the Rust table via build.rs from that source, and add a spot-check test against a handful of independently-cited reference values so a transcription error fails the build. This converts DEBT-DATA-TRANSCRIPTION into a controlled DEBT-GENERATED-ARTIFACT surface."
falsifiable_acceptance = "optical_database.rs and crystal_symmetry.rs are generated by build.rs from a cited TOML/JSON source; a reference-value spot-check test (e.g. gold n,k at known wavelengths; known space-group multiplicities) passes; the hand-keyed .rs no longer appears in the monolith inventory."
status = "alternate_approach"
predecessor_ids = ["PH-MOD-ALTERNATE-PATH"]
cross_links = [
    "plans/post_gate_optimization_2026_05_12.toml task #127",
    "data/output/audit/2026-05-13/optical_database_metadata_gaps.toml",
]

# ----------------------------------------------------------------------------
# AXIS E -- SCIENTIFIC AND METHODOLOGY DEBT (domain-aligned)
# ----------------------------------------------------------------------------

[[debt_class]]
id = "DEBT-NUMERICAL-ALGORITHM"
axis = "E_scientific_methodology"
label = "Solver/algorithm consistency and tolerances"
risk = "Divergent optimizers, tolerances, and floating-point edge behavior can produce inconsistent scientific conclusions across modules that ought to share one numerical method."
current_magnitude = "Qualitative; primary historical instance (bounded_nelder_mead duplicated across cosmology bounce and observational fitting) resolved by hoisting to a shared module. Ongoing: ensure every fitted-parameter pathway uses the shared optimizer with explicit tolerance config."
magnitude_reproduce = "rg -n 'nelder_mead|fn .*optimi[sz]e|tolerance' crates --type rust | rg -v test | wc -l"
confirmed_examples = [
    "Shared bounded_nelder_mead module (post-hoist).",
    "Gauss-Legendre quadrature usage standardized via gauss-quad 0.2.4.",
]
resolution_pattern = "Hoist solvers into shared domain-owned modules, parameterize with closures/configs, preserve parity tests around fitted outputs, and forbid per-module re-implementations."
falsifiable_acceptance = "CPD reports no duplicated optimizer/solver token cluster across crates; each fit call site references the shared optimizer module."
status = "controlled"
predecessor_ids = ["DEBT-NUMERICAL-ALGORITHM"]
cross_links = ["DEBT-DUPLICATION", "roadmap_2026_04_11 PH-1"]

[[debt_class]]
id = "DEBT-NUMERICAL-REPRODUCIBILITY"
axis = "E_scientific_methodology"
label = "Determinism, seed control, and cross-backend bitwise parity"
risk = "A scientific result that does not reproduce bit-for-bit (or within a stated tolerance) on re-run or on a different backend is not research-grade. Nondeterministic time-seeding, unseeded RNG, and silent f32-GPU vs f64-CPU divergence undermine the source-of-data claim."
current_magnitude = "36 entropy-seeded RNG sites (rand::rng() / ThreadRng) across 22 crates/ files seed computations nondeterministically. NOTE: rand 0.10 renamed thread_rng() to rand::rng(), so a thread_rng/from_entropy grep is a false-negative detector (it reports 0 while the nondeterminism persists under the new name) -- detect with 'rand::rng\\(\\)|ThreadRng'. Also 241 Instant::now() call sites (telemetry is fine, but must never seed a computation). GPU kernels run FP32 while CPU reference runs FP64, with an empirically-characterized ~1e-4 relative drift per D3Q19 step at low Mach, documented but not universally asserted by a parity test. The seeded-ChaCha discipline is exemplified in crates/cd_kernel/tests/proof_extraction_fidelity.rs."
magnitude_reproduce = "rg -c 'rand::rng\\(\\)|ThreadRng|thread_rng|from_entropy' crates --type rust | awk -F: '{s+=$2} END{print s}' ; rg -c 'Instant::now\\(\\)' crates --type rust | awk -F: '{s+=$2} END{print s}'"
confirmed_examples = [
    "36 rand::rng()/ThreadRng sites in 22 files (gororoba_algebra species_audit, lattice_filtration zero_divisor_census, cosmology_core mass_ladder, many gororoba_cli_physics bins).",
    "241 Instant::now() sites (telemetry vs computation must be separable).",
    "GPU FP32 vs CPU FP64 LBM drift (~1e-4 rel/step; documented in cd_kernel/lbm comments).",
]
resolution_pattern = "Seed every stochastic pathway from an explicit u64 (rand::rng()/ThreadRng forbidden in a computation; expose a --seed CLI flag defaulting to a fixed value so a run is reproducible yet a sweep can vary the seed); keep Instant::now strictly for telemetry; add cross-backend parity tests asserting GPU-vs-CPU agreement within a stated tolerance recorded as a claim. The 36-site conversion is a careful per-site sweep (a blind global hardcoded seed would break Monte-Carlo null-test semantics), not a single mechanical edit."
falsifiable_acceptance = "rg 'rand::rng\\(\\)|ThreadRng|thread_rng|from_entropy' crates --type rust outside tests yields zero; a GPU-vs-CPU parity test asserts a bounded relative error at fixed seed for at least one LBM and one cd_kernel pathway."
status = "open" # detector corrected; 36-site seed-parameterization sweep remaining
predecessor_ids = []
cross_links = ["DEBT-SCIENTIFIC-EVIDENCE", "DEBT-PROOF-EXTRACTION-FIDELITY"]

[[debt_class]]
id = "DEBT-DIMENSIONAL-UNITS"
axis = "E_scientific_methodology"
label = "Physical-unit correctness and non-dimensionalization conventions"
risk = "Mixing physical units or conflating a quantity with its complement is a classic silent physics bug. In a workspace spanning cosmology (Mpc, redshift), heliosphere (AU), materials (eV, nm), and LBM (lattice units), an undeclared unit or an inverted convention corrupts results without a compiler error."
current_magnitude = "The specific void-fraction notation hazard is RESOLVED: generate_topological_voids.rs uses void_fraction = void_count/total_cells for the exclusion fraction and baryon_filling_fraction = 1.0 - void_fraction for the CDG-2 complement. The print site labels both quantities, and assert!((0.0..=1.0).contains(&void_fraction)) enforces the range invariant in every build profile. Broader debt remains: cross-domain unit conventions (Mpc, AU, eV, lattice units) are documented by comment, not enforced by a newtype, so a unit mix-up is not a compile error."
magnitude_reproduce = "rg -n 'void_fraction|filling_fraction' crates --type rust ; rg -c 'AU|Mpc|redshift|lattice_units|eV' crates --type rust | awk -F: '{s+=$2} END{print s}'"
confirmed_examples = [
    "LBM void-fraction notation hazard resolved in generate_topological_voids.rs (explicit void_fraction and baryon_filling_fraction names + labeled prints + range assert).",
    "Cross-domain unit conventions still documented by comment, not by newtype.",
]
resolution_pattern = "Where a quantity and its complement coexist, encode the convention in the name (filling_fraction vs void_fraction) and ideally a newtype; document the unit system per module header; add assertions on physical ranges (0<=fraction<=1, redshift>=0) at lane boundaries."
falsifiable_acceptance = "Each physics crate header declares its unit system; boundary assertions catch out-of-range physical quantities; no variable conflates a quantity with its complement (void_fraction/baryon_filling_fraction instance closed)."
status = "in_progress" # named fraction instance resolved; cross-domain unit type-safety remains
predecessor_ids = []
cross_links = ["MEMORY notation bug B-C1", "DEBT-NUMERICAL-REPRODUCIBILITY"]

[[debt_class]]
id = "DEBT-SCIENTIFIC-EVIDENCE"
axis = "E_scientific_methodology"
label = "Claim-to-evidence linkage and reproducible verifiers"
risk = "Mathematical, materials, cosmology, algebra, and physics claims can outpace reproducible evidence links; an experiment registered without a binary cannot be re-run; a claim without an offline-checkable verifier is an assertion, not a result."
current_magnitude = "1441 claims (1201+ verified, 121 provisional, 57 refuted, plus closed/falsified categories); 227 experiments with 33 flagged binary_registered=false; 182 insights; 144 claims carry formal_proof_present. The verified:provisional ratio is healthy; the 33 binary-less experiments are the actionable surface (DEBT-EXPERIMENT-1)."
magnitude_reproduce = "grep -c '^\\[\\[claim\\]\\]' registry/claims.toml ; rg -c 'binary_registered = false' registry/experiments.toml"
confirmed_examples = [
    "33 experiments with binary_registered=false (docs/engineering/experiments_missing_binary_disposition_2026_05_09.md).",
    "MaNGA null result chain E-183/E-192/E-201/E-202 with offline CSV evidence.",
]
resolution_pattern = "Convert claims into hypothesis records with data/source/code evidence; require each experiment to register a runnable binary or a disposition explaining why none exists; add offline verifiers (no live network) for cosmology, materials, algebra, spectral outputs."
falsifiable_acceptance = "Every experiment with binary_registered=false has a disposition row; each Verified claim links a data path + a code path that re-derives it offline; make integrity passes."
status = "in_progress"
predecessor_ids = ["DEBT-SCIENTIFIC-EVIDENCE", "DEBT-EXPERIMENT-1"]
cross_links = ["roadmap_2026_04_11 PH-6", "DEBT-METHODOLOGY"]

[[debt_class]]
id = "DEBT-METHODOLOGY"
axis = "E_scientific_methodology"
label = "Experimental-design and statistical-methodology rigor"
risk = "A pipeline can produce a confident number that is a methodological artifact rather than a physical signal: sparse-bin denominator inflation, undetected multiple-comparison inflation, a sensitivity pipeline blind to injected signal, or a null result mistaken for a non-result. For arXiv-grade work this is the difference between a result and a retraction."
current_magnitude = "Qualitative, with a worked precedent: the MaNGA Q3 SNR=3.60 (C-1408) was confirmed to be a SPARSE-BIN ARTIFACT, and a blind-injection test (E-202) proved the pipeline was blind at 6 bins. This establishes the methodology bar; the debt is making blind-injection and artifact-control standard for every signal-detection claim, not retrofit after the fact."
magnitude_reproduce = "rg -n 'blind|injection|null|artifact|multiple comparison|look-elsewhere' registry/insights.toml registry/claims.toml | wc -l"
confirmed_examples = [
    "MaNGA Q3 SNR artifact -> blind-injection falsification (C-1408, C-1432, E-202).",
    "E-197 miscalibrated, correctly flagged as unusable for sensitivity validation (C-1409).",
]
resolution_pattern = "For every signal-detection claim: pre-state the test and bin scheme; run a blind signal-injection to measure recovery; correct for multiple comparisons / look-elsewhere; distinguish a measured null (with quantified sensitivity) from an absence of analysis. Record the methodology as an insight."
falsifiable_acceptance = "Each Verified signal-detection claim cites a blind-injection recovery measurement and a multiple-comparison treatment; each null result states the sensitivity at which it is null."
status = "open"
predecessor_ids = []
cross_links = ["MaNGA E-183/E-192/E-201/E-202", "DEBT-SCIENTIFIC-EVIDENCE"]

# ----------------------------------------------------------------------------
# AXIS F -- DOCUMENTATION AND ORGANIZATIONAL DEBT
# ----------------------------------------------------------------------------

[[debt_class]]
id = "DEBT-DOCUMENTATION-REQUIREMENTS"
axis = "F_documentation_organizational"
label = "Registry-driven docs freshness and install/verify coverage"
risk = "Generated docs drift from their registry sources and omit operational install or verification commands; a hand-edited generated doc desyncs silently."
current_magnitude = "docs/REQUIREMENTS.md is AUTO-GENERATED from registry/requirements_narrative.toml (authoritative=true, 13 documents). docs-freshness strict-rustdoc false positives were reduced to the docs-site redirect checker after mathematical bracket notation, generic types, and invalid Rust example fences were normalized. rust-semver-check no longer dies on cargo-semver-checks baseline checkout filename-component length or /tmp quota: the lane now extracts v1.0-methods with git archive into .cache/semver-baselines, uses repo-local semver cache/TMPDIR paths, excludes crates introduced after v1.0-methods, and reaches real public-API compatibility findings."
magnitude_reproduce = "make docs-freshness ; make rust-semver-check"
confirmed_examples = [
    "docs-freshness strict rustdoc blockers in cd_kernel, gr_core, gororoba_cli_physics, grmhd_core, qgp_scaling, gororoba_gpu_cuda, cr_transport, and verified_core.",
    "rust-semver-check reaches real API findings: removed modules/functions in algebra_experimental, data_core, and gororoba_algebra; removed cudarc features in gororoba_algebra, quantum_core, sign_imbalance, and stats_core; constructible struct field additions in provenance_core and provenance_store; constructor or method arity changes in provenance_store and snia_core; enum discriminant/order changes in gororoba_gpu_bridge; auto-trait changes in neural_homotopy and gr_core.",
]
resolution_pattern = "Edit registry narrative/requirements SOURCE then regenerate markdown (never hand-edit the AUTO-GENERATED docs); keep Rust docs strict-clean under make docs-freshness; make rust-semver-check green by restoring public compatibility shims for accidental breakage or deliberately advancing SEMVER_BASELINE_REV after an intentional public-API break is documented."
falsifiable_acceptance = "make docs-freshness and make rust-semver-check both exit zero; every audit tool in REQUIREMENTS.md has a working make target and install command."
status = "in_progress"
predecessor_ids = ["DEBT-DOCUMENTATION-REQUIREMENTS"]
cross_links = ["registry/requirements_narrative.toml", "roadmap_2026_04_11 PH-5"]

[[debt_class]]
id = "DEBT-ORGANIZATIONAL"
axis = "F_documentation_organizational"
label = "Process, coordination, and lifecycle discipline"
risk = "Plan proliferation, stale branches, undisciplined multi-agent concurrent commits, and ad-hoc registry mutation erode the repeatability the project depends on. Process debt does not show up in a linter but compounds in every future session."
current_magnitude = "2 active plans + 10 archived (plan proliferation managed via plans/archive/); 1 local branch, 1 worktree (clean); registry mutation has a mandatory SQLite-canonical workflow; multi-agent commits require a git-log check before push (background agents commit concurrently)."
magnitude_reproduce = "ls plans/*.toml | wc -l ; git branch | wc -l ; git worktree list"
confirmed_examples = [
    "plans/archive/ holds 10 retired plans (lifecycle discipline working).",
    "Branch-lifecycle ADR (docs/adr/branch-lifecycle.md) governs worktree-agent cleanup.",
    "Registry mutation workflow: gororoba-db -> provenance export -> registry-integrity (AGENTS.md).",
]
resolution_pattern = "Archive superseded plans promptly; delete zero-unique-commit branches; always git log before push when agents run concurrently; mutate the registry only through the canonical CLI chain; keep one authoritative roadmap (this file) rather than parallel competing plans."
falsifiable_acceptance = "At most one authoritative active debt roadmap exists; no branch with zero unique commits persists; every registry-counter change in git history is authored by the export tool, not a hand edit."
status = "controlled"
predecessor_ids = []
cross_links = ["docs/adr/branch-lifecycle.md", "AGENTS.md registry mutation workflow"]

[[debt_class]]
id = "DEBT-TAXONOMY-COHERENCE"
axis = "F_documentation_organizational"
label = "Coherence of the debt taxonomy itself (meta-debt)"
risk = "When canonical debt CLASSES and ephemeral Stage-B work-item TAGS share the DEBT-* namespace, a reader cannot tell a durable category from a one-off task. ~24 distinct DEBT-* strings existed across plans/data/docs/source with no single reconciliation point; this fragmentation defeats navigation and invites double-counting."
current_magnitude = "Before this file: 24 distinct DEBT-* identifiers (12 canonical classes + ~12 work-item tags/placeholders) with no reconciliation map. After this file: 22 canonical classes in 6 axes + one [[identifier_reconciliation]] table mapping every legacy tag onto a class (DEBT-MEASUREMENT-VALIDITY added 2026-07-09 with the retired DEBT-UNSAFE-PROOFS/-SCAFFOLDING tags retargeted onto it)."
magnitude_reproduce = "rg -o 'DEBT-[A-Z0-9-]+' -g '!*.lock' -g '!.cache/**' -g '!target/**' . | sort -u | wc -l"
confirmed_examples = [
    "Placeholder tags DEBT-PLACEHOLDER-1/-2 carried no durable category.",
    "DEBT-UNSAFE-1 split into -CRATES / -PROOFS / -PROOFS-SCAFFOLDING across docs with no index.",
]
resolution_pattern = "Maintain exactly one authoritative taxonomy (this file). New work-item tags must reference a canonical class id; placeholders are forbidden. The [[identifier_reconciliation]] table is the single lookup from any legacy tag to its class."
falsifiable_acceptance = "Every DEBT-* string in the repo either is one of the 21 canonical class ids OR appears as a from= entry in [[identifier_reconciliation]]; no new DEBT-PLACEHOLDER-* is introduced."
status = "resolved_by_this_file"
predecessor_ids = ["DEBT-PLACEHOLDER-1", "DEBT-PLACEHOLDER-2", "DEBT-ROADMAP-2026-04-11"]
cross_links = ["[[identifier_reconciliation]] below"]

# ----------------------------------------------------------------------------
# Identifier reconciliation: every legacy DEBT-* tag -> canonical class.
# Resolves DEBT-TAXONOMY-COHERENCE. A reader encountering any legacy tag in
# an old commit, finding-doc, or source comment looks it up here.
# ----------------------------------------------------------------------------
[[identifier_reconciliation]]
from = "DEBT-PROOF-1"
to = "DEBT-FORMAL-VERIFICATION"
disposition = "Work-item tag for strict-admit closure. RESOLVED 2026-05-09 (0 strict admits; v0 count was a docstring false positive)."

[[identifier_reconciliation]]
from = "DEBT-PROOF-2"
to = "DEBT-FORMAL-VERIFICATION"
disposition = "Work-item tag for indented-axiom/parameter proof gaps (196 total at 2026-05-09; 172 at 2026-06-04 baseline). Tracked via axiom_dispositions.toml."

[[identifier_reconciliation]]
from = "DEBT-CLAIMS-PROOF"
to = "DEBT-FORMAL-VERIFICATION"
disposition = "Work-item tag for the formal_proof claim-field backfill heuristic (formal_proof_backfill.rs)."

[[identifier_reconciliation]]
from = "DEBT-UNSAFE-1"
to = "DEBT-MEMORY-SAFETY"
disposition = "Original combined unsafe lane; split into crates/ and proofs/ scaffolding sub-lanes."

[[identifier_reconciliation]]
from = "DEBT-UNSAFE-CRATES"
to = "DEBT-MEMORY-SAFETY"
disposition = "crates/ unsafe lane (392 blocks, 65 SAFETY comments). The actionable surface."

[[identifier_reconciliation]]
from = "DEBT-UNSAFE-PROOFS"
to = "DEBT-MEASUREMENT-VALIDITY"
disposition = "Retired 2026-07-09: the lane counted dependency build-script outputs under proofs/.cache/gate-target, not project code (no .rs ever git-tracked under proofs/)."

[[identifier_reconciliation]]
from = "DEBT-UNSAFE-PROOFS-SCAFFOLDING"
to = "DEBT-MEASUREMENT-VALIDITY"
disposition = "Same artifact as DEBT-UNSAFE-PROOFS: the 3420-block count is proofs/.cache contamination that flips to 0 when the cache is pruned. Retired 2026-07-09."

[[identifier_reconciliation]]
from = "DEBT-TEST-1"
to = "DEBT-TEST-VERIFICATION"
disposition = "Work-item tag for #[ignore] tests (109 in crates/, GPU/network gating)."

[[identifier_reconciliation]]
from = "DEBT-TEST-2"
to = "DEBT-TEST-VERIFICATION"
disposition = "Work-item tag for allow(clippy::*) annotations; unjustified count driven to 0 in crates/."

[[identifier_reconciliation]]
from = "DEBT-PLACEHOLDER-1"
to = "DEBT-TEST-VERIFICATION"
disposition = "Work-item tag for TODO/FIXME/XXX/HACK markers (3 in code context after de-noising; was a 47x overcount)."

[[identifier_reconciliation]]
from = "DEBT-PLACEHOLDER-2"
to = "DEBT-TEST-VERIFICATION"
disposition = "Work-item tag for unimplemented!/todo!/unreachable! macros (0/0/41; unreachable! is exhaustiveness pins, informational)."

[[identifier_reconciliation]]
from = "DEBT-EXPERIMENT-1"
to = "DEBT-SCIENTIFIC-EVIDENCE"
disposition = "Work-item tag for experiments with binary_registered=false (33)."

[[identifier_reconciliation]]
from = "DEBT-ROADMAP-2026-04-11"
to = "DEBT-TAXONOMY-COHERENCE"
disposition = "Self-reference meta-id of the superseded 2026-04-11 roadmap; preserved for git-history lookups."

[[identifier_reconciliation]]
from = "PH-MOD-ALTERNATE-PATH"
to = "DEBT-DATA-TRANSCRIPTION"
disposition = "post_gate tracker class for transcribed-data monoliths needing codegen-from-source (optical_database, crystal_symmetry)."

[[identifier_reconciliation]]
from = "CROSS-PLATFORM-COVERAGE"
to = "DEBT-TEST-VERIFICATION"
disposition = "post_gate tracker class for non-Linux backend coverage (cubecl launcher); deferred to a non-Linux sprint."

# ----------------------------------------------------------------------------
# FORWARD PHASES. Hypergranular, falsifiable, ordered by risk and dependency.
# Each task carries an acceptance_hook (a reproducible command) and a
# risk_control. Phases are not strictly sequential -- F0 and the closing
# hygiene phases run continuously; the lettered axis phases can interleave.
# ----------------------------------------------------------------------------

[[phase]]
id = "DBT-F0"
title = "Freeze a fresh measured baseline and adopt this taxonomy"
axis = "F_documentation_organizational"
horizon = "immediate"
goal = "Lock current numbers and make this file the single authoritative roadmap."
tasks = [
    "Emit data/output/debt_baseline_2026_06_04.toml via REPO_AUDIT_OUT=data/output/audit/2026-06-04 make repo-audit (anchored, SQLite-linked).",
    "Mark plans/repo_debt_roadmap_2026_04_11.toml superseded (header note) and keep post_gate tracker as task-history.",
    "Adopt the 21-class / 6-axis taxonomy; route any new DEBT-* tag through [[identifier_reconciliation]].",
]
acceptance_hooks = [
    "test -f data/output/debt_baseline_2026_06_04.toml",
    "rg -o 'DEBT-[A-Z0-9-]+' -g '!.cache/**' -g '!target/**' . | sort -u  # every entry resolvable here",
]
risk_controls = [
    "Do not delete predecessor plans; supersede in place so git history and cross-refs survive.",
]

[[phase]]
id = "DBT-INSTRUMENT"
title = "Audit-instrument validity: exclusion sets, shadow metrics, counter re-anchoring"
axis = "C_test_verification"
horizon = "immediate"
goal = "Every debt number measures a declared population; cache state cannot move a metric; every counter reproduces via a named command."
tasks = [
    "Exclude .cache/ and target/ under every scanned root in repo_audit.rs's directory walk; emit the exclusion globs in the output [meta] block.",
    "Prove the fix with a fixture pair: run repo-audit with proofs/.cache/gate-target present and pruned; assert identical by_root.proofs output; retain both snapshots.",
    "Retire the phantom proofs-scaffolding unsafe lane from remaining prose surfaces (this file done 2026-07-09; per-project MEMORY; docs/engineering/unsafe_safety_methodology_2026_05_09.md if it cites the 3420 figure).",
    "Add a comment-resident TODO/FIXME shadow metric (comment-preserving scan, registry_mirrors and meta-tooling excluded) alongside the stripped metric so both populations are visible.",
    "Extend the marker scan to kernel-source extensions (.cu, .wgsl) -- lbm_3d_cuda/src/kernels_dark_halo.cu carries 4 real markers today.",
    "Extend the allow-rationale audit to all #[allow] attributes (dead_code included) or record a scoping decision in the metric taxonomy doc; current bare-allow census ~170 of 282.",
    "Resync registry/project.toml binary_count to 484 and re-anchor proof_files to a named population (tracked .v under proofs/) via gororoba-db -> provenance export-control-plane -> make registry-integrity; guard the known complete_experiment_count zeroing side-effect and revert it after the run.",
    "After the exclusion fix lands, emit a clean anchored snapshot and advance strict_gate_baseline in [meta] above (the raw 2026-07-09 snapshot itself carries the contamination in [totals] and must not become the comparison baseline).",
]
acceptance_hooks = [
    "make repo-audit twice (proofs/.cache present, then pruned): by_root.proofs blocks are byte-identical",
    "grep -q 'exclu' data/output/audit/<latest>/repo_audit_anchored_*.toml  # exclusion set declared in [meta]",
    "grep proof_files registry/project.toml  # value reproduces via the command named beside it",
    "make repo-audit-strict  # green against the post-fix baseline",
]
risk_controls = [
    "Fix the instrument before re-baselining: a baseline cut from a contaminated snapshot institutionalizes the artifact.",
    "Counter resync goes through the SQLite-canonical lane only; never hand-edit the AUTO-GENERATED TOMLs.",
]

[[phase]]
id = "DBT-A"
title = "Code and structure: finish monolith split and SAFETY coverage"
axis = "A_code_structure"
horizon = "1_to_3_sprints"
goal = "No production-logic .rs over 4000 lines; crates/ unsafe blocks fully SAFETY-commented or exception-listed."
tasks = [
    "Decompose provenance_store/src/lib.rs (4220) below 2000 by extracting the remaining impl ProvenanceStore surface into inherent-impl files behind pub use.",
    "Relocate algebra_analysis/boxkites/tests.rs (7016) and algebra_experimental/emanation/tests.rs (4327) bulk via #[cfg(test)] mod sub-files; production code stays under threshold.",
    "Convert xtask/src/main.rs (3411) string dispatcher into a clap Subcommand enum across sibling modules.",
    "Add // SAFETY: comments to every crates/ unsafe block lacking one; produce a documented exceptions list for any that genuinely cannot carry one.",
    "Re-split provenance_store/src/lib.rs (regrew 4220 -> 4372) and add a per-file line-count regression check so a split does not silently regrow.",
    "Start the CLI bin/ hotspot class: registry_emit.rs 3779, human_acquire.rs 3707, governance_verify.rs 3586, execution_planning.rs 3434 -- split by subcommand/report surface with pub use stability.",
]
acceptance_hooks = [
    "find crates xtask -name '*.rs' -not -path '*/registry_mirrors/*' | xargs wc -l | awk '$1>=4000'  # only transcribed-data + exempt tests",
    "make repo-audit  # safety_comments >= unsafe_blocks for crates/, or exceptions documented",
    "cargo clippy --workspace --all-targets -- -D warnings",
]
risk_controls = [
    "Splits behaviour-preserving; no API renames in the same commit; each submodule re-exported via pub use.",
]

[[phase]]
id = "DBT-B"
title = "Build/toolchain/delivery: xtask consolidation and advisory currency"
axis = "B_build_toolchain_delivery"
horizon = "2_to_4_sprints"
goal = "Typed xtask owns policy parsing; toolchain pin reconciled; the RUSTSEC ignore list shrinks, not grows."
tasks = [
    "Update the stale nightly-2026-03-05 references in AGENTS.md, CLAUDE.md, and the per-project MEMORY to the authoritative nightly-2026-04-05 pin in rust-toolchain.toml (the requirements narrative already matches).",
    "Migrate Makefile policy-heavy parsing (cpd report, integrity, governance) into xtask subcommands with structured output; keep make as a thin front door.",
    "Keep each deny.toml RUSTSEC ignore annotated with the blocking dependency and a re-check trigger (already satisfied) and remove any whose upstream fix has landed (rustls-webpki cluster already removed).",
    "Harden the proofs/Makefile toolchain guard: verify the rocq binary's OCaml build matches the existing .vo tree (or that the switch is rocq-9.1.1) before compiling, and fail fast naming the opam env remedy when it does not.",
]
acceptance_hooks = [
    "test \"$(grep -rh nightly rust-toolchain.toml AGENTS.md CLAUDE.md registry/requirements_narrative.toml | grep -o 'nightly-[0-9-]*' | sort -u | wc -l)\" -eq 1",
    "make cargo-deny-check && test \"$(grep -cE '^[[:space:]]*\"RUSTSEC' deny.toml)\" -le 6",
    "cargo run -q -p xtask -- --help >/dev/null",
]
risk_controls = [
    "Keep exploratory/noisy tools report-only; do not hard-gate a tool until its false-positive inventory is documented.",
]

[[phase]]
id = "DBT-C"
title = "Test/verification: extraction fidelity, proof discipline, hard timing gate"
axis = "C_test_verification"
horizon = "2_to_5_sprints"
goal = "Proven theorems bind to executable code via property tests; gate-timing regression is a hard gate."
tasks = [
    "Bind cd_kernel multiply to the proven recurrence with a seeded property test whose oracle is transcribed from Rocq CDDoubleFunctor::cd_mul; pass at dims 8..256 (LANDED: crates/cd_kernel/tests/proof_extraction_fidelity.rs). Extend the pattern to the next safety-critical proven identity.",
    "Record the Rocq-theorem-id <-> Rust-function binding in the formal_proof field for each proof-backed claim.",
    "Hold strict Admitted/admit at 0; keep a disposition row for every top-level Axiom.",
    "Keep CI gate-timing-regression-check as a hard gate; it no longer carries continue-on-error after the soak window ending 2026-05-26.",
    "Register the 9 substantive _RocqProject orphans (CDFidelity.v, verified/C1474_FuelAdequacy.v, 7 theories/ files) in the build manifest and fix whatever breaks; add an orphan-diff check to the proofs gate so a new .v cannot silently skip compilation.",
    "Replace the 9 PLACEHOLDER sign-table rows in proofs/verified/C1467_XORSignCocycle.v with values derived from cd_sign_fuel (or the sign-table oracle in cd_kernel) and re-verify the cocycle reflection.",
    "Compile-verify and commit the in-flight proofs/theories/QuatRotationLaws.v with its _RocqProject registration hunk.",
    "Give the 5 provably-dead long-runners (boxkites/tests.rs:6916/:6904/:6954/:6239, cd_tower_violations.rs:801) an ultra-heavy nextest profile whose slow-timeout matches their multi-hour runtimes plus a manual make target, or downscale them to a tractable dimension; the heavy lane's terminate budget currently kills all five.",
    "Add a scheduled (nightly or weekly) CI job invoking make heavy so the 68 research-lane ignored tests stop rotting unexercised; add the reserved gpu-test target (cargo nextest run -P gpu --run-ignored only) on optional self-hosted hardware for the 79 GPU-gated tests.",
    "Fix or delete the known-limitation test harper_chern.rs:617 and the 3 not-implemented GPU placeholders (boxkites Monte Carlo x2, besag_clifford_cuda.rs:478); convert the 28 bare #[ignore] attributes to #[ignore = \"reason\"] so they route to the intended budget.",
]
acceptance_hooks = [
    "cargo nextest run -p cd_kernel  # recurrence parity proptest green",
    "make -C proofs vos && make -C proofs vok",
    "make repo-audit  # rocq_admitted_strict==0 && rocq_admit_strict==0",
    "awk '/name: Gate-timing regression check/{inside=1; next} inside && /^      - name: /{inside=0} inside && /continue-on-error/{found=1} END{exit found}' .github/workflows/ci.yml",
]
risk_controls = [
    "Proptest tolerance is exact for integer/rational identities and a stated epsilon for float paths; record the epsilon as a claim.",
]

[[phase]]
id = "DBT-D"
title = "Data/provenance: codegen transcribed tables from cited source"
axis = "D_data_provenance"
horizon = "3_to_5_sprints"
goal = "Transcribed physics tables are generated from a cited TOML/JSON source with reference-value spot checks."
tasks = [
    "Extract optical_database.rs data into a cited TOML/JSON (per-row Palik/Klimchitskaya page reference); generate the Rust table via build.rs.",
    "Do the same for crystal_symmetry.rs (International Tables / Burns & Glazer page references).",
    "Add a reference-value spot-check test (gold n,k at known wavelengths; known space-group multiplicities) so a transcription error fails the build.",
]
acceptance_hooks = [
    "test -f crates/materials_core/data/optical_constants.toml  # cited source",
    "cargo nextest run -p materials_core  # reference-value spot check green",
    "find crates -name '*.rs' -not -path '*/registry_mirrors/*' | xargs wc -l | awk '$2!=\"total\" && $1>=4000 && index($2,\"optical_database.rs\")==0 && index($2,\"crystal_symmetry.rs\")==0 {bad=1; print} END{exit bad}'",
]
risk_controls = [
    "Parity hash or spot-check must pass before deleting the hand-keyed .rs surface.",
]

[[phase]]
id = "DBT-E"
title = "Scientific/methodology: determinism, units, blind-injection discipline"
axis = "E_scientific_methodology"
horizon = "ongoing"
goal = "Every result reproduces at a fixed seed; units are unambiguous; signal claims carry blind-injection evidence."
tasks = [
    "Replace the 36 rand::rng()/ThreadRng computation sites (22 files) with explicit seeded RNG behind a --seed flag (detect with 'rand::rng\\(\\)|ThreadRng'; rand 0.10 renamed thread_rng to rand::rng); keep Instant::now for telemetry only.",
    "Add at least one GPU-vs-CPU parity test asserting bounded relative error at a fixed seed (LBM and cd_kernel).",
    "Declare unit systems in physics-crate headers and add 0..1 range assertions at fraction lane boundaries (the void_fraction vs baryon_filling_fraction notation hazard in generate_topological_voids.rs is already resolved with explicit names and an assert range invariant).",
    "For each signal-detection claim, attach a blind-injection recovery measurement and a multiple-comparison treatment; record as an insight.",
]
acceptance_hooks = [
    "rg 'rand::rng\\(\\)|ThreadRng|thread_rng|from_entropy' crates --type rust | rg -v test  # empty in computation paths",
    "cargo nextest run  # GPU-vs-CPU parity test green",
    "rg -n 'void_fraction|filling_fraction' crates --type rust  # unambiguous naming",
]
risk_controls = [
    "Online research may seed provenance, but tests and gates must never depend on live network access.",
]

[[phase]]
id = "DBT-F-CONT"
title = "Documentation/organizational: unblock docs gates, hold taxonomy coherence"
axis = "F_documentation_organizational"
horizon = "continuous"
goal = "docs-freshness and semver-check run green; exactly one authoritative roadmap; no orphan DEBT-* tags."
tasks = [
    "Keep docs-freshness green under strict rustdoc by code-spanning mathematical bracket notation, generic types, and non-Rust examples.",
    "Resolve rust-semver-check API findings by restoring compatibility shims or advancing SEMVER_BASELINE_REV after documenting intentional public-API breaks.",
    "Update registry/requirements_narrative.toml to reference this taxonomy and every audit lane; regenerate docs.",
    "Reject any new DEBT-PLACEHOLDER-* and any DEBT-* tag not resolvable via [[identifier_reconciliation]].",
]
acceptance_hooks = [
    "make docs-freshness && make rust-semver-check",
    "make registry-build registry-export-markdown  # docs regenerated from source",
    "bash -lc 'comm -23 <(rg --no-filename -o \"DEBT-[A-Z0-9-]+\" -g \"!*.lock\" -g \"!.cache/**\" -g \"!target/**\" . | sort -u) <(rg --no-filename -o \"DEBT-[A-Z0-9-]+\" plans/repo_debt_taxonomy_roadmap_2026_06_04.toml | sort -u) | tee /tmp/open_gororoba_orphan_debt_tags.txt; test ! -s /tmp/open_gororoba_orphan_debt_tags.txt'",
]
risk_controls = [
    "Never hand-edit AUTO-GENERATED docs; edit the registry narrative source then regenerate.",
]

# ----------------------------------------------------------------------------
# PR REVIEW RECONCILIATION (2026-07-24).
#
# WHY: The merged data-pipeline PRs (#107-#115) accumulated 26 substantive
# Codex/CodeRabbit review findings against already-landed code. A finding on a
# merged PR is invisible debt: it survives only in the PR thread, not the tree.
# This section triages every finding against HEAD, records its disposition, and
# gives each surviving item a falsifiable acceptance hook -- turning PR-thread
# breadcrumbs into tracked, greppable debt.
#
# WHAT: 20 findings closed (16 already resolved by post-merge follow-up commits;
# 4 resolved on branch audit-refresh-pr-comment-reconciliation-2026-07-23), and
# 6 tracked (2 evidence-contradicted, 4 output-changing "evolve" decisions).
#
# HOW: Verdicts were established by inspecting current source per finding; the
# audit refresh (data/output/debt_baseline_delta_2026_07_24.toml) independently
# surfaced F115-9 as the sole allow_clippy_unjustified regression, corroborating
# the CodeRabbit flag on the same line.
# ----------------------------------------------------------------------------
[pr_review_reconciliation_2026_07_24]
source_prs = [107, 108, 109, 110, 111, 112, 113, 115]
note_114 = "GitHub issue (document inventory/classification), not a PR; closed by #115."
findings_total = 26
findings_closed = 20
findings_tracked = 6
resolved_pre_existing = [
    "F112-1", "F113-1", "F113-2", "F113-3", "F113-4", "F113-5", "F113-6",
    "F113-7", "F113-8", "F113-9", "F113-10", "F113-11", "F113-12", "F113-13",
    "F115-1", "F115-2",
]
resolved_this_branch = ["F115-9", "F115-4", "F115-8", "F113-14"]

[[pr_finding_disposition]]
id = "F115-9"
canonical_class = "DEBT-TEST-VERIFICATION"
site = "crates/gororoba_cli_data/src/bin/document_mineru_replay.rs classify()"
disposition = "RESOLVED. Bare #[allow(clippy::too_many_lines)] gained a mechanism-first rationale; this is the same line the audit surfaced as the lone allow_clippy_unjustified regression."
falsifier = "make repo-audit-strict-unjustified exits 0"

[[pr_finding_disposition]]
id = "F115-4"
canonical_class = "DEBT-DATA-PROVENANCE"
site = "document_mineru_replay.rs match_contract()"
disposition = "RESOLVED. match_contract now returns the most-restrictive matching contract, so a permissive alias cannot unlock track_compact on bytes another alias marks restricted. Unit test match_contract_most_restrictive_wins_across_aliases."
falsifier = "cargo test -p gororoba_cli_data --bin document-mineru-replay match_contract_most_restrictive_wins_across_aliases"

[[pr_finding_disposition]]
id = "F115-8"
canonical_class = "DEBT-DATA-PROVENANCE"
site = "document_mineru_replay.rs corpus lane"
disposition = "RESOLVED. Corpus ids key on the path relative to the corpus root, not the bare file stem, so two files sharing a stem under different subdirs no longer collide and trip the inverse-identity guard."
falsifier = "cargo test -p gororoba_cli_data --bin document-mineru-replay"

[[pr_finding_disposition]]
id = "F113-14"
canonical_class = "DEBT-DATA-PROVENANCE"
site = "themis_fgm_cache_restore.rs verify pass"
disposition = "RESOLVED. The verifier now scans the cache for th<p>_fgm_*.csv files carrying no manifest entry and fails on any such unaudited extra. Pure predicate is_unlisted_cache_extra with unit test unlisted_probe_csv_flagged_as_extra."
falsifier = "cargo test -p gororoba_cli_data --bin themis-fgm-cache-restore unlisted_probe_csv_flagged_as_extra"

[[pr_finding_disposition]]
id = "F113-15"
canonical_class = "DEBT-DATA-PROVENANCE"
site = "data/external/SOURCES.toml SRC-THEMIS-* active source"
disposition = "TRACKED (premise contradicted). The finding claimed an active source with empty scientific_validator_refs fails data-governance-gate --enforce-origin, but 41 active sources in the committed file share empty validator refs, so the claimed consequence does not hold. Open policy question -- should active sources require a validator ref -- not a live gate failure."
falsifier = "make data-governance-gate  # currently green; a policy change that requires validator refs would flip it"

[[pr_finding_disposition]]
id = "F115-5"
canonical_class = "DEBT-DATA-PROVENANCE"
site = "document_mineru_replay.rs index_extractions()"
disposition = "TRACKED (evidence-contradicted the proposed fix). A hard bail on an absent extraction root was reverted: the test suite (valid_pdf_without_extraction_requires_mineru) encodes that an absent extraction root is a valid pre-extraction state, not misconfiguration. Fail-loud needs a design decision (opt-in --require-extractions flag or a warning), not a silent bail."
falsifier = "a --require-extractions run over a config with an absent extraction root exits nonzero, while the default run still tolerates it"

[[pr_finding_disposition]]
id = "F112-2"
canonical_class = "DEBT-SCIENTIFIC-EVIDENCE"
site = "themis_probe_crossing_day_catalog.rs cache sweep"
disposition = "TRACKED (evolve; output-changing). Cache-only days outside --start-date/--end-date are added as n_crossings=0 control candidates though crossings were parsed only inside the window. Filtering them changes the control-day catalog and downstream statistics, so it belongs in a reviewed evidence change, not an audit-refresh batch."
falsifier = "control_candidate_days counts only days within [start,end]; a matched benchmark re-run reproduces within stated tolerance"

[[pr_finding_disposition]]
id = "F115-3"
canonical_class = "DEBT-DATA-PROVENANCE"
site = "document_mineru_replay.rs ingest lane path resolution"
disposition = "TRACKED (needs config semantics). Relative manifest paths are checked in CWD, never joined against configured source roots; a --source-root corpus with relative manifest paths routes present sources as source_missing. Correct resolution order across multiple roots needs a decision before implementation."
falsifier = "a manifest with paths relative to a configured source root resolves present sources as found, not source_missing"

[[pr_finding_disposition]]
id = "F115-6"
canonical_class = "DEBT-DATA-PROVENANCE"
site = "document_mineru_replay.rs duplicate-id reconciliation"
disposition = "TRACKED (evolve; interacts with F115-1). Byte-identical manifest ids reconcile against the lexicographically first docpipe dir; a malformed first twin routes the whole identity mineru_required even when a later twin is valid. Preferring a usable (paper.toml-bearing) duplicate needs a defined usability predicate."
falsifier = "an identity with a malformed first twin and a valid later twin routes on the valid twin"

[[pr_finding_disposition]]
id = "F115-7"
canonical_class = "DEBT-SCIENTIFIC-EVIDENCE"
site = "document_mineru_replay.rs table-risk heuristic"
disposition = "TRACKED (evolve; output-changing). Two-column tables (one gutter per row) never count toward TABLE_ALIGNED_LINES_MIN, so a doc with table_count==0 can miss table_risk. Counting single-gutter rows reclassifies committed inventory and needs a reviewed threshold re-tune."
falsifier = "a two-column-table fixture raises table_risk; the committed inventory diff is reviewed and intended"

# Live P0-P2 frontier. These rows are the active execution ledger; historical
# debt-class measurements above retain their original measurement dates.

[[frontier]]
row_id = "p0-document-index"
priority = "P0"
status = "closed"
owner_surface = "registry/markdown_owner_map.toml; registry/knowledge_sources.toml; registry/markdown_governance.toml; registry/canonical/control_plane.sqlite3"
claim = "The Markdown inventory, governance overlay, and SQLite document search index contain the same 131-document set."
evidence_refs = ["docs/engineering/evidence_ledger_operating_contract_2026_08_01.md", "docs/engineering/registry_canonical_architecture.md", "docs/engineering/validation-workflow-rca-2026_08_02.md", "registry/knowledge_sources.toml", "registry/markdown_governance.toml", "registry/canonical/control_plane.sqlite3"]
falsifier = "markdown-registry verify-all, governance-verify validate-all, or a provenance index reports a document-count or path mismatch"
next_action = "Regenerate the two Markdown registry views after every governed Markdown change, then reindex SQLite."
closure_condition = "The generators emit 131 rows, provenance indexes documents=131, and the inventory, owner-map, parity, header, and mirror checks pass."
document_updates = ["docs/engineering/evidence_ledger_operating_contract_2026_08_01.md", "docs/engineering/registry_canonical_architecture.md"]

[[frontier]]
row_id = "p0-planning-row-reconciliation"
priority = "P0"
status = "closed"
owner_surface = "registry/canonical/control_plane.sqlite3 planning tables and typed gororoba-db planning CLI"
claim = "Obsolete endpoint-probing work and stale registry views reconcile through retained evidence while live Voyager, optics, and missing external-input boundaries remain open."
evidence_refs = ["data/output/pds_sbn_nh_swap_endpoints_2026_05_10.txt", "docs/engineering/local-governance-gate-evidence-2026_08_01.md", "docs/engineering/voyager2-bartol-amda-boundary-2026_08_01.md", "reports/gates/2026-08-01/212938/summary.md", "registry/todo.toml", "registry/roadmap.toml", "registry/next_actions.toml"]
falsifier = "A closed row lacks the named retained artifact, or a live row is marked closed while its stated falsifier remains possible."
next_action = "Preserve the explicit T-058 external-input boundary, keep WS-OPTICS-GR-001 open, and admit the two missing datasets before the next full Rust validation."
closure_condition = "T-062 and T-063 are done with evidence references; T-051 records its interrupted legacy run and current passing validation result; and T-058 plus WS-OPTICS-GR-001 retain explicit open boundaries."
document_updates = ["docs/engineering/evidence_ledger_operating_contract_2026_08_01.md", "plans/repo_debt_taxonomy_roadmap_2026_06_04.toml"]

[[frontier]]
row_id = "p0-validation-pipeline"
priority = "P0"
status = "closed"
owner_surface = "Makefile; .githooks/pre-push; xtask/src/main.rs; local Rust and registry validation commands"
claim = "The active validation pipeline provides a reproducible substitute for dormant or incomplete remote workflow coverage without repeating the same verifier or build session."
evidence_refs = ["Makefile", ".githooks/pre-push", "xtask/src/main.rs", "docs/engineering/validation-workflow-rca-2026_08_02.md", "docs/engineering/evidence_ledger_operating_contract_2026_08_01.md"]
falsifier = "A required validation is absent, silently non-blocking, invokes a compatibility alias in an active path, rebuilds the shared tool bundle more than once per run, or cannot be replayed from a clean isolated worktree."
next_action = "Use the retained validation report as the baseline, then measure future dependency-closure reductions before changing the broad validation package boundary."
closure_condition = "validate-local, validate-ci, validate-registry, validate-static, ASCII, terminology, and scoped Rust lanes pass with retained output and no bypass flags; the shared validation tools build once; legacy acceptance and formatter-baseline work remain identified as historical or separate compatibility surfaces."
document_updates = ["docs/engineering/evidence_ledger_operating_contract_2026_08_01.md", "docs/engineering/validation-workflow-rca-2026_08_02.md", "plans/repo_debt_taxonomy_roadmap_2026_06_04.toml"]

[[frontier]]
row_id = "p1-voyager-bartol-amda-comparator"
priority = "P1"
status = "partial"
owner_surface = "crates/data_core/src/catalogs/voyager.rs; crates/gororoba_cli_data/src/bin/bartol_spdf_crossval.rs; Voyager source lane"
claim = "The retained comparison validates the SPDF proxy for speed but does not validate the requested Bartol to AMDA B-field comparison."
evidence_refs = ["docs/engineering/voyager2_v2_bartol_spdf_1990_1995_findings_2026_04_19.txt", "docs/engineering/voyager2-bartol-amda-boundary-2026_08_01.md", "data/output/bartol_vs_spdf_v2_bfield_correlation_1990_1995.csv"]
falsifier = "A hashed AMDA Voyager 2 magnetometer capture for 1990-1995 and a replayed AMDA comparator output enter the checkout."
next_action = "Acquire the declared AMDA product, retain raw bytes and hashes, add the source row, and replay the comparator."
closure_condition = "The AMDA-specific capture, parser identity, units, overlap counts, and comparator output are retained and reviewed."
document_updates = ["docs/engineering/evidence_ledger_operating_contract_2026_08_01.md", "docs/engineering/voyager2-bartol-amda-boundary-2026_08_01.md"]

[[frontier]]
row_id = "p1-rocq-project-completeness"
priority = "P1"
status = "partial"
owner_surface = "proofs/_RocqProject; proofs/theories; proofs/Makefile"
claim = "The formal proof surface has bidirectional source parity and replayable pinned Rocq gates, while explicit axiom and parameter declarations remain a separate assumption boundary."
evidence_refs = ["proofs/_RocqProject", "proofs/Makefile", "crates/gororoba_cli_data/src/bin/rocq_project_audit.rs", ".github/workflows/proofs.yml", "docs/engineering/rocq-proof-gate-evidence-2026_08_01.md"]
falsifier = "rocq-project-audit reports a mismatch, a pinned vos or vok gate fails, or an axiom or parameter lacks a named disposition and falsifier."
next_action = "Maintain rocq-project-audit and the pinned vos/vok replay, then bind the 169 remaining axiom or parameter declarations to named dispositions."
closure_condition = "rocq-project-audit reports 297 listed and 297 inventory sources, both pinned gates pass, and every remaining axiom or parameter has a named disposition with a falsifier."
document_updates = ["docs/engineering/evidence_ledger_operating_contract_2026_08_01.md", "docs/engineering/rocq-proof-gate-evidence-2026_08_01.md", "docs/requirements/rocq.md", "plans/repo_debt_taxonomy_roadmap_2026_06_04.toml"]

[[frontier]]
row_id = "p1-formal-evidence-registry"
priority = "P1"
status = "partial"
owner_surface = "formal proof field schema, registry theorem rows, and retained proof reports"
claim = "Formal proof status becomes research-quality only when theorem identity, assumptions, gate result, toolchain, and evidence hash share one record."
evidence_refs = ["docs/engineering/formal_proof_field_schema_2026_05_09.md", "docs/engineering/rocq-proof-gate-evidence-2026_08_01.md", "registry/canonical/control_plane.sqlite3", "registry/markdown_export/theorems_registry_mirror.rs", "proofs/"]
falsifier = "A theorem is described as proved without a replayable gate result or without an explicit assumption boundary."
next_action = "Bind the retained Rocq gate report to the formal proof registry with one typed theorem-evidence record shape."
closure_condition = "The formal registry and retained proof report expose the same theorem count, assumption set, toolchain, and gate status."
document_updates = ["docs/engineering/evidence_ledger_operating_contract_2026_08_01.md", "docs/engineering/rocq-proof-gate-evidence-2026_08_01.md", "plans/repo_debt_taxonomy_roadmap_2026_06_04.toml"]

[[frontier]]
row_id = "p2-randomness-run-manifests"
priority = "P2"
status = "partial"
owner_surface = "rand call sites, experiment binaries, and data/output run manifests"
claim = "A scientific run is replayable only when its seed, generator, toolchain, feature set, hardware, and input hashes are retained."
evidence_refs = ["docs/engineering/experiment_manifest_schema.md", "crates/gororoba_cli_data/src/bin/experiment_manifest.rs", "crates/", "data/output/"]
falsifier = "A stochastic experiment can produce a committed result without a seed, generator identity, or input hash."
next_action = "Classify the direct entropy call sites and require new scoped runs to pass experiment-manifest before evidence registration."
closure_condition = "Every scoped stochastic experiment emits a complete manifest and the manifest verifier rejects missing identity fields."
document_updates = ["docs/engineering/evidence_ledger_operating_contract_2026_08_01.md", "plans/repo_debt_taxonomy_roadmap_2026_06_04.toml"]

[[frontier]]
row_id = "p2-materials-source-contract"
priority = "P2"
status = "partial"
owner_surface = "crates/materials_data/data; crates/materials_data/build.rs; crates/materials_core/src/optical_database.rs; crates/materials_core/src/crystal_symmetry.rs"
claim = "Large transcribed materials tables require explicit source, units, provenance, and hash metadata before they support research claims."
evidence_refs = ["crates/materials_data/data", "crates/materials_data/build.rs", "crates/materials_core/src/optical_database.rs", "crates/materials_core/src/crystal_symmetry.rs", "docs/engineering/materials-data-source-contract-2026_08_01.md", "docs/engineering/experiment_manifest_schema.md"]
falsifier = "The source tables, units, and replayable provenance manifest are present and validated by a source-contract gate."
next_action = "Retain hashes for the canonical materials_data inputs and add a source-contract verifier before declaring parity complete."
closure_condition = "Optical and crystal data consumers resolve to versioned source records with units, hashes, and a passing source-contract verifier."
document_updates = ["docs/engineering/evidence_ledger_operating_contract_2026_08_01.md", "docs/engineering/materials-data-source-contract-2026_08_01.md", "plans/repo_debt_taxonomy_roadmap_2026_06_04.toml"]

[[frontier]]
row_id = "p2-structural-debt-slices"
priority = "P2"
status = "open"
owner_surface = "largest hand-written Rust modules and their public crate boundaries"
claim = "Structural splits reduce coupling only when the public API, tests, and measured behavior remain stable."
evidence_refs = ["crates/materials_core/src/optical_database.rs", "crates/algebra_analysis/src/boxkites/tests.rs", "crates/provenance_store/src/lib.rs", "plans/repo_debt_taxonomy_roadmap_2026_06_04.toml"]
falsifier = "A proposed split changes public behavior, loses tests, or has no before and after measurement."
next_action = "Choose one mechanism boundary, measure its baseline, split through re-exports, and run scoped clippy and nextest."
closure_condition = "Each structural slice has a named owner, stable API checks, passing tests, and a retained line-count and dependency delta."
document_updates = ["docs/engineering/evidence_ledger_operating_contract_2026_08_01.md", "plans/repo_debt_taxonomy_roadmap_2026_06_04.toml"]
