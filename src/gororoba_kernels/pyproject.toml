[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "gororoba-kernels"
version = "0.1.0"
description = "Rust-accelerated kernels for Cayley-Dickson algebra and GRIN optics"
requires-python = ">=3.11"

[tool.maturin]
features = ["pyo3/extension-module"]
