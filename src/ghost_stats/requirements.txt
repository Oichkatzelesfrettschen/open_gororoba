# Ghost Spectral Audit -- Python dependencies
# Sprint 50: Multidimensional falsification of phi^{-1/2} spectral claims
#
# Install: python -m venv .venv && source .venv/bin/activate && pip install -r requirements-ghost-audit.txt

astropy>=7.0
spectrum>=0.9
scipy>=1.14
statsmodels>=0.14
numpy>=2.0
matplotlib>=3.9
