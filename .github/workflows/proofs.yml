name: proofs

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'proofs/**'
      - '.github/workflows/proofs.yml'
  pull_request:
    paths:
      - 'proofs/**'
      - '.github/workflows/proofs.yml'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - name: Install opam
        run: |
          sudo apt-get update
          sudo apt-get install -y opam

      - name: Cache opam
        id: opam-cache
        uses: actions/cache@v4
        with:
          path: ~/.opam
          key: opam-${{ runner.os }}-${{ runner.arch }}-ubuntu24-ocaml5.4.0-rocq9.1.1-flocq4.2.2-v1

      - name: Install Rocq 9.1
        if: steps.opam-cache.outputs.cache-hit != 'true'
        run: |
          opam init --bare --disable-sandboxing --yes
          opam switch create rocq-9.1.1 ocaml-base-compiler.5.4.0 --yes
          opam repository add rocq-released https://rocq-prover.org/opam/released --switch=rocq-9.1.1 --yes
          eval "$(opam env --switch=rocq-9.1.1)"
          opam install rocq-core.9.1.1 rocq-stdlib.9.1.1 coq-flocq.4.2.2 --yes

      - name: Compile all proofs
        working-directory: proofs
        run: |
          eval "$(opam env --switch=rocq-9.1.1)"
          make all

      - name: Zero-Admitted check
        working-directory: proofs
        run: |
          eval "$(opam env --switch=rocq-9.1.1)"
          make check

      - name: Extraction golden-file diff
        working-directory: proofs
        run: |
          eval "$(opam env --switch=rocq-9.1.1)"
          make extract-check
