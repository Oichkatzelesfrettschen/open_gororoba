[package]
name = "gororoba_gpu_bridge"
version = "0.1.0"
edition = { workspace = true }
description = "GPU backend selection and dispatch for gororoba simulation workloads"
license = "GPL-2.0-or-later"

[dependencies]
core_affinity = { workspace = true }

[features]
default = []
gpu = []

[lints]
workspace = true
