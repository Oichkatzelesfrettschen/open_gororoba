[package]
name = "repo_root"
version = "0.1.0"
edition = { workspace = true }
license = "GPL-2.0-or-later"
description = "Runtime discovery of the open_gororoba workspace root for tools that read registry, data, or proof files."

[dependencies]

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
