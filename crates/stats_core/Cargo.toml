[package]
name = "stats_core"
version = "0.1.0"
edition = { workspace = true }
license = "MIT"
description = "Statistical methodology for physics analysis: Frechet distance, bootstrap CI, Haar unitaries"

[features]
default = []
# The explicit cudarc feature preserves the old public feature name while gpu
# enables the same CUDA surface.
cudarc = ["dep:cudarc", "dep:gororoba_gpu_cuda", "gororoba_gpu_cuda/cudarc"]
gpu = ["cudarc"]
cubecl = [
    "dep:cubecl",
    "dep:cubecl-wgpu",
    "dep:gororoba_gpu_cubecl",
    "gororoba_gpu_cubecl/cubecl",
]
vulkan = [
    "dep:gororoba_gpu_vulkan",
    "gororoba_gpu_vulkan/ash",
]

[dependencies]
clarabel = "0.11.1"
cubecl = { workspace = true, optional = true }
cubecl-wgpu = { workspace = true, optional = true }
cudarc = { workspace = true, optional = true }
gororoba_algebra = { path = "../gororoba_algebra" }
gororoba_gpu_cubecl = { workspace = true, optional = true }
gororoba_gpu_cuda = { workspace = true, optional = true }
gororoba_gpu_vulkan = { workspace = true, optional = true }
kiddo.workspace = true
kodama = { workspace = true }
nalgebra = { workspace = true }
num-complex = { workspace = true }
rand = { workspace = true }
rand_chacha = { workspace = true }
rand_distr = { workspace = true }
statrs.workspace = true

[dev-dependencies]
approx = { workspace = true }

[lints]
workspace = true
