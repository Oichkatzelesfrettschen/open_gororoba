[package]
name = "lattice_filtration"
version = "0.1.0"
edition = { workspace = true }
license = "GPL-2.0-or-later"
repository = "https://github.com/Oichkatzelesfrettschen/open_gororoba"

[dependencies]
gororoba_algebra = { path = "../gororoba_algebra", version = "0.1" }
petgraph.workspace = true
rand.workspace = true
sign_imbalance = { version = "0.1.0", path = "../sign_imbalance" }

[dev-dependencies]
criterion = { workspace = true }
proptest = { workspace = true }
stats_core = { path = "../stats_core" }

[[bench]]
name = "filtration_bench"
harness = false

[lints]
workspace = true
