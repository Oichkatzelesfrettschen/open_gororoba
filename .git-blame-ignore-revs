# `git blame` reference: commits listed here are skipped when computing
# line authorship. Use this for repository-wide reformatting commits whose
# changes have no semantic content.
#
# Activate locally with:
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub uses this file automatically on the web UI.

# 2026-05-14: cargo fmt --all baseline sweep across 115 source files.
# Realigns the workspace to rustfmt.toml after accumulated drift across
# multi-session feature branches. Zero semantic change; verified with
# `cargo check --workspace` and `cargo clippy --workspace -- -D warnings`.
c29cd2cf30898b8413e8ffb40b378f2f3ed65f39  # PR #29 squash merge
