Legacy audit reconciliation -- single active audit log
=======================================================

This file is the one active audit log. Earlier bundle files under
data/output/audit/<date>/ are frozen evidence snapshots; their open items
either close here (with the closing evidence named) or roll forward into
the ACTIVE section below. A future audit bundle supersedes this file by
carrying its own rollforward that closes or re-rolls each ACTIVE item.

RESOLVED (stays resolved; do not re-open without new evidence)
--------------------------------------------------------------
- 2026-04-30/11-clippy.log: workspace clippy exit 0 (7m20s). Superseded by
  2026-07-16/11-clippy-workspace.log (PASS, deny-warnings, stable 1.97.0).
- 2026-04-30/12-nextest-norun.log: workspace test-profile build exit 0.
  Superseded by the six-gate pre-push chain (34/34 nextest, 2026-07-16).
- 2026-04-30/13-proofs-vos.log: proofs vos exit 0 ("Nothing to be done",
  incremental). Superseded by the full clean rebuild: 296 .vo exit 0 under
  Rocq 9.1.1 / OCaml 5.5.0 (2026-07-16, commit 365eafa0).
- 2026-04-30/14-geiger.log: `cargo geiger --workspace` fails with "virtual
  manifest ... requires an actual package" (exit 1). Closed by the scoped
  invocation with an absolute --manifest-path against cd_kernel:
  2026-07-16/12-geiger-cd_kernel.log.
- 2026-04-30/14-geiger-cd-kernel.log: unsafe census snapshot, dep tree
  376/1067 fns, 25200/62444 exprs, "544 warnings" (geiger's count of
  unsafe-bearing crates, exit 1 is the unsafe-found convention, not an
  error). Superseded by 2026-07-16/12-geiger-cd_kernel.log: cd_kernel
  5/5 fns, 147/327 exprs, 4/4 impls; dep tree 171/1069, 13550/62824.
  The dep-tree shrink 376->171 fns tracks the stable-toolchain repin and
  fwht git-rev resolution (commit 4a5f17a1).
- 2026-04-30/01-cargo-metadata-{full,no-deps}.json: 6.5 MB of regenerable
  `cargo metadata` output. Gitignored (data/output/audit/**/*cargo-
  metadata*.json); the lockfile at any commit reproduces them.
- 2026-07-14/00-BLOCKER-compile-stages.txt: all three blocked stages
  (clippy, geiger, dep-audit) ran green 2026-07-16; see
  00-summary-compile-stages-unblocked.txt in this bundle.

RESOLVED IN THIS BUNDLE (closing evidence inline)
--------------------------------------------------
- proc-macro-error2 v2.0.1 future-incompatibility (E0365, private
  proc_macro re-export at proc-macro-error2 src/lib.rs:494): the
  release-build path is CLOSED. `cargo tree -i proc-macro-error2`
  showed two parents: hdf5-metno-derive 0.10.0 (release path via
  hdf5-metno 0.12.4) and iai-callgrind-macros 0.6.1 (dev-only, bench
  harness). hdf5-metno-derive 0.10.1 drops the proc-macro-error2
  dependency entirely (crates.io index deps list); `cargo update -p
  hdf5-metno-derive` locks 0.10.1. RESIDUAL (dev-only, upstream-
  blocked): iai-callgrind-macros 0.6.1 is the latest release and still
  depends on proc-macro-error2 2.0.1, itself the latest release with no
  fixed successor; the warning therefore persists in --all-targets
  builds until either upstream ships a fix. Tracking:
  https://github.com/GnomedDev/proc-macro-error-2.
- cd_kernel SAFETY-comment coverage: CLOSED. Every unsafe site now
  carries a discharge: x87_transcendentals.rs (4 asm blocks),
  x87_jacobi_kernels.rs (1 asm block), turboquant/cuda/launch.rs (2
  kernel launches), turboquant/fp8.rs (Pod/Zeroable contract expanded).
  avx2_primitives.rs, x87_primitives.rs, x87_ext80.rs, and the cubecl
  launcher already discharged each block; turboquant/vulkan/quantizer.rs
  discharges through its file-level "Universal SAFETY argument" plus
  targeted comments, mirroring lbm_vulkan/compute.rs. Verified: clippy
  -p cd_kernel --all-targets clean, 395/395 lib tests pass.
- proofs/.nia.cache: CLOSED as gitignored. The file is a Rocq nia
  tactic binary cache (Coq! magic header), the direct sibling of the
  already-ignored *.lia.cache / *.nra.cache; nothing in _RocqProject,
  the Makefile, the registries, or the hooks references it. Untracked
  via `git rm --cached` with *.nia.cache added to the cache pattern
  block in .gitignore.
- themis-staples-score-export empty-file guard: post-merge review
  (staple_associator module + both binaries) found one hardening gap --
  a file parsing to zero valid rows indexes times[0] when
  --min-samples 0 is passed. Fixed with an explicit rows.is_empty()
  guard. The review confirmed the embedding, table-multiply,
  associator, and log-moment math sound and the permissive-row-skip
  parsing intentional.

ACTIVE (rolls forward; close with named evidence in the next bundle)
--------------------------------------------------------------------
- iai-callgrind-macros -> proc-macro-error2 dev-only residual (see
  above): re-check `cargo info iai-callgrind-macros` and the
  proc-macro-error2 repository for releases; close by cargo update once
  either ships without the E0365 pattern.
- 2026-05-12/gate-timing-1778607296.jsonl is a single 6-phase timing
  sample from the gate-optimization work (total 16.2 s scoped run).
  It is tracked as frozen evidence; no further action.
