schema_version = 1
executed_producer_source = "executed-intake-producer.rs.txt"
producer_source_note = "The executed source precedes a test-module relocation required by all-target Clippy; production transformation semantics remain identical. The ledger retains its exact source and executable hashes."
frozen_checkout_commit = "c86e943b608ccd0d0441bd3dd3b8361967fe1763"
frozen_original_commit = "8bbf201a00ae3be2a3a6a31f492e067b96521654"
frozen_identity = "frozen-source-identity.json"
frozen_source_verification = "All 20 file hashes match the retained training source map."
original_archive = "data/output/audit/staples-causal-validation/source-archives/external-intake.tar.zst"
original_archive_sha256 = "9cab6bb3047a931dde66516dd36d59d9ef1d285ec0060cbad735a5f8aa10a22b"
archive_retrieval = "The non-LFS scientific-payload archive in data/retention/scientific-payloads.json retains the original archive as a hash-addressed member. Hydrate through the Rust hydrate-scientific-payloads verifier."
derive = "cargo run --profile validation -p gororoba_cli_physics --bin staples-intake-amendment -- --parent-manifest data/output/audit/staples-causal-validation/external-intake-manifest.json --input-root .cache/staples-external-intake --protocol data/output/audit/external-crossing-intake-amendment/protocol.toml --out-dir .cache/external-crossing-derived-inputs"
catalog_placement = "Copy the hash-verified V2 catalog into the derived input root at data/external/crossing_lists/themis_mp_crossings_v2.txt."
inference = "Launch the rebuilt frozen source checkout's staples-causal-validation from that checkout. Supply absolute --input-root, --protocol, --out-dir, --external-manifest, --file-map and --models-dir paths with --mode external. The model directory is the retained staples-causal-validation/results directory."
support_audit_build = "rustc --edition 2024 -D warnings -O data/output/audit/external-crossing-intake-amendment/support-audit.rs -o .cache/support-audit"
support_audit = ".cache/support-audit .cache/external-crossing-derived-inputs .cache/external-crossing-frozen-results"
support_archive = "supports.tar.zst"
support_archive_creation = "tar --sort=name --mtime=@0 --owner=0 --group=0 --numeric-owner -cf - -C .cache/external-crossing-frozen-results supports | zstd -T1 -10 -o data/output/audit/external-crossing-intake-amendment/supports.tar.zst"
retention_boundary = "Original source bytes remain in the verified public archive; deterministic transformation plus inverse ledger reproduces derived inputs. Ordinary Git retains every result JSON, the independent auditor and all compressed support records."
