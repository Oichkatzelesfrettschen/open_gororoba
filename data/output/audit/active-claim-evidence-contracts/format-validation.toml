workspace_check = "cargo +nightly fmt --all -- --check"
initial_workspace_exit = 1
initial_failing_files = 31
unchanged_baseline_failing_files = 24
changed_or_moved_failing_files = 7
repair = "Apply the repository formatter to the seven changed or moved files, then require their focused check to pass. The remaining 24 reported files have byte-identical content to base commit 33c21a6f."
focused_exit = 0
boundary = "The workspace-wide formatter has retained pre-existing failures outside the edited surfaces. New runner and schema files pass their focused formatting checks; the broad failure does not establish a clean workspace-wide format state."
