[package]
name = "lambda-gororoba-aggregator"
edition = "2021"
version = "0.1.0"
description = "Pure Rust orchestration and metadata layer for the lambda workspace"
license = "GPL-2.0-only"
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
