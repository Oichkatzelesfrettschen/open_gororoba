# Upstream blockers registry.
#
# Tracks third-party crates and tools whose upstream state is preventing
# in-repo cleanup. Each entry names the dependency, the desired upstream
# state, the in-repo work that is blocked, and a watch-target.
#
# Plan: T-158 (rocq-native), T-119 (burn 0.21), and others may add entries.
# Schema is intentionally simple while this lane lives outside the SQLite
# canonical path; will be promoted by Phase 3 schema validator (T-131) if
# adoption sticks.

[meta]
schema_version = 1
generated_at = "2026-04-30"
generated_by = "T-119"

# ----------------------------------------------------------------------------
# burn 0.21 / iai-callgrind upgrade chain
# ----------------------------------------------------------------------------
[[blocker]]
id = "UB-001"
crate = "iai-callgrind"
current_version = "0.16.1"
desired_state = "iai-callgrind upgrades past bincode 1.3.3 (pulling bincode >= 2.0)"
blocks = [
    "removal of RUSTSEC-2025-0141 from deny.toml ignore list",
]
related_advisory = "RUSTSEC-2025-0141"
related_adr = "docs/adr/rustsec-dispositions.md"
notes = """
iai-callgrind is a dev-dep used for performance benchmarks. It is the only
remaining consumer of bincode 1.3.3 in this workspace. Production code
already uses bincode 2.0.1 via burn-core 0.20.1. burn 0.21+ may also
consolidate transitive deps but the bincode 1.x dep is iai-callgrind's,
not burn's, so the burn upgrade is independent.
"""
watch_target = "https://github.com/iai-callgrind/iai-callgrind/releases"
last_checked = "2026-04-30"

# ----------------------------------------------------------------------------
# rustls-webpki -- already resolved 2026-04-30, kept for traceability
# ----------------------------------------------------------------------------
[[blocker]]
id = "UB-002"
crate = "rustls-webpki"
current_version = "0.103.13"
desired_state = "RESOLVED"
blocks = []
related_advisory = "RUSTSEC-2026-0098, 0099, 0104"
related_adr = "docs/adr/rustsec-dispositions.md"
notes = """
Resolved 2026-04-30 via cargo update -p rustls-webpki --precise 0.103.13.
Kept here so a future audit can see that this crate WAS a blocker.
"""
watch_target = "n/a (resolved)"
last_checked = "2026-04-30"

# ----------------------------------------------------------------------------
# core2 / image / rav1e chain -- yanked, awaiting upstream migration
# ----------------------------------------------------------------------------
[[blocker]]
id = "UB-003"
crate = "core2"
current_version = "0.4.0 (YANKED)"
desired_state = "bitstream-io migrates off core2 to embedded-io or no-std-io2"
blocks = [
    "removal of RUSTSEC-2026-0105 from deny.toml ignore list",
    "clean cargo deny status without yanked-warning",
]
related_advisory = "RUSTSEC-2026-0105"
related_adr = "docs/adr/rustsec-dispositions.md"
notes = """
core2 is yanked and unmaintained. Path:
  image -> ravif -> rav1e -> bitstream-io -> core2.
Suggested alternatives (embedded-io, no-std-io2) require bitstream-io
or rav1e/ravif/image upstream to migrate. We do not deserialize untrusted
AVIF/AV1 in production paths; image is loaded for static-asset rendering
only. Mitigation accepted; tracked here for re-check.
"""
watch_target = "https://github.com/tomaszkubacki/bitstream-io/issues"
last_checked = "2026-04-30"

# ----------------------------------------------------------------------------
# rand soundness -- waiting for non-breaking patch
# ----------------------------------------------------------------------------
[[blocker]]
id = "UB-004"
crate = "rand"
current_version = "0.10.0 (and 0.9.2, 0.8.5 transitive)"
desired_state = "rand publishes a soundness-fixed patch that does not break the API"
blocks = [
    "removal of RUSTSEC-2026-0097 from deny.toml ignore list",
]
related_advisory = "RUSTSEC-2026-0097"
related_adr = "docs/adr/rustsec-dispositions.md"
notes = """
Advisory affects soundness only when a custom rand logger is installed via
register_custom_getrandom. We have zero callers of that function (verified
in T-110..T-113 audit; T-118 supply-chain-gate enforces this going forward).
Exposure is provably zero in this workspace. A breaking-change rand upgrade
would be unjustified solely for this advisory.
"""
watch_target = "https://github.com/rust-random/rand/releases"
last_checked = "2026-04-30"

# ----------------------------------------------------------------------------
# Rocq native_compute / base-nnp / OCaml 5
# ----------------------------------------------------------------------------
[[blocker]]
id = "UB-005"
crate = "rocq-native"
current_version = "blocked"
desired_state = "base-nnp (or replacement) compatible with OCaml 5; rocq-native installable on Rocq 9.1.1"
blocks = [
    "use of native_compute in Rocq proofs (currently using vm_compute as fallback)",
    "potential proof-build acceleration in Phase 5",
]
related_advisory = "n/a"
related_adr = "n/a"
notes = """
Memory: 'rocq-native BLOCKED: conflicts with base-nnp on OCaml 5.
native_compute -> vm_compute.' This is a tracked Rocq tooling blocker, not
a security one, but lives in the same registry for consistency.
"""
watch_target = "https://github.com/rocq-prover/rocq/issues -- search 'native base-nnp'"
last_checked = "2026-04-30"
