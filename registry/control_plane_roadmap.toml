# Canonical mirror for historical Wave 4 roadmap.
# Archival original remains immutable at registry/wave4_roadmap.toml.

[meta]
authoritative = true
updated = "2026-03-07"
mirror_created = "2026-03-07"
intent = "Canonical control-plane roadmap entry point with archival Wave 4 provenance and live gate-remediation tranche tracking."
canonical_path = "registry/control_plane_roadmap.toml"
legacy_path = "registry/wave4_roadmap.toml"
source_table = "wave4_roadmap"
canonical_table = "control_plane_roadmap"

[control_plane_roadmap]
id = "CONTROL-PLANE-ROADMAP"
legacy_id = "WAVE-4"
legacy_label = "wave4"
title = "TOML-first control plane hardening"
status = "in_progress"
objective = "Enforce one-source-of-truth TOML registries and eliminate markdown state drift."
phase_count = 5
task_count = 32

[provenance]
legacy_updated = "2026-02-10"
legacy_sha256 = "59c159c64c389e96ef750120196ad0cf59d35e797eeb25d4f7ae2852811f7697"
legacy_line_count = 259
task_status_complete = 27
task_status_in_progress = 4
task_status_pending = 2
chronology_policy = "Preserve original phase/task IDs and completion state as archival truth."

[compatibility]
legacy_aliases = ["registry/wave4_roadmap.toml", "wave4_roadmap", "WAVE-4"]
canonical_aliases = ["registry/control_plane_roadmap.toml", "control_plane_roadmap", "CONTROL-PLANE-ROADMAP"]
bidirectional_mapping = true

[archival_policy]
legacy_mutation = "forbidden"
canonical_mutation = "allowed_with_provenance"
backport_to_legacy = "never"

[control_plane_roadmap.current_tranche]
id = "CP-GATES-2026-03"
title = "Required gate stabilization and tracker truthfulness"
status = "in_progress"
status_token = "IN_PROGRESS"
objective = "Convert the current gate-fix branch from opportunistic edits into a tracked control-plane tranche that ends with green required gates and truthful TOML-first mirrors."
owners = ["WS-QUALITY-001", "WS-CLAIMS-001", "WS-REGISTRY-001", "WS-GATES-001"]
blocking_todos = ["T-043", "T-045", "T-046", "T-047", "T-048", "T-049", "T-050", "T-051"]
active_next_actions = ["NA-032", "NA-033", "NA-034", "NA-035", "NA-036"]
exit_criteria = [
  "gate-local passes on the canonical branch state.",
  "gate-ci-python passes on the canonical branch state.",
  "gate-ci-rust passes on the canonical branch state.",
  "gate-audit produces a current summary under reports/gates/ with no failing steps.",
  "Generated roadmap/todo/next-actions mirrors match the authoritative TOML state.",
]

[control_plane_roadmap.current_gate_state]
check = "passing"
gate_local = "fast_path_retuned_nextest_target_mapping_pending"
gate_ci_python = "passing"
gate_ci_rust = "pending_revalidation_with_repo_local_cargo_home"
gate_audit = "pending_revalidation"
notes = [
  "The repo is no longer being tracked as ad hoc one-off surprises; the current remediation effort is bucketed under CP-GATES-2026-03.",
  "Tracker truth, mirror refresh, and the authoritative Python gate are green on the current branch state.",
  "The Python control-plane fast path now uses ripgrep-backed discovery where available plus worker-budget-bounded parallel scanning; do not introduce speculative JIT work before profiling proves a numeric hotspot.",
  "The terminology gate now uses ripgrep as its candidate-line prefilter and only falls back to pure Python scanning when rg is unavailable.",
  "Local Rust routing no longer escalates to --workspace for every shared file change; gate-local now runs the scoped fast path with clippy plus light nextest only, while the heavy package lane remains authoritative in gate-ci-rust.",
  "The scoped Rust fast path now fails correctly when cargo nextest fails inside rust-regression-scoped; the previous shell-block false-green behavior has been removed.",
  "Local scoped clippy is now limited to --lib --tests so push-time verification no longer compiles every example and bench in large CLI crates before reaching the real test frontier.",
  "Local nextest now skips the known heavy research and GPU-only filterset when RUST_RUN_HEAVY=0, but the workspace still pulls a large number of bin-test harnesses through target layout; the next speed tranche is target-level nextest scope mapping rather than more Cargo lock/cache surgery.",
  "Ambient Cargo tooling now defaults to .cache/cargo-default-target, while the gate runner uses its own .cache/gate-target tree, so background rust-mcp-server checks and other agent shells do not contend with the push gate.",
  "The repository remains ASCII-gated; there is no active migration to general Unicode text and no relaxation for emoji-bearing content.",
  "The remaining open tranche bucket is the Rust gate under the repo-local Cargo home/target isolation policy.",
]
